# 📝 บันทึกงาน — โปรเจคระบบติดตามการผลิตยา (PD Monitor)

> ไฟล์นี้ไว้สำหรับ "ส่งต่องาน" — พรุ่งนี้กลับมาเปิดอ่านไฟล์นี้ก่อน จะรู้ว่าวันนี้ทำอะไรไป และต้องทำอะไรต่อ

---

## 📅 บันทึกวันที่ 28 สิงหาคม 2569 — Part D ครบ 4 ก้อน (0070–0073) 🎉

### ที่มา
ทีมส่ง requirement หน้า Notion **Part D** (`3c892ef2-c18f-800c-bffb-df3d4de1ed3f`) มา **5 หมวด**
ผู้ใช้สั่งให้ทำ **เฉพาะ 3 หมวดแรก** ก่อน — หมวด "ปริ้นใบแจ้งผลิต" + "ปริ้นตารางบอร์ดงาน" ยกไปรอบหน้า
(2 หมวดนั้นต้องใช้ `ลักษณะยา` + `บริษัท` จากรอบนี้เป็นวัตถุดิบอยู่แล้ว ลำดับนี้จึงถูก)
แผนเต็ม: `~/.claude/plans/indexed-floating-treehouse.md`

> **แก่นของ Part D มี 2 เรื่อง**
> 1. **เติมข้อมูลที่ใบแจ้งผลิตต้องใช้** — ลักษณะยา + บริษัท (โรงงานเดินงานให้ 2 บริษัท เดินเลขคนละชุด)
> 2. **เก็บบั๊กหน้างานจาก Part C.4** — Incident Case ถอดแผนกแล้วปิดไม่ได้ · ขอแก้ไข in-process แล้วไม่มีใครอนุมัติได้

### ✅ ก้อน 1 — ลักษณะยา (`0070` · commit `b30ae28`)
- `products.appearance` — ช่อง "รูปร่างลักษณะยา" บนใบแจ้งผลิต F.PLN.01 (มีบันทึกไว้ตั้งแต่ `docs/feedback-raw-2026-08.md:54,60`)
- ฟอร์มเพิ่ม/แก้ผลิตภัณฑ์มี textarea เต็มแถว · แสดงบนการ์ด · แสดงในหน้ารายละเอียดงาน (**อ่านอย่างเดียว** — เป็นของทะเบียนยา ไม่ใช่ของงาน)
- 🚨 `drop function upsert_product(6 พารามิเตอร์)` ก่อน create 7 พารามิเตอร์ — กัน `PGRST203`

### ✅ ก้อน 2 — บริษัท + แยกเลข Job No. (`0071` · commit `313d154`)
- ตาราง `companies` (โครงลอกจาก `job_sub_statuses` 0053) + seed **UMEDA / POUND**
- 🔑 **วิธีแก้ปัญหาเลขซ้ำข้ามบริษัท** — `jobs.job_no` เป็น unique และเป็น URL slug ที่ 35 ไฟล์ใช้อยู่
  → เก็บใน DB **มีอักษรนำต่อบริษัท** (`690001` = UMEDA · `P690001` = POUND) แต่**หน้าจอตัดทิ้งด้วย `displayJobNo()`**
  แล้วโชว์ป้ายบริษัทกำกับ · **งานเก่า 10 ใบไม่ต้องแตะเลขเลย** ลิงก์เดิมใช้ได้ปกติ
- `job_no_counters` เปลี่ยน PK เป็น `(company_id, year_be)` · `next_job_nos(company, count)`
- `admin_set_job_no_config` — ตั้ง "เลขถัดไป" (ใช้ทันที) + "เลขตั้งต้นปีใหม่" · **ตั้งย้อนหลังถูกปฏิเสธ** (จะชนเลขที่ออกไปแล้ว)
- `jobs.company_id/company/note` + **หมายเหตุแก้ทีหลังได้ · บริษัทล็อกตาย** (ผูกกับเลขที่ออกไปแล้ว)
- หน้าใหม่ **`/admin/companies` "บริษัท / เลขงาน"** (ผู้บริหารเท่านั้น) + เมนู
- 🎁 เกินแผน 2 อย่าง: **ค้นบนบอร์ดเจอทั้งเลขเปล่า เลขจริง และชื่อบริษัท** · **`stripJobNo()` แปลงเลขที่ SQL ฝังไว้ในหัวข้อแจ้งเตือน** (เช่น "งาน P690001 ถูกตีกลับ") ตอนอ่าน แทนการไล่แก้ฟังก์ชันแจ้งเตือนใน migration เก่าทุกตัว

### ✅ ก้อน 3 — Incident Case (`0072` · commit `45476a5`)
- 🐞 **ต้นเหตุ "ถอดแผนกแล้วปิดเคสไม่ได้": `0068:200` ตั้ง `status='in_progress'` ตรงๆ ทุกครั้ง**
  → **แรงกว่าที่ทีมรายงาน: ไม่ต้องถอดแผนกด้วยซ้ำ** แค่ QA กด "แก้ไขการมอบหมาย" แล้วบันทึกซ้ำ
  เคสที่อยู่ `qa_verify` ก็ถูกดันกลับเป็น `in_progress` → กดปิดทีไรก็โดนด่าน "ปิดข้ามขั้นต้องระบุเหตุผล"
  **แก้:** นับแผนกที่ยังไม่ตอบใหม่หลัง DELETE/INSERT แล้วเลือกสถานะเอง (cast CASE ทั้งก้อนเป็น enum) + แจ้ง QA
- **`current_role_badge()` ฟังก์ชันใหม่** — ป้ายนับ role สูงสุด (ผู้บริหาร/admin มาก่อน)
  🚨 **แยกขาดจาก `current_role_group()`** ที่ใช้จับคู่ `deviation_departments` — ถ้ารวมกัน คน QC ที่มี role admin ด้วยจะกลายเป็น "ผู้บริหาร" แล้ว**ส่งผลแก้ไขแทนแผนก QC ไม่ได้เลย**
- **ปุ่มเดียวต่อบทบาท**: QA เห็น "ตรวจสอบผลแก้ไข" · แผนกที่ถูกมอบหมายเห็น "รายงานผลแก้ไข" (คลิกเดียวจบ) · **คนอื่นไม่มีปุ่ม** (เดิมทุกคนเห็น "อัปเดต / ปิด")
- ตัดช่อง "กำหนดปิด" ออกจากฟอร์ม — ไม่ต้องแก้ signature เพราะ RPC ใช้ `coalesce(p_due_date, due_date)`
- 🐞 `QaReviewForm` ล้าง state จาก props ทุกครั้งที่เปิด — แผนกที่ถอดออกไม่ติ๊กค้างและไม่ถูก insert กลับ

### ✅ ก้อน 4 — In-process approver + ลบ `sample_point` (`0073` · commit `25ff22f`)
- 🐞 **ต้นเหตุ "ขอแก้ไขแล้วไม่มีปุ่มอนุมัติ": ยื่นได้ = qc/qc_lead/manager แต่อนุมัติได้ = manager/qa**
  หัวหน้า QC **ไม่เห็นแม้แต่เมนู** และไม่ได้รับแจ้งเตือน → คำขอค้างไม่มีทางออก
  (กติกาเขียนไว้ตั้งแต่ `0033` ตอนที่ยังไม่มี role `qc_lead` — เพิ่งเกิดที่ `0060` แล้วไม่ได้อัปเดตตาม)
- แก้ **6 จุด** ผ่านค่าคงที่กลาง `EDIT_REVIEWER_ROLES`: guard RPC · แจ้งเตือน · `canReviewEdit` · redirect หน้า · server action · เมนู+badge
- 🎁 `getTargetSnapshot` เพิ่ม `station_id`/`valid_date` — whitelist เปิดให้แก้ตั้งแต่ `0065:136` แต่หน้ารีวิวโชว์ "ค่าเดิม" เป็น "—"
- `drop column qa_samples.sample_point` + เก็บกวาด "จุด/รอบ (ข้อมูลเดิม)" ออกจากการ์ด/ตาราง/eBR ครบ
- ⚠️ **ก้อนนี้ push ก่อน paste** (กลับด้านกับ 3 ก้อนแรก) เพราะโค้ดเดิมบนเว็บยัง `select sample_point`

### 🔑 Decisions ที่ผู้ใช้ยืนยัน
1. **ทำเฉพาะ 3 หมวดแรกของ Part D** — หมวดปริ้น 2 หมวดยกไปรอบหน้า
2. **เลขงาน: เก็บมีอักษรนำ แสดงเลขเปล่า** (ไม่เอาแบบโชว์ `U690001` และไม่เปลี่ยน URL เป็น `/board/umeda/690001`)
3. **"เลขเริ่มต้น" ทำทั้งสองแบบ** — เลขถัดไปตอนนี้ + เลขตั้งต้นปีใหม่
4. **หมายเหตุแก้ทีหลังได้ · บริษัทแก้ไม่ได้**
5. **เพิ่มหัวหน้า QC เป็นผู้อนุมัติคำขอแก้ไข in-process** (ไม่เพิ่มปุ่มอนุมัติในหน้างาน)

### 🧠 บทเรียนรอบนี้
- **ยกบอดี้ฟังก์ชันยาวๆ จาก migration เก่ามาแพตช์ด้วยสคริปต์ python ดีกว่าพิมพ์ใหม่** — รอบนี้ใช้กับ `update_job_details` (288 บรรทัด) · `qa_review_deviation` · `review_edit_request` · assert ว่า match เจอ 1 ครั้งเสมอ ไม่งั้นหยุด
- 🚨 **heredoc พังครั้งที่ 5** (เนื้อหายาว + quote ผสม) → เลิกใช้ เขียนสคริปต์ลง scratchpad แล้วรันแทนตั้งแต่แรก
- **อย่าแทรก import ด้วยวิธี "หาบรรทัด import สุดท้าย"** — เจอ multi-line import แล้วแทรกกลางบล็อกจนไฟล์พัง (`board-view.tsx`) · ต้องเช็กว่าบรรทัดนั้นจบด้วย `;`
- **`raise exception` ใช้ `%` เป็น placeholder — `%%s` จะกลายเป็นข้อความ `%s` แล้วพารามิเตอร์เกิน** (จับได้ตอน review ก่อน paste)
- **แยก "ป้ายที่โชว์" ออกจาก "ฝ่ายที่ใช้ตัดสินสิทธิ์" ให้ขาด** — requirement ขอแค่เปลี่ยนป้าย แต่ถ้าไปแก้ฟังก์ชันเดียวจะพังสิทธิ์การส่งผลแก้ไข
- **ตรวจข้อมูลจริงหลัง paste ช่วยจับของค้างได้** — เจอเคส `022` ที่ค้างจากบั๊กจริง และยืนยันว่า `audit_log` เก็บ `sample_point` ไว้ 19 แถวก่อนกล้า drop คอลัมน์

### ✅ verify แล้ว
- `npm run lint` + `npm run build` ผ่าน 0 error **ทุกก้อน** · push ครบ 4 commit
- **REST verify หลัง paste ครบทุกก้อน:** `products.appearance` มีจริง · `companies` 2 แถว prefix ถูก ·
  `job_no_counters` มี `company_id` · **งานเก่า 10 ใบเป็น UMEDA ครบ ไม่มีใบไหน `company_id` ว่าง** ·
  `qa_samples.sample_point` ได้ `42703` (ลบจริง) · **`audit_log` ยังเก็บค่าเดิมไว้ 19 แถว** ·
  `next_job_nos(integer)` + `upsert_product(6)` ได้ `PGRST202` (drop สำเร็จ · ไม่มี overload ซ้อน) ·
  RPC ใหม่ทุกตัว anon ได้ `42501` · `current_role_group()` ยังอยู่ครบ (ไม่ถูกแตะ)
- 📝 Notion **"Part D.1 claude"** `3ca92ef2-c18f-8177-a2c3-c357d3a52d0f` — 4 ก้อน + เช็กลิสต์กดติ๊ก 28 ข้อ

### 🎉 ข่าวดี: role ครบทุกฝ่ายแล้ว
เรื่องที่เตือนค้างมาตั้งแต่ Part C.3/C.4 ว่าขาดคนถือ role สำคัญ — **ตรวจแล้วมีครบ**
`qa` 4 บัญชี · `qc_lead` 5 · `production_lead` 5 · `manager` 5 · ทุกฝ่ายที่เหลือมีครบ
→ Incident Case / Line Clearance / การอนุมัติผลตรวจ **เดินได้หมดแล้ว**

### ▶️ พรุ่งนี้เริ่มตรงนี้
- 📌 **ซ่อมเคส `022` ด้วยมือ** — QA เปิดเคส → "🔎 แก้ไขการมอบหมาย" → "ส่งให้แผนกที่เกี่ยวข้อง"
  (เป็นเคสเดียวที่ค้างจากบั๊กเดิม · ไม่แก้ตรงๆ ใน DB เพราะประวัติจะไม่ระบุว่าใครทำ)
- 📌 **ตั้งเลขเริ่มต้นของ 2 บริษัท** ที่เมนู "บริษัท / เลขงาน" ก่อนเริ่มใช้งานจริง
  (ตอนนี้ UMEDA ออกไปถึงเลข 11 · POUND ยังไม่เคยออกเลข)
- 🔜 **ทีมทดสอบ UI ตามเช็กลิสต์ 28 ข้อในหน้า Notion "Part D.1 claude"** — หมวดก้อน 3 ต้องสลับหลายบัญชี ทำรวดเดียวคุ้มสุด
- 🔜 **Part D อีก 2 หมวดที่ยกไว้** (อยู่ในหน้า Notion "Part D" เดิม ไม่ต้องเขียนใหม่):
  **ปริ้นใบแจ้งผลิต F.PLN.01** · **ปริ้นตารางบอร์ดงาน 12 คอลัมน์** — วัตถุดิบครบแล้ว (ลักษณะยา + บริษัท)
- 🔜 ค้างจาก Part C: เช็กลิสต์ Notion 15 ข้อ · ตัวกรอง "งานที่ลงแผนเดือน MM/YY" บนบอร์ดรวม
- **migration ล่าสุด = 0073** · ของค้างใน working tree จาก 2 ก.ค. **ยังไม่ตัดสินใจ (ค้างมา 9 รอบแล้ว)**:
  `index.html` (ราก) · `docs/presentation-script.html/.pdf` · `docs/presentation.html`

---

## 📅 บันทึกวันที่ 27 สิงหาคม 2569 (รอบบ่าย) — Part C.4 ครบ 6 ก้อน (0065–0069) 🎉

### ที่มา
ทีมส่ง requirement **Part C.4** (Notion `3c792ef2-c18f-8035-8614-c32aac75d816`) — 4 เรื่อง:
บั๊ก `/trace` · In-process QC 3 บั๊ก · จุดเก็บตัวอย่าง → "ตรวจ Finished product" · Deviation → Incident Case
แผนเต็ม: `~/.claude/plans/part-c-4-snazzy-gray.md`

> **แก่นของ Part C.4 มี 2 เรื่อง**
> 1. **เก็บบั๊กที่ทีมเจอหน้างาน** — `/trace` พังบางเลข · หัวหน้า QC กดอนุมัติไม่ได้เลยตั้งแต่ 0064
> 2. **ยกเครื่อง Deviation เป็น Incident Case** — flow 5 ขั้นที่ QA เป็นศูนย์กลาง
>    (ปิดข้อ §8.1 · §8.2 · §8.4 ของ `docs/feedback-raw-2026-08.md` ที่ประเมินไว้ว่า "ใหญ่")

### ✅ ก้อน 1 — ซ่อม `/trace` + error boundary (commit `28bd092` · ไม่มี migration)
- 🐞 **ต้นเหตุ: `trace/page.tsx:82` เรียก `formatQty()` ที่ export จากไฟล์ `"use client"`**
  → Next โยน `Attempted to call formatQty() from the server` ทันที
  อยู่ใน branch `t.materials.length > 0` เท่านั้น → **งานที่มีรายการเบิกพัง · งานที่ไม่มีปกติ** = "บางเลขได้ บางเลขไม่ได้"
  **ไม่เกี่ยวกับสิทธิ์เลย** (policy อ่านทุกตารางเป็น `using(true)` เหมือนกันหมด)
- ย้าย `formatQty` → `lib/format.ts` · เพิ่ม **`app/(app)/error.tsx` + `app/global-error.tsx`** (เดิมทั้งโปรเจคไม่มีสักไฟล์)
- `genealogy.ts` เลิกทิ้ง `error` ทั้ง 6 query (query พังเคยกลายเป็น "ไม่พบงาน" หลอกผู้ใช้) · ตัด N+1 61 → 6 query

### ✅ ก้อน 2 — In-process QC 3 บั๊ก (`0065` · commit `f30fef2`)
- 🐞 **`0064:244` — ทั้งสองแขนของ `CASE` เป็น unknown literal** → Postgres resolve เป็น `text` ก่อนดูชนิดคอลัมน์
  → `42804 column "status" is of type inprocess_status...` · แก้ด้วย cast ผลของ CASE ทั้งก้อน
  (ตระกูลเดียวกับบั๊ก `0055` — type inference ของ unknown literal)
- `valid_date` แก้ได้แล้ว — เดิมไม่มีครบ 3 ที่ (field ในโมดัล · whitelist `request_edit` · UPDATE ใน `review_edit_request`)
- **ปุ่ม "ขอแก้ไข" ของ in-process เห็นเฉพาะ QC** — แยก prop `canAmendCheck` ออกจาก `canAmend`
  \+ กันที่ DB ด้วย `can_record_inprocess()` ใน `request_edit` ไม่ใช่แค่ซ่อนปุ่ม
- 🎁 อุดช่องโหว่: อนุมัติคำขอแก้ `result`/`value` ของแถวที่ `approved` แล้ว → เด้งกลับ `pending` + ล้างผู้อนุมัติ

### ✅ ก้อน 3 — จุดเก็บตัวอย่าง → "ตรวจ Finished product" (`0066` · commit `8941869`)
- ตัดช่อง `sample_point` ออกจากฟอร์ม → **QA กรอก `collected_at` เองได้** (เดิมเป็น `now()` เสมอ)
- เพิ่ม `result` (reuse enum `check_result`) · `update_qa_sample` / `delete_qa_sample` / `can_record_qa_sample()`
- **ไม่ drop `sample_point`** — ปลด `not null` แทน · UI/eBR โชว์ "จุด/รอบ (ข้อมูลเดิม)" ของแถวเก่า
- **ลบเป็น soft delete + บังคับเหตุผล** — ก้อน 6 ผูกตัวอย่างที่ไม่ผ่านเข้ากับ Incident Case ลบจริงแล้วเคสกำพร้า

### ✅ ก้อน 4 — Deviation → Incident Case: สถานะใหม่ (`0067` · commit `4e09c66`)
- enum ใหม่ `incident_status` 5 ค่า (`qa_review`/`in_progress`/`qa_verify`/`closed`/`cancelled`) แทน `deviation_status` + drop type เก่า
  **แปลง `investigating` เป็น `qa_verify` เฉพาะที่เคยกด "แจ้งแก้ไขเรียบร้อย"** (เหมารวมจะทำให้งานถอยหลัง)
- **ทุกคนที่ล็อกอินเปิดเคสได้** · ตัด "กำหนดปิด" ออกจากตอนเปิด · drop `assigned_to` (null ทุกแถว)
- ตัด root cause ออกจากฟอร์ม + เลิกบังคับตอนปิด · CAPA → **"การแก้ไขเบื้องต้น"** (คงคอลัมน์ `root_cause` ไว้อ่านอย่างเดียว)
- 🐞 **`current_role_group()` ใช้ `has_role()` ซึ่ง admin ผ่านทุกข้อ** → หมายเหตุที่ admin เขียนถูก tag เป็น `'production'` เสมอ
  \+ ไม่รู้จัก engineering/warehouse/planner/lead (5 ฝ่ายคอมเมนต์ไม่ได้เลย) → เพิ่ม **`has_exact_role()`** + คืน `'other'` แทน null
- `open_deviation_internal()` — ตัวเปิดเคสที่ไม่เช็กสิทธิ์ ให้ RPC อื่นเรียกต่อ · เจอซ้ำคืน id เดิม ไม่ raise

### ✅ ก้อน 5 — ขั้น QA ตรวจสอบ + แผนกหลายแผนก (`0068` · commit `15ea96b`)
- `incident_case_type` (dev/oos/nc) + `case_no` (QA พิมพ์เอง) + unique index กันเลขซ้ำ
- **ตาราง `deviation_departments`** — ไม่ใช้ `app_role[]` เพราะต้องรู้ว่าแผนกไหนตอบแล้ว/ยัง
  (`role_group` เป็น `app_role` ตรงๆ → ส่งเข้า `create_notification` ได้เลย)
- `qa_review_deviation()` — คัดแยก + มอบหมาย + แจ้งทุกแผนก · หรือ `cancel` (ต้องมีเหตุผล)
- `submit_deviation_resolution()` เป็นรายแผนก — **ครบทุกแผนกถึงจะเข้า `qa_verify`**
- **วาล์วกันเคสค้างถาวร 2 ทาง**: QA ถอดแผนกที่ยังไม่ตอบออก · หรือปิดข้ามขั้นโดยบังคับกรอกเหตุผล

### ✅ ก้อน 6 — เปิดเคสอัตโนมัติ (`0069` · commit `69ddb62`)
- `review_inprocess_check` reject **หรือ** approve+fail → เปิดเคสเอง · `add/update_qa_sample` ผล fail → เปิดเคสเอง
- กันซ้ำ 2 ชั้น (guard + partial unique index) + **pre-flight หยุดไฟล์ถ้ามีเคสซ้ำค้างอยู่**
- **ลบปุ่ม `FailQuickOpen` ทั้งชุด** — ของเดิมไม่เช็ก `status` จึงโผล่กับผลที่ยัง pending/rejected และกดซ้ำได้
- ⚠️ **เรียกใน RPC ไม่ใช่ trigger** — trigger จะยิงตอน `review_edit_request` แก้ `result` ย้อนหลังด้วย = เปิดซ้ำโดยไม่มีใครสั่ง

### 🔑 Decisions ที่ผู้ใช้ยืนยัน
1. **แผนก = 4 แผนกตามเอกสาร** (WHS/QC/PRD/ENG) · **เลือกได้หลายแผนก**
2. **เลข DEV/OOS/NC — QA พิมพ์เอง ระบบไม่ออกเลขให้**
3. **แทนที่สถานะเดิมทั้งหมด + แปลงข้อมูลเก่า**
4. **ตัดช่อง "จุด/รอบ" ออกจากฟอร์ม** ใช้ `collected_at` แทน
5. 🔑 **ฝ่ายผลิตต้องไม่มีปุ่มขอแก้ไขใน In-process QC — ทำได้เฉพาะ QC**
   (ตอนแรกผมตีความกลับด้าน คิดว่าเขาอยากให้ฝ่ายผลิตแก้ได้ — **ถามก่อนถึงรู้ว่าตรงข้าม**)
6. **ต้องส่งครบทุกแผนก · QA/ผู้บริหารข้ามได้**
7. **คงด่าน "งานต้องอยู่สถานะ QA" ของจุดเก็บตัวอย่าง รวมตอนแก้/ลบ**

### 🧠 บทเรียนรอบนี้
- 🚨 **ไฟล์ `"use client"` export ได้แต่ Component** — ฟังก์ชันธรรมดาที่ฝั่ง server อาจเรียก ต้องอยู่ไฟล์กลาง
  สแกนทั้ง repo แล้ว `formatQty` เป็นตัวเดียวที่ผิดกติกานี้ · **หลักฐานที่ดีที่สุดคือ grep ใน `.next` หา `registerClientReference`**
- **"บางเลขได้ บางเลขไม่ได้" มักไม่ใช่เรื่องสิทธิ์** — ให้ดูว่าโค้ดเส้นไหนวิ่งเฉพาะเมื่อมีข้อมูล
- **`CASE` ที่ทุกแขนเป็น literal = ได้ `text`** ต่อให้คอลัมน์ปลายทางเป็น enum · เจอครั้งที่ 2 แล้ว (0055, 0064)
- **เปลี่ยนชนิด enum ให้ใช้ `create type` ใหม่ + `alter column type ... using`** ไม่ใช่ `alter type add value`
  (ลบค่า enum ทิ้งไม่ได้ · และ `create type` ใหม่ไม่ติดข้อห้าม "แยกรอบ paste")
- **ถามก่อนเดา** — ข้อ (ค) ของ requirement ("ฝ่ายผลิตแก้ไขไม่ได้") ผมกับ Plan agent ตีความว่า "ควรให้แก้ได้"
  แต่ผู้ใช้หมายถึง "ไม่ควรมีปุ่มด้วยซ้ำ" · **ถ้าลงมือเลยจะทำตรงข้ามกับที่ทีมต้องการทั้งก้อน**
- **heredoc พังซ้ำอีกรอบ** (เนื้อหายาว + quote) → เขียนสคริปต์ python ลง scratchpad แล้วค่อยรัน (ครั้งที่ 4 แล้ว)
- **console เป็น cp874 → `print()` ภาษาไทย/สัญลักษณ์พัง** ต้อง `export PYTHONIOENCODING=utf-8`

### ✅ verify แล้ว
- `npm run lint` + `npm run build` ผ่าน 0 error **ทุกก้อน** · push ครบ 6 commit
- บันเดิลใหม่ **ไม่มี stub `Attempted to call formatQty`** เหลืออยู่แล้ว (grep `.next` ยืนยัน)
- **REST verify หลังผู้ใช้ paste SQL ครบ:** `deviation_departments` มีจริง · `qa_samples.result` · `deviations.case_type/case_no/qa_sample_id` มีจริง ·
  `assigned_to` ได้ `42703` · สถานะเก่า `open`/`investigating` ได้ **`22P02` (ค่าหายจริง)** · สถานะใหม่ 5 ค่าใช้ได้ครบ ·
  signature เก่า 3 ตัวได้ `PGRST202` · RPC ใหม่ทุกตัว anon ได้ `42501` · `open_deviation_internal` ไม่ grant ให้ใครเลย
  **ยิง sanity เทียบทุกกลุ่ม** (คอลัมน์มั่ว `42703` · ฟังก์ชันมั่ว `PGRST202` · enum มั่ว `22P02`)
- 📝 Notion **"😍 Part C.4 claude"** `3c992ef2-c18f-80d7-8624-cf9f3d67495c` — รายละเอียด 6 ก้อน + flow diagram + ตารางสิทธิ์
  \+ **เช็กลิสต์กดติ๊กได้ 6 หมวด**

### ▶️ พรุ่งนี้เริ่มตรงนี้
- 🚨 **ต้องมีคนถือ role `qa` จริง** ไม่งั้น Incident Case ค้างที่ "รอ QA ตรวจสอบ" ตลอดไป **และบล็อกการปล่อย FG ทุกงานที่มีเคสเปิด**
  (ต่อจาก Part C.3 ที่ต้องตั้ง `production_lead` + `qc_lead` — **ยังไม่ยืนยันว่าตั้งครบหรือยัง**)
- ⚠️ **ด่าน GMP เข้มขึ้น** — ผลตรวจที่ไม่อนุมัติ / ตัวอย่างที่ไม่ผ่าน เปิดเคสเองและบล็อก `qa → finished_goods` ทันที
- 🔜 **ทีมทดสอบ UI ตามเช็กลิสต์ Notion 6 หมวด** — หมวด ง (เดินครบ flow) ต้องสลับล็อกอินหลายบัญชี ทำรวดเดียวคุ้มสุด
- 🔜 **SQL 2 ข้อที่ยังไม่ได้รัน** (อยู่ท้ายหน้า Notion): เช็กเคสที่เคยแจ้งแก้ต้องอยู่ `qa_verify` · เช็กว่าไม่มีฟังก์ชันซ้อน 2 เวอร์ชัน
- 🔜 ค้างจาก Part C: เช็กลิสต์ Notion 15 ข้อ · ตัวกรอง "งานที่ลงแผนเดือน MM/YY" บนบอร์ดรวม · Export 12 คอลัมน์ + ใบแจ้งผลิต F.PLN.01
- **migration ล่าสุด = 0069** · ของค้างใน working tree จาก 2 ก.ค. **ยังไม่ตัดสินใจ (ค้างมา 8 รอบแล้ว)**:
  `index.html` (ราก) · `docs/presentation-script.html/.pdf` · `docs/presentation.html`

---

## 📅 บันทึกวันที่ 27 สิงหาคม 2569 — Part C.3 ครบ 6 ก้อน (0058–0064) 🎉

### ที่มา
ทีมส่ง requirement **Part C.3** (Notion `3c392ef2-c18f-808a-9aec-fb01374dd38d`) — 3 หัวข้อ:
หน้าผลิตภัณฑ์/ขั้นตอนการผลิต · หน้าเครื่องจักร · หน้ารายละเอียดงาน (LC + บันทึกผลผลิต + In-process QC)
แผนเต็ม: `~/.claude/plans/part-c-3-zesty-volcano.md`

> **แก่นของ Part C.3 มี 2 เรื่อง**
> 1. **เลิกใช้ "กลุ่มหลัก"** — ระบบมีสถานีซ้อนกัน 2 ชั้น (สถานีจริง 9 ตัว กับ enum `production_station` 4 กลุ่ม)
>    ที่แดชบอร์ด/รายงาน/eBR/การกรองเครื่องจักรใช้อยู่เบื้องหลัง → ถอดชั้นกลางออกให้หมด
> 2. **แบ่งหน้างานเป็นแท็บตามขั้นตอน (route)** — เดิมเป็นหน้าเดียวยาว LC ทำครั้งเดียวต่องาน
>    แต่หน้างานจริงเดินหลาย stage แต่ละ stage ต้อง LC / บันทึกผลผลิต / ตรวจ QC แยกกัน

### ✅ ก้อน 1 — เครื่องจักรผูกสถานีจริง (`0058` · commit `36f0d7f`) 【expand】
- `machines.station_id` (FK stations) — ของเดิมมีแต่ `machines.station` ที่เป็น enum 4 กลุ่ม **ไม่มี FK เลย**
- ล้างข้อมูลทดสอบตามลำดับ FK (`inprocess_checks` → `line_clearances` → `production_records` → `machines`)
  แล้ว **seed 18 เครื่อง** (สถานี active 9 ตัว × 2) · `WEIGH-01/02` … `PACK-01/02`
- `upsert_machine` รับ `p_station_id` แทน `p_station` (drop signature เดิมก่อน)
- แอป: dropdown "สถานี" หน้าเครื่องจักร = สถานีจริง · `record-form` กรองเครื่องด้วย `station_id` ตรงๆ

### ✅ ก้อน 2 — ลบ "กลุ่มหลัก" ทั้งระบบ (`0059` · commit `39d803d` + `4c9c02a`) 【contract】
- drop คอลัมน์ **5 จุด**: `production_records.station` · `inprocess_checks.station` · `machines.station` ·
  `job_routes.station_group` · `stations.station_group` → **drop type `production_station`**
- **recreate 8 ฟังก์ชัน** ที่อ้าง enum โดยยกบอดี้ล่าสุดมาทั้งดุ้น (upsert_station · upsert_machine ·
  add_production_record · add_inprocess_check · create_production_jobs · sync_job_route · request_edit · review_edit_request)
- drop `create_job_with_order` — ฟังก์ชันสร้างงานรุ่นเก่าที่แอปเลิกเรียกตั้งแต่ 0048 และ body อ้างคอลัมน์ที่หายไป
- แอป: แดชบอร์ดรวมยอด DL cost ตาม **9 สถานีจริง** · หน้า recipes ตัด dropdown/คอลัมน์ "กลุ่มหลัก" ทิ้ง

### ✅ ก้อน 3 — role หัวหน้า + แท็บตามขั้นตอน + เครื่องจักรของ route (`0060`+`0061` · commit `57bf9ba`)
- `0060` (ไฟล์แยกตามแพทเทิร์น 0038): enum `app_role` += `production_lead`, `qc_lead`
- `0061`: ตาราง **`job_route_machines`** — ผูกกับ **`job_routes.id` ไม่ใช่ `station_id`**
  (route ที่เดินสถานีเดิมซ้ำจะได้ไม่ปนกัน) · ด่าน: เครื่องต้องประจำสถานีเดียวกับขั้นตอน
- แอป: `route-tabs.tsx` แถบ pill ใช้ `?step=<job_routes.id>` (server component ล้วน ไม่มี client state)
  · `route-machines-card.tsx` การ์ดรายละเอียดเครื่อง **อ่านอย่างเดียว**
  · บันทึกผลผลิต + ตรวจ QC กรองตามขั้นตอนที่เลือก · ย้ายการ์ดเบิกวัตถุดิบขึ้นเหนือแท็บ (เป็นข้อมูลระดับงาน)

### ✅ ก้อน 4 — Line Clearance ต่อขั้นตอน × เครื่องจักร (`0062` · commit `eece200`)
- `line_clearances` += `job_route_id` (NOT NULL) · `machine_id` (NOT NULL) · `room` · `headcount` ·
  `cleared_old_time` · `cleaned_time` · unique เปลี่ยนจาก `(job_id)` → **`(job_route_id, machine_id)`**
- ผู้ยืนยันเปลี่ยนจาก ผลิต/QC/QA/ผู้บริหาร → **หัวหน้าฝ่ายผลิต/ผู้บริหารเท่านั้น** · ผ่อน "ครบ 3 ข้อ" → อย่างน้อย 1 ข้อ
- **ย้ายด่าน**: ถอด LC ออกจาก `planned → in_production` ไปกั้นที่ `add_production_record` รายสถานี/เครื่องแทน
- eBR หัวข้อ 2 เปลี่ยนเป็นตารางหลายใบ

### ✅ ก้อน 5 — บันทึกผลผลิตรายวัน (`0063` · commit `51e2822`)
- enum `work_shift` (morning/night) + `work_period` (normal/ot)
- `production_records`: **rename `hours` → `minutes`** · += `job_route_id`, `shift`, `work_period`, 3 หน่วย
- `inprocess_checks` += `production_record_id` (**ย้ายมาจากแผนก้อน 6** เพราะคอลัมน์สถานะ QC ต้องใช้ขาผูกนี้)
- `add_production_record` รับ `p_job_route_id` แทน `p_station_id` (derive สถานีเอง) + ตรวจว่าเครื่องผูกกับขั้นตอน
- แอป: ฟอร์มตัดช่อง "สถานีผลิต" · กะ/OT · นาที (0–1440) · "ยอดที่ต้องการ" · dropdown หน่วยต่อช่อง
  · **คอลัมน์ "สถานะ QC" + ปุ่ม 🔬 QC** ที่ลิงก์ `?step=..&qc=..#inprocess` ล็อกแถวที่จะตรวจให้เลย
- `station-constants.ts` → **`production-constants.ts`** (ชื่อเดิมชวนเข้าใจผิดตั้งแต่ 0059)

### ✅ ก้อน 6 — In-process QC: Valid date + รออนุมัติ (`0064` · commit `f1bad26`)
- enum `inprocess_status` (pending/approved/rejected) + `valid_date` · `approved_by/at` · `approve_note`
- `review_inprocess_check()` — **หัวหน้า QC/ผู้บริหาร** · คงกฎสองลายเซ็น (ผู้อนุมัติ ≠ ผู้ลงผล) · ไม่อนุมัติต้องมีเหตุผล
- `inprocess_route_complete()` เข้มขึ้น: นับเฉพาะ `status='approved' and result='pass'`
- แถวเก่าตั้งเป็น `approved` ให้เลย — ระบบเดิมไม่มีขั้นตอนอนุมัติ ถ้าตั้ง pending จะกลายเป็น
  "เคยผ่านแล้วกลับไปค้าง" ย้อนหลังโดยไม่มีใครสั่ง

### 🔑 Decisions ที่ผู้ใช้ยืนยัน
1. **ลบ "กลุ่มหลัก" ทิ้งทั้งระบบ** ไม่เก็บซ่อนไว้เบื้องหลัง (ยอมให้แดชบอร์ด/รายงานเปลี่ยนหน้าตา)
2. **เพิ่ม role ใหม่ 2 ตัว** — `production_lead` + `qc_lead` (ไม่ใช้ role เดิมแทน)
3. **Line Clearance กั้นระดับสถานี** ไม่ใช่กั้นตอนกด "เริ่มผลิต"
4. **ล้างเครื่องจักร 18 แถวพร้อมข้อมูลทดสอบที่อ้างถึง** แล้ว seed ใหม่

### 🧠 บทเรียนรอบนี้
- **🚨 ค่าคงที่ที่ client ใช้ ห้ามอยู่ในไฟล์ที่ import `supabase/server`** — ผมเผลอวาง `INPROCESS_STATUS_META`
  ไว้ใน `lib/data/quality-checks.ts` → `quality-checks.tsx` (use client) เปลี่ยนจาก `import type` เป็น value import
  → ลาก `next/headers` เข้า client bundle → **build ล้มทั้งระบบ** · แก้โดยแยก `lib/data/inprocess-constants.ts`
- **ผู้ใช้เห็นของเก่าไม่ได้แปลว่าโค้ดผิดเสมอไป** — รอบก้อน 2 ผู้ใช้ส่งรูปว่า "กลุ่มหลักยังอยู่" 2 ครั้ง
  ตรวจจริงพบว่า deploy Ready แล้ว · เป็น **Router Cache ฝั่ง client ของ Next.js** (แท็บเปิดค้างแล้วเดินด้วยการคลิกเมนู)
  → ทางออกที่ชัวร์: เปิด URL ต่อท้าย `?v=2` หรือ Incognito · **ตรวจ build จริงก่อนสรุปว่าใครผิด**
- **`grep -c` ใน built bundle เป็นหลักฐานที่ดีกว่าการเถียง** — เช็ก `.next/static` + `.next/server` ว่ามีสตริงเหลือไหม
  (ระวังนับ `.js.map` ของ build เก่าเข้าไปด้วย ต้อง `grep -v '\.map$'`)
- **heredoc พังซ้ำอีกรอบ** (single quote + `${}` ในเนื้อหา) → เขียนสคริปต์ patch ลง scratchpad แล้วค่อยรัน
- **`rep()` แบบแทนที่ครั้งแรกอันตราย** — `canCheck={canInprocess}` ไปโดนตัวใน `<RecordQcCell>` แทน `<QualityChecks>`
  build จับได้เพราะ type ไม่ตรง · ถ้าเป็น prop ชื่อเดียวกันคนละ component จะพังเงียบ
- **ประกาศ component ระหว่าง render** (`QtyField` ใน `RecordForm`) → React unmount/mount input ทุกตัวอักษร โฟกัสหลุด
  · eslint `react-hooks/static-components` จับได้

### ✅ verify แล้ว
- `npm run lint` + `npm run build` ผ่าน 0 error **ทุกก้อน** · Vercel deploy Ready ทุกตัว
- REST verify ครบทั้ง 7 migration — คอลัมน์ที่ต้องหายได้ `42703` · signature เก่าได้ `PGRST202` ·
  RPC ใหม่ anon ได้ `42501` · ค่า enum มั่วได้ `22P02` · ทุกครั้งยิง sanity เทียบว่าการทดสอบเชื่อถือได้
- เครื่องจักร 18 แถว สถานีละ 2 ครบ 9 สถานี · `to_regtype('production_station')` = null
- 📝 Notion **"🛠️ Part C.3 claude เเก้"** `3c992ef2-c18f-8116-98b1-e3b09898aca1` — ก้อน 1–6 + เช็กลิสต์ทดสอบทุกก้อน

### ▶️ พรุ่งนี้เริ่มตรงนี้
- 🚨 **ต้องตั้ง role `หัวหน้าฝ่ายผลิต` + `หัวหน้า QC` ให้คนจริงที่หน้า Admin ก่อนใช้งาน**
  ไม่งั้น **ยืนยัน LC ไม่ได้ → บันทึกผลผลิตไม่ได้** และ **อนุมัติผลตรวจไม่ได้ → ส่ง QC ไม่ได้**
  (ผู้บริหาร/admin ทำแทนได้ทั้ง 2 หน้าที่ระหว่างนี้)
- ⚠️ **ด่าน LC ไม่กั้นถ้าขั้นตอนนั้นยังไม่ได้ผูกเครื่องจักรเลย** — เพราะ LC ผูกกับเครื่อง ถ้าไม่มีเครื่องก็ทำ LC ไม่ได้
  จะกลายเป็นล็อกตาย · ถ้าทีมอยากให้กั้นเด็ดขาด ต้องเพิ่มกฎ "ต้องผูกเครื่องก่อนเริ่มผลิต"
- 🔜 **ทีมทดสอบ UI ทั้ง 6 ก้อนบนเว็บจริง** — เช็กลิสต์กดติ๊กได้ในหน้า Notion Part C.3
- 🔜 **Part C.4 รออยู่แล้วใน Notion** (`3c792ef2-c18f-8035-8614-c32aac75d816`):
  บั๊ก `/trace` ค้นบางเลขขึ้น server error · จุดเก็บตัวอย่าง (เปลี่ยนชื่อ + ลบ/แก้ได้ + ผลตรวจ) ·
  Deviation (เลข DEV/OOS/NC · แผนกรับผิดชอบ · flow ใหม่ เปิด → QA ตรวจ → ส่งแผนก → กลับ QA → อนุมัติ)
- 🔜 ค้างจาก Part C: เช็กลิสต์ Notion 15 ข้อ · ตัวกรอง "งานที่ลงแผนเดือน MM/YY" บนบอร์ดรวม · Export 12 คอลัมน์ + ใบแจ้งผลิต F.PLN.01
- **migration ล่าสุด = 0064** · ของค้างใน working tree จาก 2 ก.ค. **ยังไม่ตัดสินใจ (ค้างมา 7 รอบแล้ว)**:
  `index.html` (ราก) · `docs/presentation-script.html/.pdf` · `docs/presentation.html`

---

## 📅 บันทึกวันที่ 21 สิงหาคม 2569 — Part C.2 เบิกวัตถุดิบ/บรรจุภัณฑ์ ครบ 4 ก้อน (0056–0057) 🎉

### ที่มา
ทีมส่ง requirement **Part C.2** (Notion `3c292ef2-c18f-802e-ba39-cf2094982835`) — ส่วน "เบิกผลิตภัณฑ์" ในหน้ารายละเอียดงาน
ผู้ใช้สั่งชัดว่า **หัวข้อ Line Clearance ในหน้าเดียวกันยังไม่ต้องทำ** ไว้รอบหน้า
แผนเต็ม: `~/.claude/plans/moonlit-spinning-eagle.md`

> **แก่นของ Part C.2:** ระบบเบิกเดิมผูกกับ "ล็อตในคลัง" — เลือกล็อตจริง → คลังกด "จ่าย" → ตัดสต็อก `qty_on_hand`
> แต่โรงงานไม่ได้ทำงานแบบนั้น และไม่ได้ใช้ความพร้อมวัตถุดิบเป็นด่านก่อนเข้าผลิต
> → เปลี่ยนเป็น **note list**: ฝ่ายผลิตจดว่าต้องใช้อะไร · ฝ่ายคลังติ๊ก พร้อม/ไม่พร้อม

### ✅ ก้อน 1 — ตาราง + RPC ของใหม่ (`0056` · commit `d14c563`)
- ตาราง **`job_materials`** — `item_name` (พิมพ์เอง ไม่ผูกทะเบียนยา) · `item_type` RM/PM · `qty` + `qty_unit` (เว้นว่างได้ทั้งคู่) ·
  `note` · `status` not_ready/ready · **`status_changed_by/at`** (ALCOA — การ์ดต้องโชว์ "พร้อม โดยใคร เมื่อไหร่" ทุกใบ ถ้าไปดึงจาก audit_log = N+1 + ติด RLS)
- **สิทธิ์กั้นด้วยการ "แยก RPC" ไม่ใช่ซ่อนปุ่ม** — `upsert_job_material` **ไม่มีพารามิเตอร์ status ให้ส่ง** ฝ่ายผลิตจึงเปลี่ยนสถานะไม่ได้แม้ยิง REST ตรง ·
  `set_job_material_status` แตะเฉพาะคอลัมน์สถานะ · `delete_job_material` คลังเรียกไม่ได้
- **auto-reset:** แก้ ชื่อ/ประเภท/จำนวน/หน่วย หลังคลังกด "พร้อม" → เด้งกลับ `not_ready` เอง · **แก้แค่หมายเหตุไม่เด้ง**
- `revoke` จาก public/anon ครบก่อน `grant authenticated` → **anon เรียก RPC ทั้ง 3 ตัวไม่ได้เลย (42501)** ต่างจากของเดิมที่ anon เรียกได้

### ✅ ก้อน 2 — หน้ารายละเอียดงาน (commit `0ef9f6a`)
- **`components/job-material-card.tsx`** = การ์ด 1 ใบ + `ReadyStatusPicker` (ลอก `MachineStatusPicker` 0052) — วางใน `components/` เพราะใช้ร่วมกับหน้ารวมก้อน 3
- **`lib/actions/job-materials.ts`** (โฟลเดอร์ใหม่) — 2 route ใช้ร่วมกัน ถ้าวางใน route folder จะกลายเป็น `components/` พึ่งพา route
- การ์ดอย่างเดียว ไม่มีตารางจอกว้างแล้ว · ฝ่ายผลิตแก้/ลบ inline · **ฝ่ายผลิตเห็นป้ายสถานะแต่กดไม่ได้** (`canSetStatus ? Picker : Badge` เป๊ะ)
- `role-access.ts` เพิ่ม `canEditJobMaterials` / `canSetJobMaterialStatus` + ตาราง mirror — **เลิกเช็ก `hasAnyRole` inline ใน page.tsx** แบบระบบเบิกเก่า
- **ลบ 4 ไฟล์:** `requisitions.tsx` (390 บรรทัด) · `requisition-actions.ts` · `lib/data/requisitions.ts` · `requisition-constants.ts`
- eBR ส่วน 3 → "วัตถุดิบ/บรรจุภัณฑ์ที่ต้องใช้" + กำกับว่าไม่ผูกเลขล็อต · `/trace` แผงซ้ายเป็นรายการใหม่

### ✅ ก้อน 3 — หน้ารวมฝ่ายคลัง `/job-materials` (commit `e5b6be9`) 🆕 นอกโจทย์เดิม
- ผู้ใช้ขอเพิ่มระหว่างวางแผน — เดิมฝ่ายคลังต้องเปิดงานทีละใบ ไม่มีที่ไหนเห็นของค้างข้ามงาน
- เมนูใหม่ roles = คลัง/ผลิต/ผู้บริหาร · จัดกลุ่มตามเลขงาน · การ์ดสรุป 3 ใบ · ตัวกรอง `<form method="get">` ล้วน
- **query เดียวแล้วจัดกลุ่มด้วย `Map` ฝั่ง TS** — `jobs!inner` จำเป็นสำหรับกรอง `jobs.status` บน embedded resource
- **ไม่มีปุ่มเพิ่ม/แก้/ลบบนหน้านี้** (`actions` ส่ง undefined) — ตั้งใจไม่ทำ 2 ทางเข้า

### ✅ ก้อน 4 — ลบระบบเบิกเก่า (`0057`)
- **pre-flight 2 ชั้น** — มีใบ `issued` หรือคำขอแก้ไขค้าง = `raise` หยุดทั้งไฟล์ (ผู้ใช้รันเช็กแล้ว **0 แถว** ผ่านฉลุย)
- recreate 3 ฟังก์ชันที่อ้างตารางเก่า โดย**ยกบอดี้ล่าสุดมาทั้งดุ้น**: `request_edit` + `review_edit_request` (0037) · `product_delete_report` (0050)
- drop RPC เบิก 3 ตัว (ระบุ signature) → ถอด realtime → drop ตาราง → drop enum `requisition_status`

### 🔑 Decisions ที่ผู้ใช้ยืนยัน
1. **ตัวย่อในเอกสารสลับกัน** — requirement เขียน "วัตถุดิบ (PM), บรรจุภัณฑ์ (RM)" → แก้เป็นสากล **RM = วัตถุดิบ · PM = บรรจุภัณฑ์**
2. **ลบระบบเบิกเดิมทิ้งทั้งระบบ** (ไม่เก็บไว้คู่กัน) — รับผลว่า `/trace` เสียขา recall
3. สถานะ **ไม่กั้น** การเดินสถานะงาน แต่ขึ้นแถบเตือน
4. manager ทำได้ทุกอย่าง
5. จำนวน = **ตัวเลข + ช่องหน่วยแยก** · ทั้งคู่เว้นว่างได้ (เผื่อ "แป้ง — ตามสูตร")
6. eBR **แสดงรายการใหม่แทน** ไม่ตัดหัวข้อทิ้ง
7. **ทำหน้ารวมฝ่ายคลังในรอบนี้ด้วย** (ขยายสโคปเอง)
8. แก้ข้อมูลหลังคลังกด "พร้อม" → **รีเซ็ตอัตโนมัติ**

### 🧠 บทเรียนรอบนี้
- **🚨 กับดัก `else` ใน `review_edit_request`** — ของเดิมเป็น `if production / elsif material_requisition / else inprocess_check`
  ถ้าตัด branch ใบเบิกทิ้งเฉยๆ คำขอชนิดเก่าที่ค้างจะ**ตกลง else แล้วสั่ง update inprocess_checks ด้วย id ของใบเบิก = 0 แถว แต่ปิดสถานะเป็น applied** = "อนุมัติสำเร็จ" ทั้งที่ไม่ได้แก้อะไร
  → ต้องเปลี่ยนเป็น `elsif inprocess_check` + `else raise` · **Plan agent จับได้ ผมมองข้าม**
- **อย่าเชื่อ path/ชื่อฟังก์ชันจากความจำ** — ผมเขียนในแผนว่า `create_edit_request`/`apply_edit_request`/`delete_product`
  ของจริงคือ **`request_edit`/`review_edit_request`/`product_delete_report`** · ตรวจกับไฟล์จริงก่อนถึงเจอ
- **expand → migrate → contract** — สร้างของใหม่ (0056) → deploy โค้ด → ค่อยลบของเก่า (0057)
  ถ้ารวมเป็นไฟล์เดียวจะมีช่วงที่เว็บรันโค้ดเก่าแต่ตารางหายแล้ว = หน้างานพังทั้งหน้า
- **REST verify ต้องพิสูจน์ว่าการทดสอบเชื่อถือได้ด้วย** — `[]` แปลว่าอะไรก็ได้ ต้องยิงคอลัมน์มั่ว (`42703`) / ความสัมพันธ์มั่ว (`PGRST200`) เทียบเสมอ
- **JSON ภาษาไทยผ่าน curl ใน Git Bash เพี้ยน** (ตอบ `PGRST102`) — ทดสอบ RPC ให้ใช้ payload ASCII
- **heredoc พังซ้ำอีกรอบ** — คราวนี้เพราะ single quote ในเนื้อหา ไม่ใช่แค่ความยาว/ภาษาไทย → เขียนไฟล์ยาวใช้ Write/Edit tool เท่านั้น

### ✅ verify แล้ว
- `npm run lint` + `npm run build` ผ่าน 0 error ทั้งก้อน 2 และ 3 · Vercel deploy **Ready ทั้ง 2 ตัว**
- REST: `job_materials` 200 (คอลัมน์มั่ว `42703`) · RPC ใหม่ 3 ตัว anon ตอบ `42501` · helper ตอบ false · query หน้ารวมรูปถูก
- ผู้ใช้รันเอง: realtime publication = 1 · trigger = 2 ตัว · `material_requisitions` = **0 แถว**
- ผู้ใช้ทดสอบ UI จริงผ่าน 3 ข้อหลัก: ฝ่ายผลิตกดสถานะไม่ได้ · ฝ่ายคลังคลิกป้ายเปลี่ยนได้ไม่มีปุ่มลบ · auto-reset ทำงาน (แก้หมายเหตุไม่เด้ง)
- **ก้อน 4 ผู้ใช้รัน SQL ตรวจเองแล้ว ผ่านครบ:** `to_regclass('public.material_requisitions')` = **null** ·
  `request_material`/`issue_requisition`/`cancel_requisition` = **0** ·
  3 ฟังก์ชันที่ recreate ได้ **ตัวละ 1 แถว ไม่มี overload ซ้อน** —
  `product_delete_report(uuid)` · `request_edit(edit_target_type,uuid,jsonb,text)` · `review_edit_request(uuid,text,text)`
- 📝 Notion **"🖐🏼 Part C.2 claude เเก้"** `3c392ef2-c18f-80ea-85f0-d10e0d97e634` — ก้อน 1–4 + ตารางสิทธิ์ + เช็กลิสต์ 6 หมวด

### ▶️ พรุ่งนี้เริ่มตรงนี้
- ⚠️ **แจ้งทีมด้วย: ไม่มีระบบใดตัดสต็อก `material_lots.qty_on_hand` อัตโนมัติแล้ว** — ตัวเลขหน้าผลิตภัณฑ์คลังจะนิ่งจนกว่าฝ่ายคลังไปแก้เอง
- ⚠️ **`/trace` ค้นจากเลขล็อตวัตถุดิบ (recall) ไม่ได้อีกแล้ว** — ถ้าทีมต้องใช้จริง ทางแก้คือเพิ่มคอลัมน์ `lot_no text` (พิมพ์เอง) ใน `job_materials`
- 🔜 **ก้อนถัดไปที่ทีมรออยู่ = Line Clearance** (อยู่ในหน้า Notion Part C.2 เดียวกัน):
  เลือก Stage ที่ทำอยู่แล้วเพิ่ม LC ได้เรื่อยๆ · แยกหน้าที่ "หัวหน้าฝ่ายยืนยัน" กับ "หัวหน้าห้องตรวจ" · ย้ายไปทำตอน "กำลังผลิต" ·
  ไม่ต้องติ๊กครบ 3 ข้อ · เพิ่มช่อง "ห้อง" + (อาจ) "จำนวนคน" · เพิ่มช่องเวลาใน check line เคลียร์ของเก่า/ทำความสะอาด
- 🔜 ค้างจาก Part C: เช็กลิสต์ Notion 15 ข้อ (หมวดสิทธิ์ + ด่านล็อก) · ตัวกรอง "งานที่ลงแผนเดือน MM/YY" บนบอร์ดรวม · Export 12 คอลัมน์ + ใบแจ้งผลิต F.PLN.01
- **migration ล่าสุด = 0057** · ของค้างใน working tree จาก 2 ก.ค. **ยังไม่ตัดสินใจ (ค้างมา 6 รอบแล้ว)**:
  `index.html` (ราก) · `docs/presentation-script.html/.pdf` · `docs/presentation.html`

---

## 📅 บันทึกวันที่ 20 สิงหาคม 2569 — Part C หน้ารายละเอียดงาน (0053–0054) 🎉

### ที่มา
ทีมส่ง requirement **Part C** = หน้าที่กดเข้าไปในงาน 1 ใบจากบอร์ด — ขอ 3 เรื่อง:
เปลี่ยนชื่อช่องให้ตรงใบงานจริง · ให้ **แก้ข้อมูลงานได้ตามฝ่าย** · ทำ **Status เป็น dropdown + ค้นหา + จัดการทะเบียนเอง**
(ปิดข้อ §3 ของ `docs/feedback-raw-2026-08.md` — "สถานะย่อย ~20 ขั้น + UNPLAN + แผน MM/YY" ที่ค้างนานที่สุด)
แผนเต็ม: `~/.claude/plans/part-twinkly-knuth.md`

> **แก่นของ Part C:** เดิมหน้ารายละเอียดงาน "ดูได้อย่างเดียว" กรอกผิดตั้งแต่สร้างงาน = แก้ไม่ได้เลย
> ต้องลบงานทิ้งสร้างใหม่ ซึ่งกินเลขงาน 6 หลักที่ reset รายปี

### ✅ ก้อน 1 — การ์ดข้อมูลงาน (commit `f242085` · ไม่มี migration)
- `"จำนวน"` → **Batch Size** · `"ขนาดบรรจุ"` → **Packing Size** แยกบรรทัด (1)(2)(3) · `"ออเดอร์"` → **Job No.**
  (แสดง `job_no` ตรงๆ ไม่ต้องตัด `ORD-` เพราะ DB การันตี `order_no = 'ORD-' + job_no` เสมอ)
- เพิ่ม **Reg No.** (`products.reg_no`) + **กำหนดส่ง** (`orders.due_date` — เดิมกรอกตอนสร้างงานแล้วไม่โผล่ที่ไหนอีกเลย)
- แก้ helper `Field`/`Row` ให้ `""` กลายเป็น `—` ด้วย (ของเดิม `?? "—"` ดักแค่ null)
- **แก้ `jobs.ts` SELECT จุดเดียว มีผลทั้งบอร์ด · หน้ารายละเอียด · eBR** (eBR เรียก `getJobByNo` ต่อ)

### ✅ ก้อน 2 — ทะเบียนสถานะ + เดือนแผน (`0053` · commit `576f523`)
- ตาราง **`job_sub_statuses`** (name/description/sort_order/**requires_plan_month**/**is_system**/is_active)
  + seed **51 ค่า** ตามลิสต์ที่ทีมส่ง (ใส่ครบทุกบรรทัดรวมตัวที่ดูซ้ำ ตามที่ผู้ใช้สั่ง)
- **`jobs.plan_month` (date, วันที่ 1 ของเดือน)** — แยกคอลัมน์จาก `sub_status` เพื่อให้กรอง/เรียง/ทำรายงานตามเดือนได้จริง
  แต่ "แสดงผล" รวมเป็น **`มีแผน08/26`** ด้วย helper `formatSubStatus()`
- `upsert_job_sub_status` — **แก้ชื่อแล้ว sync `jobs.sub_status` ให้อัตโนมัติ** (แพทเทิร์น `upsert_customer` 0047)
- `delete_job_sub_status` — ไม่มีงานใช้=ลบจริง · มีงานใช้=ตอบ `blocked` ให้ยืนยัน แล้ว**ปิดใช้งานแทน** (งานเก่ายังโชว์ชื่อเดิม = ALCOA)
- `board/status-picker.tsx` — **input ค้นหา + `<select>` ที่กรองแล้ว** (ไม่เขียน popover เอง · ห้าม `size={n}` ตามบทเรียน Part 2.1)

### ✅ ก้อน 3 — แก้ข้อมูลงานตามฝ่าย (`0054` · commit `26537cf` + `31af720`)
- **`update_job_details(p_job_id, p_fields jsonb, p_reason)`** — RPC เดียว 1 ทรานแซกชัน เขียน 3 ตาราง (jobs · orders · batches)
  🚨 ใช้ **`p_fields ? 'key'`** (มีคีย์ไหม) ไม่ใช่ `->>'key' is not null` — ไม่งั้นช่องที่ไม่ได้ส่งโดน null ทับหมด
- **`job_field_rules()`** = ตารางกติกาสิทธิ์+ด่านล็อกรายช่อง เป็นแหล่งความจริงเดียว (ฝั่งแอปมีสำเนาที่ `lib/data/job-field-rules.ts`)
- **LOT No./วันผลิต/วันหมดอายุ: เรียก `set_job_lot()` ของเดิม ไม่ลอกด่านมาเขียนใหม่** → ด่าน GMP 0049 ทั้งชุดทำงานเป๊ะ
- ผู้บริหารข้ามด่านล็อกได้ **แต่บังคับกรอกเหตุผล** (ลง `audit_log.reason`)
- **`orders` เข้า realtime publication** (0009 มีแค่ 3 ตาราง) — ถ้าไม่เพิ่ม จอคนอื่นค้างแบบเงียบ ไม่มี error
- UI: `job-info-card.tsx` การ์ด 2 โหมด ปุ่ม **"✏️ แก้ไขข้อมูล"** เดียว บันทึกทีเดียว · ช่องที่แก้ไม่ได้ = ข้อความ + 🔒 **พร้อมบอกเหตุผล**
- **ลบ `job-lot-form.tsx` + `lot-actions.ts`** → เหลือ `lot-notice.tsx` (แถบเตือนอย่างเดียว) กันไม่ให้ 2 ที่เขียน `batches.lot_no` ทับกัน

### 🔑 Decisions ที่ผู้ใช้ยืนยัน
1. **แยก 2 คอลัมน์** `sub_status` + `plan_month` (ไม่เก็บ `"มีแผน08/26"` เป็นสตริงเดียว)
2. seed **ครบทุกบรรทัด 51 ค่า** ตามที่ส่งมา (รวมคู่ที่ซ้ำ) แล้วทีมไปยุบเองทีหลัง
3. **manager แก้ได้ทุกช่อง** · จัดการทะเบียนสถานะ = **ทุกฝ่ายที่ล็อกอิน**
4. **คงด่าน LOT No. เดิม (0049) ทุกประการ** — ไม่มี bypass แม้แต่ผู้บริหาร
5. planner แก้วันผลิต/หมดอายุได้ **เฉพาะเมื่อมีล็อตแล้ว** (ไม่ย้ายคอลัมน์ออกจาก `batches`)
6. **Status + กำหนดส่ง = ไม่ล็อกเลย** · Batch Size/บรรจุ ล็อกตอน `in_production` · ที่เหลือล็อกตอน FG
7. **ผู้บริหารข้ามด่านล็อกได้ ต้องกรอกเหตุผล** (เพราะ `edit_requests` ยังไม่รองรับข้อมูลงาน = ล็อกแล้วแก้ไม่ได้ตลอดไป)
8. **ลูกค้า + C.P.O DATE แก้ได้ทั้งคู่** (planner/manager)

### 🧠 บทเรียนรอบนี้
- **`revoke ... from public` อย่างเดียวไม่พอบน Supabase** — ตรวจ REST หลัง paste แล้วพบว่า `job_field_rules()` **anon เรียกได้จริง**
  เพราะ Supabase ตั้ง default privileges แจก execute ให้ `anon`/`authenticated` ทุกฟังก์ชันใหม่ → ต้อง revoke ครบ 3 role (บทเรียนซ้ำจาก 0050)
  **ถ้าไม่ยิงทดสอบจริงจะไม่มีทางรู้** — เขียน revoke ไว้แล้วนึกว่าปิดแล้ว
- **ลำดับก้อนสำคัญกว่าที่คิด** — ตอนแรกใส่ `plan_month` ลง SELECT ตั้งแต่ก้อน 1 (ที่ไม่มี migration)
  ถ้า deploy ไปก่อน paste = **บอร์ดพังทั้งหน้า** เพราะ PostgREST ตอบ 42703 ทั้ง query → ต้องเลื่อนไปก้อนที่มี migration คู่กันเสมอ
- **heredoc ของ Git Bash รับ SQL/ข้อความยาว+ภาษาไทยไม่ไหว** (unexpected EOF) → เขียนไฟล์ใหญ่ด้วย Write tool หรือสคริปต์ python แทน
- **ปุ่มเดียวแก้หลายช่อง = ต้อง atomic** — ถ้าแยกเป็น RPC ละช่องแบบ 0051/0052 ช่องที่ 5 พังแต่ 1–4 ลงไปแล้ว = ข้อมูลครึ่งๆ
  แต่ยังต้องกัน snapshot เก่าทับ → คำตอบคือ **RPC เดียว + ส่งเฉพาะคีย์ที่ dirty**
- **ฟีเจอร์ใหม่อาจย้อนงานเก่า** — Plan agent เสนอให้เอา StatusPicker ไปใส่หน้าสร้างงานด้วย ซึ่งจะ**ย้อน P3.1-1 ที่เพิ่งตัดช่อง Status ออกไป**
  \+ ต้อง drop/สร้าง `create_production_jobs` (13 args) ใหม่โดยไม่จำเป็น → ตัดทิ้ง

### ✅ verify แล้ว
- `npm run lint` + `npm run build` ผ่าน 0 error ทุกก้อน
- REST: `job_sub_statuses` 200 · `jobs.plan_month` มีจริง (คอลัมน์มั่วตอบ `42703` = การทดสอบเชื่อถือได้) ·
  RPC ใหม่ 3 ตัวตอบ guard ไทยถูก · **`set_job_lot` + `create_production_jobs` ชุดพารามิเตอร์เดิมยังตอบเหมือนเดิม = ไม่มี overload ซ้อน** ·
  `job_field_rules()` หลังปิดสิทธิ์ตอบ `42501 permission denied` แต่ `update_job_details` ยังทำงานปกติ (security definer)
- push แล้ว 4 commit · Vercel auto-deploy
- 📝 Notion **"🍉 part C claude เเก้"** `3c292ef2-c18f-8069-b885-ee6adf641095` — เขียนครบ 3 ก้อน + ตารางสิทธิ์รายช่อง
  + **เช็กลิสต์กดติ๊กได้ 26 ข้อ 6 หมวด** (ก ตรวจ SQL · ข หน้าจอ · ค สิทธิ์ · ง Status · จ ด่านล็อก · ฉ หลายเครื่อง)

### ▶️ พรุ่งนี้เริ่มตรงนี้
- ⚠️ **เช็กลิสต์ Notion เหลือ 15 ข้อยังไม่ติ๊ก** — กองอยู่ที่ **หมวด ค (สิทธิ์แต่ละฝ่าย)** กับ **หมวด จ (ด่านล็อก + ผู้บริหารข้ามด่าน + Audit)**
  สองหมวดนี้ต้องสลับล็อกอินหลายบัญชี ทดสอบยากสุด · หมวด ก/ข ผ่านแล้ว (ทะเบียน 51 ค่าลงจริง)
- 🔜 **งานที่ตั้งใจยกไปรอบหน้า** (เขียนไว้ท้ายหน้า Notion แล้ว):
  แสดง Status + **ตัวกรอง "งานที่ลงแผนเดือน MM/YY" บนบอร์ดรวม** (ตอนนี้เห็นแค่หน้ารายละเอียด/eBR — ของที่ทีมอยากได้จริงคือดูทั้งเดือนทีเดียว) ·
  Export 12 คอลัมน์ + ใบแจ้งผลิต F.PLN.01 (ค้างจาก Part 3) ·
  เพิ่ม target `job` เข้าระบบ `edit_requests` เพื่อเลิกใช้ manager-bypass
- 📋 **มี requirement Part C.2 รออยู่ใน Notion แล้ว** — `3c292ef2-c18f-802e-ba39-cf2094982835` (เรื่อง "เบิกผลิตภัณฑ์ แก้ไขการทำงาน")
- **migration ล่าสุด = 0054** · ของค้างใน working tree จาก 2 ก.ค. **ยังไม่ตัดสินใจ (ค้างมา 5 รอบแล้ว)**:
  `index.html` (ราก) · `docs/presentation-script.html/.pdf` · `docs/presentation.html`

> ℹ️ **หมายเหตุ: บันทึกวันที่ 19 ส.ค. ขาดหายไป** — มี 5 commit ที่ไม่เคยผ่าน handoff (`0049`–`0052`):
> P3-3 ฝ่ายผลิตกรอก LOT No. + ด่านห้ามเริ่มผลิตถ้าไม่มีเลขล็อต · P3-4 ลบผลิตภัณฑ์ถาวร ·
> P3-5 ฝ่ายวางแผนปลดสถานะล็อตเอง · P3.1-1 หน้าสร้างงานตัดช่อง Status + ล็อกหน่วยตามทะเบียนยา ·
> P3.1-2 หน้าเครื่องจักรกดเปลี่ยนสถานะบนการ์ด · รายละเอียดดูที่ `git log` และ Notion "Part B.2 claude เเก้"

---

## 📅 บันทึกวันที่ 18 สิงหาคม 2569 — Part 3 ก้อน 1–2 (0047–0048) · ล้างข้อมูลทดสอบเริ่มเลขงานใหม่

### ที่มา
ทีมประชุมแล้วส่ง feedback รอบใหม่ (**ยังไม่ได้เปิดใช้ระบบจริง** — งานใน DB เป็นงานทดสอบล้วน)
แบ่งเป็น 2 ชุด: (1) หน้าสร้างงานผลิตต้องตรงกับใบงานจริงของโรงงาน (2) QoL ที่ค้างจาก Part 2.1
→ วางแผนเป็น **Part 3 · 5 ก้อน** · แผนเต็ม: `~/.claude/plans/twinkling-whistling-cray.md`

### ✅ ก้อน 1 — ทะเบียนลูกค้า (`0047` · commit `2bce393`)
- ตาราง **`customers`** (เก็บแค่ชื่อ) + trigger meta/audit + RLS + realtime (ตารางใหม่ต้องผูกเองทั้งหมด — 0002 ไม่รู้จัก)
- `orders.customer_id` FK (NO ACTION) · **คง `orders.customer` (text) เป็น snapshot ชื่อ ณ วันสั่ง**
  → เหตุผลหลัก: **8 ไฟล์ที่อ่าน `orders.customer` ไม่ต้องแก้เลยสักไฟล์** + ตรงหลัก ALCOA
- **backfill** ชื่อที่พิมพ์ไว้เดิม → ทะเบียน แล้วผูก `customer_id` ครบ (idempotent)
- `upsert_customer` — **แก้ชื่อแล้ว sync `orders.customer` ให้อัตโนมัติ** (ไม่งั้นบอร์ด/eBR โชว์ชื่อเดิมค้าง)
- `delete_customer(p_id, p_reassign_to)` — ไม่มีงานผูก=ลบจริง · มีงานผูกคืน `{action:'blocked'}` พร้อมจำนวน → UI เด้งให้เลือกลูกค้าปลายทาง → ย้ายทุกใบแล้วลบใน**ทรานแซกชันเดียว**
- UI: `customer-picker.tsx` (dropdown + modal จัดการ/ลบ/ย้ายงาน) แทน input พิมพ์อิสระ

### ✅ ก้อน 2 — JOB NO. 6 หลัก + สร้างงานหลายใบ (`0048` · commit `602c522`)
- `jobs` + **`request_no`** (ใบคำขอ) · **`cpo_date`** (C.P.O DATE) · **`sub_status`** (Status ข้อความอิสระ)
  → `sub_status` มี `comment on column` กำกับว่า **ห้ามใช้ตัดสินด่าน GMP** (flow จริงคุมด้วย enum `jobs.status` เท่านั้น)
- **`job_no_counters` + `next_job_nos()`** — ออกเลข `690001` (ปี พ.ศ. 2 หลัก + running 4 หลัก reset รายปี)
- **drop `create_job_with_order` (13 args)** → **`create_production_jobs()`** สร้างได้ 1–50 ใบ · ยกด่าน GMP มาครบ (`can_plan_jobs` · `product_has_route` · copy route กรอง `s.is_active`)
- ฟอร์มสร้างงานยกเครื่อง: JOB NO. อ่านอย่างเดียว · จำนวน→**Batch size** · +ใบคำขอ/C.P.O DATE/Status/จำนวนใบ · **ตัดช่องเลขล็อต + แผนเริ่ม-เสร็จ** · ตัด **"กระปุก"** · สร้างหลายใบขึ้นหน้าสรุปเลขงาน
- หน้ารายละเอียดงาน + eBR แสดง 3 ช่องใหม่

### 🧹 ล้างข้อมูลทดสอบแล้ว (ผู้ใช้รันเอง · สคริปต์ `web/supabase/scripts/`)
เลขงานรูปแบบเก่า (`JOB-2569-0001`, `Roblox001`, `2569`) ปนกับเลข 6 หลักไม่ได้ → ต้องล้างก่อน
- **ตรวจก่อนลบเจอ: ใบเบิกที่ "จ่ายแล้ว" 30 รายการ** → สต็อกถูกหักไปแล้วและการลบงานไม่คืนให้ (พฤติกรรมเดิม 0035)
  → **ผู้ใช้ตัดสินใจล้างคลังทิ้งด้วย เริ่มคีย์ล็อตใหม่หมด** (สะอาดกว่าไล่ปรับมือ 30 รายการ)
- ผลลัพธ์: jobs · batches · orders · **material_lots · fg_inventory = 0 ทั้งหมด**
- **ไม่ลบ** (ยังอยู่ครบ): ทะเบียนยา · ขั้นตอนการผลิต · สถานี · ทะเบียนลูกค้า · ผู้ใช้/สิทธิ์ · เครื่องจักร

### 🔑 Decisions ที่ผู้ใช้ยืนยัน
1. **ลบงานทดสอบเก่าทิ้งทั้งหมด** แล้วเริ่มนับเลข 6 หลักใหม่ (ไม่แปลงเลขเก่า)
2. **Batch NO. = LOT No. ตัวเดียวกัน** — ผู้ใช้แจ้งว่าเข้าใจผิดเอง · **ฝ่ายผลิตกรอกก่อนเริ่มผลิต ฝ่ายวางแผนกรอกตอนสร้างงานไม่ได้**
3. ช่อง Status = **คอลัมน์ใหม่แยกจาก enum เดิม** (ไม่แตะ 6 ขั้น GMP)
4. Batch NO. ตอนสร้างชุด = **ปล่อยว่าง** ไปกรอกทีหลังรายใบ
5. "ลบถาวร" ผลิตภัณฑ์ที่มี jobs = **ห้ามลบ บอกเหตุผลแทน**
6. Export 12 คอลัมน์ = **แยกรอบ** · Packing Size 3 ช่องเดิมพอแล้ว
7. สิทธิ์ LOT No. = ฝ่ายผลิต + ผู้บริหาร · **ล็อกเมื่อเข้าสถานะ `in_production`**

### 🧠 บทเรียนรอบนี้
- **`nextval` กินเลขทิ้งตอน rollback** → เลขงานขาดช่วง ไม่ผ่าน GMP · เปลี่ยนมาใช้ **ตาราง counter + `insert on conflict do update returning`** = atomic คำสั่งเดียว, จองทีเดียวหลายใบได้, rollback แล้วเลขไม่หาย
- **timezone กัดตอนออกเลขปี** — session Supabase เป็น UTC ถ้าใช้ `current_date` ดิบ ช่วง 00:00–07:00 ของ 1 ม.ค. จะออกเลขปีเก่า → ต้อง `now() at time zone 'Asia/Bangkok'`
- **ไฟล์ `"use server"` ของ Next เวอร์ชันนี้ export ค่าคงที่ไม่ได้** (ได้แต่ async function) — build ฟ้อง "The module has no exports at all" ทั้งที่ไฟล์มี export ชัดๆ → ย้าย `MAX_JOBS_PER_CREATE` ไป `job-constants.ts`
- **แก้ Notion ผ่าน MCP ต้อง fetch หน้ามาดูก่อน** — ตาราง markdown ถูกเก็บเป็น `<table>` tags และ bullet ย่อยใช้ `	` → `old_str` ที่พิมพ์เป็น pipe table หาไม่เจอ
- **เก็บ snapshot คู่กับ FK ช่วยได้มาก** — เลือกคง `orders.customer` (text) ไว้ ทำให้ก้อน 1 ไม่ต้องแตะหน้าจอสักหน้า ความเสี่ยง regression ต่ำสุด

### ✅ verify แล้ว
- `npm run lint` + `npm run build` ผ่าน 0 error ทั้ง 2 ก้อน
- REST ก้อน 1: ตาราง `customers` 200 · `orders.customer_id` มีจริง · RPC 2 ตัวตอบ guard ถูก
- REST ก้อน 2: 3 คอลัมน์ใหม่มีจริง · `job_no_counters` 200 · `create_production_jobs` ตอบ guard ถูก · **`create_job_with_order` ตอบ `PGRST202` = drop สะอาด ไม่มี overload ค้าง** · `next_job_nos` validate ทำงาน
- 📝 Notion **"🍉 Part B.2 claude เเก้"** `3c092ef2-c18f-80d7-8f1d-c0f001a12ae0` — เขียนครบทั้ง 5 ก้อน (ก้อน 1–2 = เสร็จแล้ว) + เช็กลิสต์ทดสอบ + SQL ให้รันเอง

### ▶️ พรุ่งนี้เริ่มตรงนี้
- ⚠️ **ฝ่ายคลังต้องคีย์ล็อตของจริงเข้าใหม่** ที่หน้า "ผลิตภัณฑ์คลัง" ก่อนเริ่มใช้งาน (คลังถูกล้างหมด · ทะเบียนยายังอยู่ครบ)
- ⚠️ **ยังไม่มีใครทดสอบ UI จริงก้อน 1–2** — เช็กลิสต์อยู่ใน Notion · ที่คุ้มสุด: สร้างงาน 1 ใบต้องได้ `690001` แล้ว 3 ใบทีเดียวต้องได้ `690002–690004` + ขึ้นหน้าสรุป
- 🔜 **ก้อน 3 (`0049_job_lot_entry.sql`) เป็นตัวถัดไป** — ปิดช่องว่างที่ก้อน 2 เปิดไว้: ตอนนี้สร้างงานแล้ว **ยังไม่มีที่กรอกเลขล็อตเลย**
  `set_job_lot(p_job_id, p_lot_no, p_mfg_date, p_exp_date)` — สิทธิ์ production/manager · ล็อกเมื่อสถานะถึง `in_production` · UI inline panel ในหน้ารายละเอียดงาน
- ก้อน 4 (ลบถาวรผลิตภัณฑ์) · ก้อน 5 (ฝ่ายวางแผนปลดสถานะล็อต) — ไม่มี dependency สลับลำดับได้
- **migration ล่าสุด = 0048** · ของค้างใน working tree จาก 2 ก.ค. **ยังไม่ตัดสินใจ (ค้างมา 4 รอบแล้ว)**:
  `index.html` (ราก) · `docs/presentation-script.html/.pdf` · `docs/presentation.html`

---

## 📅 บันทึกวันที่ 17 สิงหาคม 2569 — Part 2.1 แก้ตามผลทดสอบ UI จริง ครบ 3 ก้อน (0044–0046) 🎉

### ที่มา
ผู้ใช้ทดสอบ Part 2 บนเว็บจริงเอง แล้วส่งรายงานปัญหา 9 ข้อใน 3 พื้นที่ พร้อมสั่งว่า **"ต้องช่วยเช็กด้วย อย่าเชื่อทั้งหมด"**
→ ส่ง Explore agent 3 ตัวตรวจโค้ดจริงก่อนลงมือ · แผนเต็ม: `~/.claude/plans/dreamy-orbiting-catmull.md`

### 🔍 ผลตรวจ — จริง 6 · **คลาดเคลื่อน 3** (ดีที่ตรวจก่อน)
| ข้อกล่าวหา | ผล |
|---|---|
| ปุ่ม "ลบ" ไม่ได้ลบจริง | ✅ จริง |
| ~~ข้อมูลที่ลบยังโชว์ซ้อนในลิสต์~~ | ❌ **ซ่อนอยู่แล้ว** (`recipes-view.tsx:43,115`) + มีปุ่ม "กู้คืน" · **ปัญหาจริง = ข้อมูลตายสะสมใน DB** |
| ลำดับสถานี +10 | ✅ จริง — **regression**: `97e5952` เขียนทับ fix `9f1756d` ที่เคยแก้เป็น +1 |
| ~~"ประเภท" ไม่ได้ใช้แล้ว~~ | ❌ **ยังใช้อยู่** — เป็นตัวกรอง `.eq("type","fg")` หน้าสร้างงาน (ผู้ใช้ยืนยันให้เลิกใช้ รับผลข้างเคียง) |
| งานเก่าไม่อัปสถานีตาม | ✅ จริง **และหนักกว่าที่รายงาน** (ดูล่าง) |
| ~~dropdown ไม่หุบ (บั๊ก UI)~~ | ❌ `size={8}` ทำให้เป็น **listbox** ไม่ใช่ dropdown (เกิดเฉพาะล็อต >6) |
| ~~search ถูกล้าง (React 19)~~ | ❌ โค้ดสั่ง `setLotQuery("")` เองที่ `requisitions.tsx:74` |
| สถานะ QC 6 ค่ามากไป · วันที่/สิทธิ์ในฟอร์มล็อต | ✅ จริง |

> 🚨 **บั๊กที่ไม่มีใครรายงาน แต่หนักสุด:** งานที่ `job_routes` ว่าง ทำให้ **ด่าน GMP `in_production→qc` ไม่บล็อกเลย**
> (`0034:137-157` — `not exists(job_routes)` = true → ผ่านทันที) กดส่ง QC ได้โดยไม่ต้องตรวจ in-process สักสถานี
> และ **ไม่มี RPC/UI ไหนซ่อม route ย้อนหลังได้เลย** — ไม่ใช่แค่ "เลือกสถานีมั่วได้" อย่างที่รายงาน

### ✅ ทำอะไรไปบ้าง (3 ก้อน · paste + verify REST + push ครบ)

**ก้อน 1 — ลบจริง + สิทธิ์ฝ่ายคลัง + เลิกใช้ "ประเภท"** (`0044` · commit `8c33852`)
- **`delete_product` / `delete_station`** — ลบจริงถ้ายังไม่มีใครใช้ · ติด FK แล้ว **เปลี่ยนเป็นปิดใช้งานให้อัตโนมัติ** + คืนข้อความไทยบอกว่าติดอะไร (`jsonb {action, message}`)
  ⚠️ **ต้อง pre-check `material_lots` เอง** — FK เป็น cascade ถ้าไม่ดัก ลบยา 1 ตัว = ล็อตในคลังหายเงียบทั้งหมด
- `can_manage_products()` = วางแผน + **คลัง** + ผู้บริหาร → ฝ่ายคลังจัดการผลิตภัณฑ์ได้แล้ว (route ยังเป็นผู้บริหารเท่านั้น)
- **drop `products.type`** + ตัดช่อง "ประเภท" · ป้ายสี ยา/RM/PM · ตัวกรอง `fg` · `PRODUCT_TYPES` ออกทั้งระบบ
- `recipes-view.tsx:166` `+10` → **`+1`** (+ เปลี่ยนเป็น `Math.max` เพราะของเดิมนับสถานีที่ปิดใช้งานด้วย)

**ก้อน 2 — อุดช่องโหว่ route + UI หน้าเบิก** (`0045` · commit `f649215`)
- `product_has_route()` + **ด่านใน `create_job_with_order`**: ยาไม่มีขั้นตอนการผลิต = สร้างงานไม่ได้
- **`sync_job_route()`** — ปุ่มซ่อมงานเก่า · เงื่อนไข 2 ชั้นกันชน snapshot GMP: route ต้อง**ว่างจริง** + สถานะยังไม่พ้น `in_production`
- **ตัด fallback "โชว์ทุกสถานี" ออก 2 จุด** (`page.tsx:116-127`, `:146-149`) → เตือนชัดแทนการเงียบ
- แถบเตือน + ปุ่ม "🔄 ดึงขั้นตอนการผลิตล่าสุดมาใส่" บนหัวงาน · หน้าสร้างงานปิดปุ่มถ้ายาไม่มี route
- **หน้าเบิก:** ลบ `size={8}` (dropdown หุบได้แล้ว) · กดขอเบิกแล้ว **คงคำค้น + ฟอร์มเปิดค้างไว้** + ขึ้น "✓ ส่งคำขอเบิกแล้ว"
- 🔍 **เจอเพิ่มนอกรายงาน:** `create_job_with_order` เดิมลอก `product_routes` มา**โดยไม่กรองสถานีที่ปิดใช้งาน** → งานใหม่มีสถานีที่บันทึกผลไม่ได้ค้างในลิสต์ (สำคัญเพราะ 0040 เพิ่งปิด `ST-MILL`/`ST-SIEVE`) — กรอง `is_active` ทั้ง create และ sync แล้ว

**ก้อน 3 — สถานะล็อต 2 ค่า + สิทธิ์ฝ่ายวางแผน** (`0046` · commit `21e4d40`)
- แปลงข้อมูลเก่า: `released→available` · `testing/rejected/expired→quarantine` (ไม่แตะ enum — ลบค่า enum ทิ้งไม่ได้)
- `can_set_lot_status()` = วางแผน/ผู้บริหาร + **field-level guard** ใน `upsert_product_lot`
  (เพิ่มใหม่บังคับ `quarantine` · แก้ของเดิมคงค่าเดิม — แพตเทิร์นเดียวกับ `upsert_machine` ใน 0039)
- `request_material`/`issue_requisition` เปลี่ยนด่านเป็น **whitelist `available`** + `getSelectableLots` กรองให้ตรงกัน
  (ของเดิมกันแค่ rejected/expired → ขอเบิกล็อตที่ยังไม่ปล่อยได้ **แล้วไปตายตอนคลังกดจ่าย**)
- ฟอร์ม: `"สถานะ QC"`→`"สถานะ"` · dropdown 6 ตัว → **ปุ่มกดติ๊ก 2 ปุ่ม** · ย้ายวันรับเข้า/วันหมดอายุเข้ากลุ่ม "หมายเหตุ" (**ยังเป็นช่อง date → FEFO + ด่านหมดอายุไม่เสีย**)
- เพิ่มคอลัมน์ **"หมายเหตุ"** ในตารางล็อต — เดิมบันทึกไว้แต่ไม่แสดงที่ไหนเลย

### 🔑 Decisions ที่ผู้ใช้ยืนยัน (ก่อนลงมือ)
1. **ลบจริงก่อน ติดแล้วปิดใช้งานให้อัตโนมัติ** พร้อมบอกเหตุผล (ไม่ให้ error เฉยๆ)
2. **วันที่ = ย้ายตำแหน่งอย่างเดียว** ยังเป็นช่อง date เหมือนเดิม (ตัวเลือกอื่นจะทำให้ FEFO + ด่าน GMP พัง)
3. **แปลงข้อมูลสถานะให้เหลือ 2 ค่าจริง** (ยอมเสียร่องรอย rejected — audit log ยังเก็บไว้)
4. **route: กันต้นทาง + ปุ่มซ่อม + ตัด fallback** ครบ 3 ชั้น
5. **ล็อตใหม่ตั้งต้น "ไม่พร้อมใช้"** รอฝ่ายวางแผนปลด
6. **เลิกใช้ `products.type` ทั้งระบบ** (รับผลข้างเคียงว่า dropdown สร้างงานจะมี RM/PM ปน)

### 🧠 บทเรียน/แง่คิดรอบนี้
- **"อย่าเชื่อรายงานทั้งหมด" คุ้มมาก** — 3 ใน 9 ข้อสาเหตุจริงคนละเรื่องกับที่เข้าใจ ถ้าแก้ตามที่รายงานจะแก้ผิดจุด
- **regression กลับมาได้ถ้าเขียนไฟล์ทับ** — `+1` เคยแก้แล้วที่ `9f1756d` แต่ Part 2 เขียน `recipes-view.tsx` ใหม่ทั้งไฟล์เลยกลับเป็น `+10`
  (คนละแบบกับ gotcha `create or replace` ของ SQL แต่รากเดียวกัน: **เขียนทับของเดิมโดยไม่ได้ดูว่าเคยแก้อะไรไว้**)
- **ลำดับ validate vs guard สำคัญ** — ตอนแรกวาง validate สถานะไว้ก่อน field-level guard → ล็อตค่าเก่าที่หลุดรอดจะกลายเป็น**แก้ไม่ได้เลย** ย้ายให้ validate เฉพาะคนที่มีสิทธิ์ตั้งจริง
- **fallback ที่ "ใจดี" = ช่องโหว่เงียบ** — `jobRoute.length ? … : activeStations` ดูเหมือนกันแอปพัง แต่จริงๆ คือปิดปากไม่ให้ผู้ใช้รู้ว่าข้อมูลไม่ครบ **และทำให้ด่าน GMP หลุดไปด้วย**
- **RLS กั้น Claude verify ข้อมูลเองไม่ได้** — REST ด้วย publishable key แบบไม่ล็อกอินอ่านตารางไม่ได้ (ตอบ `[]`) → verify ได้แค่ "ฟังก์ชัน/คอลัมน์มีจริงไหม" ส่วน**ข้อมูลถูกแปลงจริงหรือไม่ต้องให้ผู้ใช้รัน query เอง**

### ✅ verify แล้ว
- `npm run build` + `npm run lint` ผ่าน 0 error ทุกก้อน
- REST: `products.type` หายจริง (`42703`) · RPC ใหม่ 6 ตัวตอบ guard ถูก · **signature เก่าถูก drop สะอาดทั้ง `upsert_product` (7→6) และ `create_job_with_order` (9→13)** = ไม่มี overload ซ้อน
- **Vercel deploy Ready ทั้ง 4 ตัว** · alias `pd-monitor.vercel.app` ชี้ deploy ล่าสุด · หน้า login ตอบ HTTP 200
- ✅ **`vercel ls` ใช้ได้แล้ว** — CLI login สำเร็จ (note เดิมบันทึกว่าหมด auth) ตรวจ deploy จากเครื่องได้เหมือนเดิม
- 📝 Notion **"🖐🏼 claude part B เเก้ไขเพิ่มเติม"** `3bf92ef2-c18f-80df-b135-d44fc09966d3` เขียนครบทั้ง 3 ก้อน (ภาษาคน ไม่ใช่ภาษาโปรแกรม)

### ▶️ ครั้งหน้าเริ่มตรงนี้
- ⚠️ **ยังไม่มีใครทดสอบ UI จริง** (เหมือน Part 2 เป๊ะ) — เช็กลิสต์ 17 ข้ออยู่ใน Notion
  flow ที่คุ้มสุด: ฝ่ายคลังเพิ่มผลิตภัณฑ์+ล็อต → ฝ่ายวางแผนกดปลดสถานะ → สร้างงาน → เบิก → บันทึกผลผลิต → ส่ง QC
- ⚠️ **3 query ที่ต้องให้ผู้ใช้รันเช็ก** (Claude อ่านตารางเองไม่ได้):
  ```sql
  select status, count(*) from public.material_lots group by status;              -- ต้องเหลือ 2 ค่า
  select code,name from public.products p where p.is_active and not public.product_has_route(p.id);  -- สร้างงานไม่ได้
  select j.job_no,j.status from public.jobs j
   where not exists (select 1 from public.job_routes jr where jr.job_id=j.id);    -- ต้องกดปุ่มซ่อม
  ```
  ถ้า 2 อันหลังเยอะจนติดงานจริง → ปรับด่านเป็น "เตือนอย่างเดียว" ได้ที่ `0045` จุดเดียว
- **งานที่เหลือจากเอกสาร feedback** (ยังเท่าเดิม ดู `docs/feedback-raw-2026-08.md` §9):
  **สถานะย่อย ~20 ขั้น + UNPLAN + แผน MM/YY** (ก้อนใหญ่สุด) · เปลี่ยนชื่อช่อง FORMULA/JOB/BATCH NO. + BATCH SIZE + กำหนดส่ง 75 วัน ·
  ตรวจผลรวมขนาดบรรจุ ≤ Batch Size · พิมพ์ใบแจ้งผลิต F.PLN.01 + Export 12 คอลัมน์ + PENDING ORDER ·
  Line Clearance ต่อสถานี · แยก OOS/NC/Deviation · บันทึกผลผลิต กะ/OT/ห้อง/นาที
- **migration ล่าสุด = 0046** · ของค้างใน working tree จาก 2 ก.ค. **ยังไม่ตัดสินใจ (ค้างมา 3 รอบแล้ว)**:
  `index.html` (ราก) · `docs/presentation-script.html/.pdf` · `docs/presentation.html`

---

## 📅 บันทึกวันที่ 14 สิงหาคม 2569 — Part 2 ครบ 4 ก้อน (migration 0040–0043) 🎉

### ที่มา
ผู้ใช้ส่งเอกสารเขียนมือ 3 ฉบับจากทีม (ฝ่ายวางแผน · ฝ่ายผลิต · QA/QC) ให้ศึกษาเก็บเป็นข้อมูลอ้างอิงก่อน
แล้วสั่งแก้เป็น **Part 2** โดยแบ่งทำทีละก้อน · แผนเต็ม: `~/.claude/plans/woolly-brewing-owl.md`

> **แก่นของ Part 2 = โรงงานไม่ได้ใช้ BOM** — ฝ่ายวางแผนเขียนไว้ว่า *"ยังไม่เพิ่มการใช้ BOM"*
> ขอเปลี่ยนเป็น "กรอกเฉพาะชื่อ product + บรรจุภัณฑ์" แทนการกรอกวัตถุดิบทีละตัว

### 📚 ก่อนเริ่ม — เก็บ feedback เป็นเอกสารอ้างอิง (commit `dfda5d3`)
- PDF สแกน 3 ฉบับ (68MB + 43MB + 6MB) เปิดด้วย Read ไม่ได้ (ไม่มี poppler)
  → **เรนเดอร์ด้วย `pypdfium2` + PIL ที่มีใน Python เครื่องนี้** (สคริปต์อยู่ scratchpad)
- อ่านครบ **71 หน้า** → ถอดลายมือทั้งหมดเป็น **`docs/feedback-raw-2026-08.md`** (แยกตามพื้นที่ระบบ + คำถามค้าง 10 ข้อ)
- ภาพหน้าที่มีลายมือ 41 หน้า เก็บที่ **`docs/feedback-img/`** · `CLAUDE.md` ชี้ทางให้ session หน้าอ่านก่อนเริ่ม Part ใหม่

### ✅ ทำอะไรไปบ้าง (4 ก้อน · paste + verify REST + push ครบทุกก้อน)

**ก้อน 1 — หน้า "ผลิตภัณฑ์ / ขั้นตอนการผลิต"** (`0040` · commit `97e5952`)
- `products` เพิ่ม **`unit`** (TAB/CAP/LT./KG.) · **`reg_no`** (เลขทะเบียนตำรับ) · **`type`** (`fg`/`rm`/`pm` — text+check ไม่ใช้ enum เลี่ยง gotcha paste แยกไฟล์)
- RPC ใหม่ 3 ตัว: `upsert_product` (planner+manager) · `set_product_active` · `set_station_active`
- **สถานี 9 ตัวจริง**: เปลี่ยนชื่อ 8 + เพิ่ม `ST-BAND` คาดแคปซูล + **ปิดใช้ `ST-MILL`/`ST-SIEVE`** (บด/ร่อน ยุบเข้าผสมเปียก ตามที่ทีมเขียน)
- ตัดจากหน้าจอ: `＋ เพิ่มสูตร` · `รายการวัตถุดิบ (BOM)` · `แก้บรรจุ` → เพิ่มฟอร์มผลิตภัณฑ์ + ปุ่ม `แก้ไข`/`ลบ`
- **ลบตัวกรอง BOM ในระบบเบิกพร้อมกันในก้อนนี้** (ถ้าเลื่อนไป ก้อน 3 จะทำ dropdown ล็อตว่างแบบเงียบ)

**ก้อน 2 — หน้าสร้างงานผลิต + รูปแบบบรรจุ** (`0041` · commit `2b4e911`)
- `jobs` เพิ่ม `pack_type` + `pack_pattern_1/2/3` — **บรรจุย้ายจากระดับยา → ระดับงาน** (ยาตัวเดียวกันแต่ละล็อตบรรจุคนละแบบ)
- `create_job_with_order` **drop signature เก่า (9) → สร้างใหม่ (13)** · ยกบอดี้จาก 0039 ครบ · ตัด auto-select สูตร + เลิกเขียน `jobs.recipe_id`
- `drop create_product` · ตัดปุ่ม `＋ เพิ่มยาใหม่` ออกจากหน้าสร้างงาน
- dropdown ยากรอง `type='fg'` → RM/PM ไม่โผล่ให้เลือกผลิต · เลือกยาแล้วเติมหน่วยจากทะเบียนอัตโนมัติ
- แสดง "รูปแบบบรรจุ/ขนาดบรรจุ" ที่หัวงาน + eBR ส่วน 1

**ก้อน 3 — "ผลิตภัณฑ์คลัง" (ย้ายข้อมูลจริง)** (`0042` · commit `f609182`)
- คัดลอก `materials` → `products` (type = rm/pm ตามเดิม) · `material_lots.material_id` → **`product_id`**
- **pre-flight อยู่ในไฟล์เอง**: รหัสชนกัน / ล็อตหาปลายทางไม่เจอ → `raise` แล้ว rollback ทั้งชุด · **รันซ้ำได้**
- `upsert_product_lot` แทน `upsert_material_lot` · drop `upsert_material`
- ตัด `＋ เพิ่มวัตถุดิบ` + ปุ่ม `แก้ข้อมูล` ออกจากหน้าคลัง (แก้ผลิตภัณฑ์ที่หน้าผลิตภัณฑ์ที่เดียว)

**ก้อน 4 — เบิกผลิตภัณฑ์ + ค้นหาล็อต** (`0043` · commit `8391506`)
- `create or replace` 3 RPC เบิก **โดยไม่เปลี่ยน signature** — เปลี่ยนแค่ข้อความไทย "วัตถุดิบ"→"ผลิตภัณฑ์" ไม่แตะด่านกั้นใดๆ
- **ช่องค้นหาล็อต** กรองฝั่ง client จาก label (รหัส/ชื่อ/เลขล็อต) · แสดง "พบ n ล็อต (จากทั้งหมด m)" · เกิน 6 ตัวเปลี่ยน select เป็น list
  · **ล็อตที่เลือกไว้แล้วคงอยู่ในลิสต์เสมอ** (กันค่าใน form หลุดเงียบ)

### 🔑 Decisions ที่ผู้ใช้ยืนยัน (ก่อนลงมือ)
1. **แยก RM/PM ด้วยคอลัมน์ `products.type`** — dropdown สร้างงานกรอง `fg` · หน้าเบิกเห็นทุกประเภท
2. **ปุ่ม "ลบ" = ปิดใช้งาน (soft delete)** ทั้งสถานีและผลิตภัณฑ์ — FK RESTRICT 4 จุด + ประวัติ GMP
3. **Packing Size = 3 คอลัมน์ตายตัว** (ตรงกับใบแจ้งผลิต F.PLN.01 ที่มีช่อง (1)(2)(3))
4. **"ชนิด" = เปลี่ยนป้ายของ `dosage_form` เดิม** ไม่ลบ-สร้างใหม่ (ข้อมูลยาเดิมไม่หาย)

### 🧠 บทเรียน/แง่คิดรอบนี้
- **ตัด BOM ต้องตัดตัวกรองในระบบเบิกพร้อมกัน** — ไม่งั้นตัวกรองจะเทียบ material-id กับ product-id → dropdown ว่างเปล่า **โดยไม่มี error** (บั๊กเงียบแบบเดียวกับ `VALID_ROLES` รอบที่แล้ว)
- **ใส่ pre-flight ไว้ใน migration เอง** (raise + rollback) ดีกว่าให้ผู้ใช้รัน query เช็กเองก่อน — กันลืมและกันเดาแทน
- **ไม่เปลี่ยนชื่อตาราง `material_lots`/`material_requisitions`** แค่สลับ FK column → ประหยัด RLS · realtime · audit label แถวเก่า · และ **RPC เบิก 3 ตัวไม่ต้องแตะโครงเลย**
- verify overload: ถ้ามีฟังก์ชันซ้อน 2 ตัว PostgREST จะตอบ `PGRST203` (เลือกไม่ถูก) — เรียกแล้วผ่านปกติ = ไม่ซ้อน

### ▶️ ครั้งหน้าเริ่มตรงนี้
- ⚠️ **ยังไม่มีใครทดสอบ UI จริงเลยสักก้อน** — เช็กลิสต์อยู่ใน Notion "🛠️ Claude ปรับ" รอบที่ 2–5 ครบแล้ว
  flow ที่คุ้มสุด: เพิ่มผลิตภัณฑ์ (ยา+RM) → สร้างงานใส่บรรจุ 2 ขนาด → เพิ่มล็อต → เบิก+ค้นหา → คลังจ่าย → ดู eBR
- ⚠️ **ฝ่ายคลังเพิ่ม RM/PM เองไม่ได้แล้ว** (สิทธิ์เพิ่มผลิตภัณฑ์ = planner/manager) — ถ้าทีมอยากได้เหมือนเดิม แก้ guard `upsert_product` จุดเดียวเป็น `can_plan_jobs() or can_manage_materials()`
- **งานที่เหลือจากเอกสาร feedback** (ดู `docs/feedback-raw-2026-08.md` §9 + Notion รอบที่ 5):
  เปลี่ยนชื่อช่อง FORMULA/JOB/BATCH NO. + BATCH SIZE + กำหนดส่ง 75 วัน · ตรวจผลรวมขนาดบรรจุ ≤ Batch Size ·
  **สถานะย่อย ~20 ขั้น + UNPLAN + แผน MM/YY** (ก้อนใหญ่สุด กระทบ dashboard/report) ·
  พิมพ์ใบแจ้งผลิต F.PLN.01 + Export ตาราง 12 คอลัมน์ + PENDING ORDER ·
  Line Clearance ต่อสถานี · แยก OOS/NC/Deviation · บันทึกผลผลิต กะ/OT/ห้อง/นาที
- **migration ล่าสุด = 0043** · ของค้างใน working tree จาก 2 ก.ค. ยังไม่ตัดสินใจ:
  `index.html` (ราก) · `docs/presentation-script.html/.pdf` · `docs/presentation.html` (แก้เพิ่มบัญชีเดโม + รหัส `123` ลงสไลด์)

---

## 📅 บันทึกวันที่ 11 สิงหาคม 2569 — Part A ตั้งค่าระบบ (feedback ทีมหลังใช้จริง) — migration 0038–0039

### ที่มา
ส่งแอป/เอกสารให้ทีมไปใช้จริง → ทีมส่ง feedback กลับมาเป็น **"Part A — ตั้งค่าระบบ"** 2 หัวข้อ (A1 หน้าจัดการผู้ใช้ · A2 หน้าเครื่องจักร)
แผนเต็ม: `~/.claude/plans/streamed-knitting-snowglobe.md`

> **Decisions (ผู้ใช้ยืนยัน):** (1) เครื่องจักร = แยกหน้าที่ — ผลิตเพิ่ม/แก้ข้อมูลทั่วไปได้ แต่ **ช่องกำหนดซ่อม/สอบเทียบเป็นของวิศวกรรม/ผู้บริหารเท่านั้น**
> (2) PLN = สร้างงาน + เพิ่มยาใหม่ + ยืนยันแผนผลิต (3) COST = เห็นบล็อกต้นทุนค่าแรง + ปรับอัตรา ฿/ชม. ได้ (4) ชื่อ role ใน DB เก็บเป็นอังกฤษพิมพ์เล็ก `planner`/`cost`/`engineering` (แสดงบนจอเป็น PLN/COST/ENG)

### ✅ วันนี้ทำอะไรไปบ้าง (3 ก้อน)

**ก้อน 1 — DB (`0038` + `0039`) · paste + verify REST ครบแล้ว**
- **`0038_roles_add_values.sql`** — เพิ่ม enum `app_role` 3 ค่า: `planner` · `cost` · `engineering`
  ⚠️ **ต้อง paste แยกรอบก่อน 0039** — Postgres ห้ามใช้ค่า enum ใหม่ในทรานแซกชันเดียวกับที่เพิ่ง `ADD VALUE` (SQL Editor รันทั้งไฟล์เป็น 1 tx)
- **`0039_part_a_roles_machine_room.sql`**
  - คอลัมน์ใหม่ **`machines.room`** (ห้องที่ติดตั้งเครื่อง)
  - helper 3 ตัว: `can_manage_machines()` (ผลิต/วิศวกรรม/ผู้บริหาร) · `can_set_machine_schedule()` (วิศวกรรม/ผู้บริหาร) · `can_plan_jobs()` (วางแผน/ผู้บริหาร)
  - **`upsert_machine`** — เพิ่มพารามิเตอร์ `p_room` (⚠️ ต้อง `drop function` signature เก่าก่อน ไม่งั้นเป็น overload ซ้อน 2 ตัว) + guard ใหม่
    + **field-level guard:** ใครไม่ใช่วิศวกรรม/ผู้บริหาร → เพิ่มใหม่บังคับวันซ่อม/สอบเทียบเป็น null · แก้ของเดิม **คงค่าเดิมไว้** (กันที่ DB ไม่ใช่แค่ซ่อนช่อง)
  - **`create_product` · `create_job_with_order` · `advance_job_status`** → เปลี่ยน guard เป็น `can_plan_jobs()`
    (ยกบอดี้ล่าสุดจาก 0034 มาครบทั้งดุ้น — gate line clearance + in-process + deviation + บล็อกแจ้งเตือน ตาม gotcha regression เดิม)
  - **`current_role_group()`** เพิ่ม branch `engineering` (ให้วิศวกรรมเขียนหมายเหตุ deviation เครื่องเสียได้)

**ก้อน 2 — A1 หน้าจัดการผู้ใช้ + เปิดใช้ role ใหม่ทั่วแอป**
- **ใหม่ `web/lib/data/role-access.ts`** = แหล่งความจริงเดียวของสิทธิ์ฝั่งแอป
  (`ROLE_ACCESS` ใช้ render ตารางอธิบายสิทธิ์ + helper `canPlanJobs`/`canManageMachines`/`canSetMachineSchedule`/`canSeeCost` ที่**ต้องตรงกับ guard ใน DB**)
- `/admin/users`: **แถบชิปนับบัญชีแต่ละฝ่าย** (9 role + "ยังไม่กำหนดสิทธิ์") · กดชิป = **กรองรายชื่อ** · แผงพับ **"📋 แต่ละสิทธิ์เข้าถึงอะไรได้บ้าง"** (ตารางบน PC / การ์ดบนมือถือ)
- PLN: ปุ่มสร้างงาน (`board`, `board/new`) + ปุ่ม "ยืนยันแผนผลิต" (`job-constants` TRANSITIONS) · COST: บล็อกต้นทุน DL + ช่องอัตราค่าแรงบนแดชบอร์ด
- `deviation-constants` เพิ่มป้ายฝ่าย "วิศวกรรม"

**ก้อน 3 — A2 หน้าเครื่องจักร**
- ช่อง **"ห้อง"** ในฟอร์ม + chip 🚪 บนการ์ดรายการ
- สิทธิ์: **ฝ่ายผลิตเพิ่ม/แก้เครื่องได้แล้ว** (เดิมผู้บริหารอย่างเดียว) · ช่องกำหนดซ่อม/สอบเทียบเห็นเป็น **อ่านอย่างเดียว** ถ้าไม่ใช่วิศวกรรม/ผู้บริหาร
- **ฟอร์ม "เพิ่มเครื่องจักร" ตัดช่อง "ทำความสะอาดล่าสุด" ออก** (เหลือเฉพาะตอนกดแก้ไข) ตามที่ทีมขอ

### ✅ verify แล้ว
- `npm run build` + `npm run lint` ผ่าน (0 error)
- REST (publishable key): `machines.room` select ผ่าน · RPC `can_plan_jobs`/`can_manage_machines`/`can_set_machine_schedule` ตอบ 200 (false) ·
  `upsert_machine` รับ `p_room` แล้วชน guard "ยังไม่ได้เข้าสู่ระบบ" · เรียกด้วยชุดพารามิเตอร์เดิม (ไม่มี p_room) ก็ยังตอบ guard เดิม = **ไม่มี overload ซ้อน** ·
  `admin_set_roles` รับ `["planner","cost","engineering"]` ผ่าน enum แต่ค่ามั่วโดน `22P02` = **0038 ลงจริง**
- commit `25cdcd4` (DB) · `ffb8723` (A1) · `c4e450d` (A2) — push แล้ว
- ⚠️ **`vercel ls` ใช้ไม่ได้แล้ว — Vercel CLI หมด auth** ("The request is missing an authentication token") → ตรวจ deploy ต้องดูที่หน้าเว็บ Vercel เอง หรือ `vercel login` ใหม่

### 🔧 แก้ตามหลัง — บันทึกสิทธิ์ 3 role ใหม่ไม่ลง DB (commit `80b5f1d`)
- **อาการ:** ผู้ใช้ติ๊ก PLN/COST/ENG ในหน้าจัดการผู้ใช้ → กดบันทึกขึ้นว่าสำเร็จ **แต่ DB ไม่เปลี่ยน** (role ใหม่ใช้งานจริงไม่ได้)
- **สาเหตุ:** `web/app/(app)/admin/users/actions.ts` มี **`VALID_ROLES` = รายชื่อ role ที่ก็อปไว้ซ้ำอีกชุด** (ยังเป็นของเดิม 6 ตัว)
  และ `cleanRoles()` ใช้มันกรองก่อนส่งเข้า RPC `admin_set_roles` → 3 role ใหม่ถูกคัดทิ้ง**เงียบๆ** ไม่มี error ให้เห็น
  (ตอนวางแผนสรุปว่า "ไม่ต้องแตะ actions.ts" — พลาดตรงนี้ เพราะดูแค่ว่า RPC รับ `app_role[]` ได้)
- **แก้:** `cleanRoles()` เปลี่ยนไปกรองด้วย `ALL_ROLES` จาก `lib/nav.ts` (รายชื่อเดียวของทั้งระบบ) + คอมเมนต์เตือนห้ามก็อปรายชื่อ role มาไว้ซ้ำอีก
- **แง่คิด:** เพิ่มค่าใน enum ต้องไล่หา **whitelist ฝั่งแอปที่ก็อปรายชื่อไว้ซ้ำ** ด้วยเสมอ — filter แบบนี้ล้มเหลวแบบเงียบ (ไม่ error) จับได้ยากกว่าพัง
- ⚠️ สิทธิ์ที่เคยกดบันทึกไปก่อนหน้านี้ = ไม่ได้ลง DB จริง ต้องกดใหม่หลัง deploy

### ⚠️ เหลือผู้ใช้ทดสอบ UI จริง (สร้างบัญชีทดสอบ 3 ใบจาก /admin/users)
- **PLN:** สร้างงานผลิต + เพิ่มยาใหม่ + กด "ยืนยันแผนผลิต" ได้ · แก้เครื่องจักร/อนุมัติ QC ไม่ได้
- **COST:** แดชบอร์ดเห็น "ต้นทุนค่าแรงทางตรง" + ปรับ ฿/ชม. ได้ · สร้างงานไม่ได้
- **ENG:** เพิ่ม/แก้เครื่อง + ตั้งกำหนดซ่อม/สอบเทียบได้
- **ฝ่ายผลิต:** กด "＋ เพิ่มเครื่องจักร" ได้ · ฟอร์มเพิ่มไม่มีช่องทำความสะอาดล่าสุด · แก้เครื่องเดิมได้แต่ช่องซ่อม/สอบเทียบเป็นอ่านอย่างเดียว
- **ผู้ดูแล:** /admin/users เห็นแถบนับบัญชี (กดกรองได้) + ตารางสิทธิ์ครบ 9 role

### ▶️ ครั้งหน้าเริ่มตรงนี้
- **เช็กก่อนเริ่มงานใหม่:** ให้ผู้ใช้ติ๊กสิทธิ์ PLN/COST/ENG **ใหม่อีกครั้ง** (หลัง commit `80b5f1d` deploy แล้ว) → ป้ายสิทธิ์ต้องขึ้นข้างชื่อบัญชี + ตัวเลขบนแถบนับต้องเพิ่ม
- **ทีมน่าจะส่ง Part B / Part C ตามมา** (feedback ชุดนี้ขึ้นต้นว่า "Part A") — รอไฟล์จากผู้ใช้
  · หน้า Notion ที่ใช้บันทึกการแก้ไขให้ทีม = **"🛠️ Claude ปรับ"** `3b992ef2-c18f-806f-8b8a-d8238ae8ac8f` (ใต้หน้าโปรเจคหลัก) — รอบหน้าต่อ "รอบที่ 2" ในหน้าเดิม
- ⚠️ **COST ยังไม่มี guard ที่ DB** (ต้นทุนคำนวณสดจาก `production_records` ที่ทุก role อ่านได้) — ถ้าอยากกันจริงต้องทำ RLS/ view แยกทีหลัง
- ⚠️ ถ้าจะเพิ่ม role อีก: enum ลบค่าไม่ได้ + ต้อง paste แยกไฟล์เหมือนรอบนี้
- migration ล่าสุด = **0039**

---

## 📅 บันทึกวันที่ 3 กรกฎาคม 2569 — อัปเดตคู่มือผู้ใช้ 2 เล่มให้เป็นปัจจุบัน (เอกสารล้วน ไม่แตะโค้ด)

### ที่มา
คู่มือ `docs/user-guide.md` (อ้างอิงละเอียด) + `docs/tutorial-walkthrough.md` (ฝึกลงมือ 1 ล็อต) เขียนล่าสุด **25 มิ.ย. 69**
→ ตกยุคหลายรอบ (migration 0027–0037 + แก้ UI) ผู้ใช้เปิดคู่มือแล้วไม่ตรงของจริง · แผน: `~/.claude/plans/tutorial-walkthrough-tingly-clock.md`

### ✅ วันนี้ทำอะไรไป
1. **แก้ข้อมูลที่ผิด (stale) 1 จุด:** user-guide ขั้นที่ 6 เขียน "บันทึกผลผลิตได้ตอน กำลังผลิต/QC/QA" → ของจริง **"กำลังผลิต เท่านั้น"** (`RECORDABLE_STATUSES` 0028)
2. **เติมฟีเจอร์ที่คู่มือยังไม่มี** (ตรวจโค้ดจริงก่อนเขียน): F1 คำขอแก้ไขย้อนหลัง (Amendment) · ด่านกั้น In-process QC ก่อนส่ง QC + แถบความคืบหน้า QC ตามสูตร · แจ้งเตือนครบ 7 ชนิด (เพิ่ม arrival/edit_request/edit_reviewed) · deviation หมายเหตุแยกฝ่าย + ส่งกลับ QA · เบิกตาม BOM · บันทึกผลผลิตเลือกสถานีตาม route · ลบงาน · Audit 30/หน้า · งาน FG หายจากบอร์ด
3. **สเต็ปใหม่ใน tutorial: C10 ฝึกขอแก้ไขย้อนหลัง** (ผลิตขอแก้จำนวนคน → ผู้บริหารอนุมัติ → เห็นใน Audit)
4. **สกรีนช็อต 4 รูปใหม่** — จับจากเว็บจริงด้วย **puppeteer-core script** (วิธีเดิม เซฟ PNG ลงโฟลเดอร์ตรง) เพราะ browser MCP tool `save_to_disk` ไม่คืน path ให้เขียนไฟล์ลง repo:
   `tutorial-img/t90-edit-request-modal.png` · `t91-edit-approve.png` · `user-guide-img/25-edit-requests.png` · `26-inprocess-progress.png`

### ไฟล์ที่เปลี่ยน
- `docs/user-guide.md` (15 จุด) · `docs/tutorial-walkthrough.md` (9 จุด รวม C10) · + รูป 4 ไฟล์

### ✅ ทำครบแล้ว (อัปเดตท้าย session)
- **PDF 2 เล่ม export ใหม่แล้ว** (pagedjs: user-guide 33 หน้า · tutorial 43 หน้า) — commit `4df38f8`
- โพสต์ Dev Log กลับ Notion แล้ว (sub-page `39292ef2-c18f-81eb-97cc-f200c70f4992`)

### ▶️ พรุ่งนี้เริ่มตรงนี้ (next steps)
- มี "คำขอแก้ไข" ตัวอย่างค้างบน **JOB-001** (จำนวนคน 2→3) ที่สร้างไว้ถ่ายรูป — กดอนุมัติ/ปฏิเสธทิ้งได้ (ข้อมูลเดโม)
- ไม่แตะโค้ด/migration (ล่าสุดยังเป็น 0037)

---

## 📅 บันทึกวันที่ 3 กรกฎาคม 2569 — แก้ 3 ปัญหา UI จากการใช้งานจริง (frontend ล้วน ไม่มี migration)

### ที่มา
ผู้ใช้รายงาน 3 ปัญหาจากการใช้จริง → ตรวจโค้ดแล้วเป็นบั๊กจริงทั้งหมด · แผนเต็ม: `~/.claude/plans/web-app-hashed-feather.md`

### ✅ วันนี้ทำอะไรไปบ้าง
1. **ข้อ 1 — ฟอร์ม "ขอแก้ไข" ไม่ถูกบีบบน PC** (`web/app/(app)/board/[jobNo]/edit-request-button.tsx`)
   - เดิม panel แก้ไขกางอยู่ *ในช่องตาราง (`<td>`) ที่แคบ* → `grid sm:grid-cols-2` ถูกอัดจนกรอกไม่เห็นครบ
   - เปลี่ยนเป็น **modal ป็อปอัพกลางจอ** (`fixed inset-0` + กล่อง `max-w-lg` + ปุ่ม x + ล็อก body scroll)
   - แก้ไฟล์เดียว มีผลครบทั้ง 3 ตาราง (ผลผลิต/เบิกวัตถุดิบ/in-process QC) โดยไม่ต้องแตะไฟล์ตาราง
2. **ข้อ 2 — sidebar + ปุ่ม logout ค้างในจอ** (`web/components/app-shell.tsx`)
   - `<aside>` เดสก์ท็อปเพิ่ม `md:sticky md:top-0 md:h-screen` → ไม่ยืดตามหน้ายาวอีก
   - เมนู (`flex-1 overflow-y-auto`) เลื่อนภายในเอง · บล็อกชื่อผู้ใช้+ปุ่มออก (`border-t`) ปักมุมซ้ายล่างของ viewport เสมอ
3. **ข้อ 3 — Audit 200 → 30 รายการ + แบ่งหน้า** (`web/lib/data/audit.ts` + `web/app/(app)/audit/page.tsx`)
   - `getAuditLog` เปลี่ยน `.limit(200)` → `.range()` ดึง `pageSize+1` เช็ก hasNext (default 30/หน้า) · return `{ rows, hasNext }`
   - หน้า audit: รับ `?page=` + ปุ่ม "← ก่อนหน้า / ถัดไป →" (คงตัวกรอง table/action ข้ามหน้า · กดกรองใหม่ = กลับหน้า 1)

### ✅ verify
- `cd web && npm run build` ผ่าน · TypeScript เคลียร์ทุก route · dev server บูตไม่มี error
- ⚠️ **ยังไม่ได้เทสใน browser จริง** (Chrome extension ไม่ได้เชื่อม + ผู้ใช้เทสเองแล้วแจ้งว่าเรียบร้อย)

### ▶️ ครั้งหน้าเริ่มตรงนี้
- ถ้าผู้ใช้เจอ edge case เพิ่มค่อยปรับ · ไม่มี migration ค้าง (migration ล่าสุดยังเป็น **0037**)
- งานหลัก/MES-grade ที่เหลือดูบล็อกด้านล่าง (B5–B8) หรือ UAT กับทีม

---

## 📋 Backlog — gap จาก brief CEO (วิเคราะห์ 23 มิ.ย. 69 จาก PDF ผังระบบ + voice.md)
> **✅ โพสต์ Notion แล้ว (23 มิ.ย. 69):** หน้า "🗺️ Roadmap หลัง D9 — เติม brief CEO + ยกระดับเป็น MES (สำหรับทีมศึกษา)"
>   `38892ef2-c18f-81b5-b3d2-e9d9ca793baf` (ใต้หน้าโปรเจคหลัก) — มีรายละเอียดออกแบบ (ตาราง/RPC/RLS) ของ A0–A6 + B1–B8 + ลำดับ D10–D12
> **TODO หลังปิด D9:** (ออปชัน) สรุป roadmap ลง `docs/` ในเครื่องด้วย · เริ่มลงมือ D10 = A0 หน้า Admin จัดการผู้ใช้
>
> **แกนหลักที่ CEO อยากได้ = ทำครบแล้ว** (แทนจดมือ · pending board · in/out รายสถานี · ทุกฝ่ายเห็นตรงกัน · lot/หมดอายุ · ต้นทุนค่าแรง · QC/QA)
> **แต่ยังไม่ครอบคลุม 100% — เหลือสิ่งที่ CEO พูดถึงตรงๆ:**
> 1. 🔴 **ระบบเครื่องจักร (M/C)** — ทะเบียนเครื่อง + "ใช้เครื่องไหน" ตอนบันทึก + รายงานใช้เครื่อง + แจ้งเตือนซ่อม/ทำความสะอาด (PDF น.2 "EN DATABASE")
> 2. 🔴 **เบิกวัตถุดิบ + คลัง RM/PM** (PDF "RM/PM WHS → เบิกของ" · voice "ไปเบิกของ")
> 3. 🔴 **Line Clearance / Set-up / Washing** ก่อนผลิต (PDF "LINE CLEARANCE, SET UP TIME") — GMP
> 4. 🟠 สูตรการผลิต (recipe/BOM) + สถานีย่อยจริง (บด/ร่อน/ผสมแห้ง/ฉาบ/ฟิล์ม/คัด-ขัด) + รูปแบบบรรจุ (Blister/Strip/ซอง/ขวด, แผง 50×10's)
> 5. 🟠 คลัง FG (สต็อกหลังเข้าคลัง) · จำนวนคนต่อขั้น (ต้นทุนคิดแค่ชั่วโมง) · in-process QC + จุดเก็บ sample QA
> 6. 🟢 auto-gen เลขงานต่อไลน์ (ทำง่าย) · หน้า Admin จัดการผู้ใช้ (จำเป็นก่อนเปิด 30 คน)
>
> **MES-grade เสริม (จาก research/Notion 11–23):** eBR · lot genealogy · deviation · notification/escalation · barcode/QR · OEE/downtime · capacity planning · integration ERP/LIMS
> **ลำดับแนะนำ:** รอบหน้า = admin users + เครื่องจักร + เบิกวัตถุดิบ + line clearance + จำนวนคน/auto job no → แล้วค่อย MES-grade

---

## 📅 บันทึกวันที่ 1 กรกฎาคม 2569 — งานใหญ่ 2 ข้อที่เลื่อนไว้: E1/E2 + F1 (ล่าสุด)

### ที่มา
งานใหญ่ที่ค้างจากรอบแก้บั๊ก (ดู log ด้านล่าง) = **E1/E2** (บังคับ flow ตามสูตร/BOM) + **F1** (ขออนุมัติแก้ไขย้อนหลัง)
→ ทำเป็น 2 ก้อนแยก ตามกติกาทีละก้อน · แผนเต็ม: `~/.claude/plans/2-silly-volcano.md`
> **Decisions (ผู้ใช้ยืนยัน):** E2 gate = ระดับสถานีย่อย (10 สถานีตาม route) · E1 = auto เลือกสูตร active ·
> F1 = ครบ 3 ชนิด (ผลผลิต/ใบเบิก/QC) · ผู้อนุมัติ F1 = manager/admin เสมอ · qa เฉพาะข้อมูล QC

### ✅ ก้อน 5 — E1/E2: ผูกสูตร/route กับงาน + gate in-process QC (migration 0031–0032) — verified DB + deployed
- **`0031`** — `jobs.recipe_id` + ตาราง **`job_routes`** (snapshot route ต่องาน · GMP: route เปลี่ยนภายหลังไม่กระทบงานเก่า) +
  ยกเครื่อง `create_job_with_order` (auto เลือกสูตร `is_active` + copy `product_routes` → `job_routes`)
- **`0032`** — `inprocess_checks.station_id` (FK stations · **คง enum `station` เดิมไว้** ให้ dashboard ไม่พัง) +
  ยกเครื่อง `add_inprocess_check` (รับ `p_station_id` → set station_id + enum group) + helper **`inprocess_route_complete()`** +
  **GATE ใน `advance_job_status` ที่ `in_production→qc`** (ต้องตรวจ QC ผ่านครบทุกสถานีในสูตร · งานไม่มี route = ไม่บล็อก)
- **แอป:** `getJobRoute` (`lib/data/stations.ts`) · `station_id` ใน checks (`quality-checks.ts`) ·
  ฟอร์ม in-process เลือกสถานี**จาก route ของงาน** + แถบ **"ความคืบหน้า QC ตามสูตร"** (n/n) ในหน้างาน
- verify REST ✅: job_routes/recipe_id/station_id (200) · 4 RPC guard ทำงาน · `inprocess_route_complete` = true (งานไม่มี route)

### ✅ ก้อน 6 — F1: ระบบขออนุมัติแก้ไขย้อนหลัง (migration 0033) — verified DB + deployed
- **`0033`** — enum + ตาราง **`edit_requests`** + RPC **`request_edit`** (whitelist ฟิลด์ต่อชนิด · กันคำขอค้างซ้ำ · แจ้ง manager +qa ถ้าเป็น QC) /
  **`review_edit_request`** (approve → **UPDATE ตารางจริง** → `log_audit` trigger เก็บ old→new อัตโนมัติ · manager เสมอ · qa เฉพาะ inprocess_check) /
  **`cancel_edit_request`** · ครอบ 3 ชนิด: production_records · material_requisitions · inprocess_checks
- **แอป:** `lib/data/edit-requests(+constants).ts` · ปุ่ม **"✏️ ขอแก้ไข"** ต่อแถว (ผลผลิต/ใบเบิก/QC) + badge "⏳ รออนุมัติแก้ไข" ·
  หน้า **`/edit-requests`** (diff เดิม→ใหม่ + อนุมัติ/ปฏิเสธ) + เมนู + badge nav (สีเหลือง) · ประวัติแก้ไขในหน้างาน · inbox รองรับ kind ใหม่
- verify REST ✅: ตาราง edit_requests (200) · 3 RPC guard ทำงาน

### ▶️ ครั้งหน้าเริ่มตรงนี้
- **เหลือผู้ใช้ทดสอบ UI จริง ทั้ง 2 ก้อน:**
  - E2: สร้างงานยาที่ตั้ง route แล้ว → qc บันทึก in-process ทีละสถานี → **กดส่ง QC ก่อนครบ = ถูกบล็อก** · ครบ = ผ่าน · งานเก่าไม่มี route = ส่งได้ปกติ
  - F1: ผลิตกด "ขอแก้ไข" ผลผลิต → manager อนุมัติที่ `/edit-requests` → ค่าจริงเปลี่ยน + audit old→new · qc ขอแก้ QC → **qa อนุมัติได้**
- migration ล่าสุด = **0033** (0031–0033 paste + verify REST ครบ)
- งานใหญ่ที่ค้างใน backlog หมดแล้ว → ถัดไปเลือกได้: MES-grade B5–B8 · หรือ UAT กับทีม

---

## 📅 บันทึกวันที่ 1 กรกฎาคม 2569 — ยกระดับคุณภาพจากรายงานผู้ทดสอบ (ก้อน 1–4)

### ที่มา
น้องฝึกงานทดสอบแอปแล้วสรุปบั๊กลง Notion หน้า `production_qc_warehouse_bug_fix`
(`38f92ef2-c18f-801a-9454-d45feb34e291`) + หน้าแม่ `QA review By Programmer`
(`38e92ef2-c18f-80bb-ab96-c7131d54a597`) รวม ~19 ประเด็น
→ ส่ง Explore agent 3 ตัวตรวจโค้ดจริงก่อน (อย่าเชื่อทั้งหมด) = **บั๊กจริง 15 · ไม่ใช่บั๊ก 3 · เลื่อนเฟสหลัง 2 งานใหญ่**
> แผนเต็ม: `.claude/plans/web-app-sharded-hoare.md`

### ✅ ทำเสร็จ + verified ทุกก้อน (build ผ่านทุกครั้ง · migration paste + verify REST ครบ)
- **ก้อน 1 (frontend ล้วน ไม่มี migration):**
  - A1 login คงอีเมลเมื่อรหัสผิด (React 19 auto-reset ฟอร์ม → `auth.ts` return email + `login-form.tsx` defaultValue)
  - A2 timezone ล็อก Asia/Bangkok — util กลางใหม่ `web/lib/format.ts` (`fmtDateTime`/`fmtDate`) แทน `new Date().toLocaleString` ทุกจุด**ที่เป็นวันที่** (ไม่แตะตัวเลข)
  - A3 โชว์ note ประวัติผลผลิต · A4 โชว์ note ใบเบิก · A5 โชว์ note เครื่องจักรใน list (ทุก role) · A6 seq สถานีเพิ่มทีละ 1
- **ก้อน 2 (migration 0027–0028):**
  - B1 ซ่อนงาน FG ที่รับเข้าคลังแล้วจากบอร์ด — `getJobs` เติมธง `fg_received` จาก fg_inventory · KPI "เข้าคลังแล้ว" นับ received จริง · board subscribe fg_inventory (ไม่มี migration)
  - B2 `add_qa_sample` guard สถานะ = qa เท่านั้น (`0027`)
  - B3 `add_production_record` guard สถานะ = **in_production เท่านั้น** + `RECORDABLE_STATUSES` (`0028`) — ผู้ใช้เลือก "กำลังผลิตเท่านั้น"
- **ก้อน 3 (migration 0029):**
  - C1 `advance_job_status` แจ้ง role ปลายทาง "📥 งานมาถึงคุณ" (→production/qc/qa/warehouse ตอน forward)
  - auto-hide noti หมดหน้าที่ ผ่านคอลัมน์ `notifications.relevant_status` (ซ่อนเมื่องานเลื่อนพ้นสถานะนั้น / FG รับเข้าคลังแล้ว) — sync ทั้ง RPC `unread_notification_count` + `getInbox` (app)
- **ก้อน 4 (migration 0030):**
  - D1 ตาราง `deviation_comments` **append-only** แยกหมายเหตุตามฝ่าย (RPC `add_deviation_comment` + `current_role_group` tag ฝ่ายจาก session กันปลอม) — ไม่มีใครแก้ทับกันได้
  - D2 `submit_deviation_resolution` ปุ่ม "✅ ส่งกลับให้ QA ตรวจสอบ" + คอลัมน์ `resolution_*` + แจ้ง qa/manager + badge "🔄 รอ QA ตรวจสอบ"

### ❌ ไม่แก้ (รายงานคลาดเคลื่อน)
N1 ปุ่ม logout จมมือถือ (app-shell pin ปุ่มล่างสุดถูกแล้ว reproduce ไม่ได้) · N2 sidebar เลื่อนตามเนื้อหา (แยกโครงอยู่แล้ว) · N3 QC ตรวจ lot "รอตรวจ QC" (ระบบไม่มีสถานะนี้ มีแค่ `qc`)

### ▶️ ครั้งหน้าเริ่มตรงนี้
- **เหลือผู้ใช้ทดสอบ UI จริง** ทุกก้อน (login แต่ละ role): B1 รับเข้าคลัง→งานหายจากบอร์ด · B3 บันทึกผลผลิตได้เฉพาะกำลังผลิต · C1 กระดิ่งแจ้ง role ถัดไป + หายเมื่อเลื่อน · D1/D2 หมายเหตุแยกฝ่าย + ส่งกลับ QA
- **งานใหญ่ที่เลื่อนไว้ (เฟสหลัง):** E1/E2 ผูกสูตร/BOM กับงาน + บังคับ flow ตาม route + gate in-process QC ครบทุกสถานีก่อนส่ง QC · F1 ระบบขออนุญาตแก้ไขย้อนหลัง (amendment: record/requisition/QC)
- migration ล่าสุด = **0030** (0027–0030 paste + verify REST ครบแล้ว)
> ⚠️ B2 ตั้ง QA sample = สถานะ qa เท่านั้นตามรายงาน — ถ้าทีมอยากเก็บตอน qc ด้วย ปรับ guard บรรทัดเดียว

---

## 📅 บันทึกวันที่ 24 มิถุนายน 2569 — เฟส 12 / D12 (ก้อน 4): B4 Notification (in-app inbox)

### ✅ วันนี้ทำอะไรไปบ้าง — กล่องแจ้งเตือนในแอป 🔔
> fetch Notion ก่อนเริ่ม (demo-feature-suggestions = 10 ฟีเจอร์เดิม ครบ ไม่มีของใหม่)
> **Decisions ผู้ใช้:** triggers ครบ 4 (reject · deviation major/critical · overdue · stuck) · ส่ง**ตาม role ที่เกี่ยว**
1. **DB (`web/supabase/migrations/0026_notifications.sql`)** — ⏳ รอ paste:
   - `notifications` (kind/title/body/job_id/job_no/`target_role`[null=ทุกคน]) + `notification_reads` (อ่านแล้ว ต่อคน, pk(notif,profile))
   - RLS: เห็นเฉพาะ `target_role is null or has_role(target_role)` · reads = ของตัวเอง
   - helper `create_notification(...)` · RPC `mark_notification_read`/`mark_all_notifications_read`/`unread_notification_count` (สำหรับกระดิ่ง)
   - **ยกเครื่อง 2 ฟังก์ชัน (event-driven):** `advance_job_status` → ตีกลับ = แจ้ง `production` · `open_deviation` → major/critical = แจ้ง `qa`+`manager` (คง gate/logic เดิมทั้งหมด)
   - realtime: notifications + notification_reads
2. **แอป (`web/`)** — build ผ่าน:
   - `lib/data/notification-constants.ts` (InboxItem/KIND_META/STUCK_DAYS — ไม่มี server import) · `lib/data/notifications.ts` (`getInbox`/`getUnreadCount` + **derived overdue/stuck คำนวณสด** เฉพาะ production/manager)
   - หน้า `/inbox` (`page.tsx`+`inbox-view.tsx`+`actions.ts`) — รายการ stored+derived · ปุ่มอ่าน/อ่านทั้งหมด · realtime
   - **กระดิ่ง:** เมนู "🔔 แจ้งเตือน" ใน nav + **badge เลขยังไม่อ่าน** (layout ส่ง unreadCount → app-shell)
3. push แล้ว · ตรวจ deploy ด้วย `vercel ls` (กัน gotcha push หลายก้อนข้าม deploy)

### ✅ verified DB แล้ว (24 มิ.ย. 69)
- ผู้ใช้ paste `0026_notifications.sql` แล้ว · Claude เช็กผ่าน REST: ตาราง `notifications`+`notification_reads` มีจริง (200) ·
  RPC `mark_notification_read`+`mark_all_notifications_read` guard ทำงาน · `unread_notification_count` ตอบ 200 (0) ✅

### ⚠️ เหลือผู้ใช้ทดสอบ UI จริง
- QA/QC ตีกลับงาน → login ฝ่ายผลิตเห็นกระดิ่งเด้ง + /inbox มีรายการ · เปิด deviation major → QA/manager เห็น · งานเลย planned_end → เห็น "เกินกำหนด"

### ▶️ ขั้นถัดไป (เลือกได้ — ปิด D12 จริงเมื่อ verify B4)
- MES-grade ที่เหลือ: **B5 Barcode/QR · B6 OEE/downtime · B7 capacity · B8 integration** (ดู Notion Roadmap)
> ก่อนเริ่มเฟสใหม่: fetch Notion เช็ก requirement ล่าสุด

---

## 📅 บันทึกวันที่ 24 มิถุนายน 2569 — เฟส 12 / D12 (ก้อน 3): B1 eBR — ปิด D12 (ส่วน B1/B2/B3)

### ✅ วันนี้ทำอะไรไปบ้าง — แฟ้มบันทึกการผลิตรวมของล็อต + พิมพ์ได้ 📄🖨️
> ก้อน 3 เป็น **read view ล้วน — ไม่มี SQL ให้ paste** (รวมข้อมูลที่มีอยู่ทั้งหมดของงานหนึ่ง)
1. **`lib/data/ebr.ts`** — `getBatchRecord(jobNo)` รวมจาก data layer เดิม (Promise.all): ข้อมูลงาน + line clearance + วัตถุดิบที่เบิก + เครื่องที่ใช้ + บันทึกผลผลิต + in-process QC + QA sample + deviation + ลายเซ็น QC/QA + FG ที่รับเข้า
2. **หน้า `/board/[jobNo]/ebr`** (`ebr/page.tsx`) — เอกสารแฟ้มเดียว 10 ส่วนเรียงตามลำดับ GMP · `ebr/print-button.tsx` (client `window.print()`)
3. **print CSS** ใน `globals.css` (`@media print`): ใช้ visibility trick ซ่อน app chrome เหลือเฉพาะ `#ebr` (ไม่ขึ้นกับโครง layout) + `.no-print` + `@page margin`
4. **ปุ่มลิงก์ "📄 ดู eBR"** ในหน้า job detail (`board/[jobNo]/page.tsx`) · build ผ่าน · push แล้ว
> = **ปิด D12 ครบ 3 ก้อน** (B3 deviation · B2 traceability · B1 eBR) 🎉

### ⚠️ เจอ gotcha deploy (24 มิ.ย. 69)
- **push หลาย commit ติดๆ กัน → Vercel อาจ "ข้าม" commit สุดท้าย** (eBR fdbca1f ไม่ trigger deploy → หน้า ebr 404 บนเว็บจริง)
- แก้: `git commit --allow-empty -m "trigger" && git push` เพื่อบังคับ deploy ใหม่ · ตรวจด้วย `vercel ls pd-monitor --yes` (CLI authed อยู่)
- **ครั้งหน้า: ถ้า push หลายก้อนรวด ให้เช็ก `vercel ls` ว่า deploy ตรง commit ล่าสุดจริง**

### ▶️ ขั้นถัดไป (เลือกได้)
- **B4 Notification** (in-app inbox: เตือนงานใกล้เกินกำหนด/ค้าง/ตีกลับ/deviation) — อยู่ใน roadmap คู่กับ B3
- หรือ MES-grade อื่น: B5 Barcode/QR · B6 OEE/downtime · B7 capacity · B8 integration (ดู Notion Roadmap)
> ก่อนเริ่มเฟสใหม่: fetch Notion เช็ก requirement ล่าสุด

---

## 📅 บันทึกวันที่ 24 มิถุนายน 2569 — เฟส 12 / D12 (ก้อน 2): B2 Lot Genealogy / Traceability

### ✅ วันนี้ทำอะไรไปบ้าง — หน้าไล่ย้อนสายโซ่ล็อต 🔗🔍
> ก้อน 2 เป็น **read view ล้วน — ไม่มี SQL ให้ paste** (สายข้อมูล lot มีครบแล้ว: material_lots → material_requisitions → jobs → batches/fg_inventory)
1. **`lib/data/genealogy.ts`** — รวม query ข้ามตาราง:
   - `getJobTrace(jobId)` = ผังของงานเดียว (วัตถุดิบที่เบิก RM/PM lot + FG lot ที่ออก + วันผลิต/หมดอายุ + จำนวน deviation/เปิดค้าง)
   - `searchTrace(q)` = ค้นด้วยเลขงาน/เลขล็อต → **ขาไป** (job/FG lot → วัตถุดิบที่ใช้) + **ขาย้อน** (RM lot → งานที่เบิกใช้ล็อตนั้น เผื่อ recall)
2. **หน้า `/trace`** (`app/(app)/trace/page.tsx`) — ช่องค้นหา (GET) → การ์ดผังสายโซ่ "วัตถุดิบ → JOB → FG" คลิกเข้างาน/ดู eBR ได้ · realtime
3. **เมนู** `lib/nav.ts` เพิ่ม "ไล่ย้อนล็อต (Trace)" (roles: qa/warehouse/manager) · build ผ่าน · push แล้ว
> ⚠️ ในการ์ดมีลิงก์ "ดู eBR →" ชี้ `/board/[jobNo]/ebr` ซึ่งทำในก้อน 3 (B1) — จะ 404 จนกว่าจะทำ B1 เสร็จ

### ▶️ ขั้นถัดไป (D12 ก้อน 3)
- **B1 eBR** — หน้า view รวมแฟ้มการผลิตของ lot หน้าเดียว + ปุ่มพิมพ์ (print CSS)

---

## 📅 บันทึกวันที่ 24 มิถุนายน 2569 — เฟส 12 / D12 (ก้อน 1): B3 Deviation / Incident

### ✅ วันนี้ทำอะไรไปบ้าง — ระบบบันทึกเหตุผิดปกติ + gate กัน QA→FG ⚠️
> เริ่ม D12 (MES-grade) · fetch Notion Roadmap (B1–B8) + recommendations ก่อนเริ่ม · ทำทีละก้อน B3 → B2 → B1
> **Decisions (ผู้ใช้ยืนยัน):** (1) deviation เปิดค้าง = บล็อกเฉพาะ QA→FG (2) เปิด: ผลิต/QC/QA · ปิด: QA/ผู้บริหาร (3) eBR = หน้า view + ปุ่มพิมพ์
1. **DB (`web/supabase/migrations/0025_deviations.sql`)** — ⏳ รอ paste:
   - enum `deviation_severity` (minor/major/critical) · `deviation_status` (open/investigating/closed)
   - ตาราง `deviations` (ผูก job · machine(null) · `inprocess_check_id`(null เชื่อมผล in-process fail) ·
     title/description/dev_type/severity/status/reported_by/assigned_to/due_date/root_cause/capa/closed_by/closed_at) + ALCOA+audit+RLS+realtime
   - RPC `open_deviation` (production/qc/qa/manager) · `update_deviation` (**ปิด=closed เฉพาะ qa/manager + ต้องมี root_cause+capa**)
   - helper `has_open_deviation(job)` · **ยกเครื่อง `advance_job_status` เพิ่ม GATE: qa→finished_goods ถ้ามี deviation เปิดค้าง = บล็อก** (คง gate line clearance เดิม)
2. **แอป (`web/`)** — build ผ่าน (Next 16/Turbopack):
   - `lib/data/deviation-constants.ts` (severity/status/type + canOpen/canClose) · `lib/data/deviations.ts` (`getDeviationsByJob`)
   - `board/[jobNo]/deviation-actions.ts` (`openDeviation`/`updateDeviation`) · `board/[jobNo]/deviations.tsx` (ส่วน "⚠️ Deviation" + ฟอร์มเปิด/ปิด + quick-open จากผล in-process ที่ "ไม่ผ่าน")
   - ฝังใน `board/[jobNo]/page.tsx` (fetch + failChecks ที่ยังไม่ผูก deviation + realtime `deviations`)
3. push แล้ว → Vercel auto-deploy

### ✅ verified DB แล้ว (24 มิ.ย. 69)
- ผู้ใช้ paste `0025_deviations.sql` แล้ว · Claude เช็กผ่าน REST: ตาราง `deviations` มีจริง (200) ·
  RPC `open_deviation`+`update_deviation` guard ทำงาน ("ยังไม่ได้เข้าสู่ระบบ") · helper `has_open_deviation` ตอบ 200 (false) ✅
- ⚠️ หมายเหตุ: `SUPABASE_SECRET_KEY` ใน `.env.local` ในเครื่อง **ใช้ไม่ได้แล้ว (401 — ถูก rotate)** → verify ทำผ่าน publishable key แทน
  (ฝั่ง Vercel น่าจะยังมี key ที่ถูก เพราะแอปใช้งานได้ · ถ้าจะรันแอด local ต้องอัปเดต secret key ใหม่จาก Supabase dashboard)

### ⚠️ เหลือผู้ใช้ทดสอบ UI จริง
- ทดสอบ UI: production/qc/qa เปิด deviation บนงาน → QA กด QA→FG **ต้องถูกบล็อก** "มี deviation เปิดค้าง" →
  QA กด "อัปเดต/ปิด" ใส่ root cause+CAPA เลือก closed → QA→FG ผ่าน · ผล in-process "ไม่ผ่าน" → ปุ่ม "เปิด deviation" ด่วน ผูก check ถูก

### ▶️ ขั้นถัดไป (D12 — หลัง verify B3)
- **B2 Lot Genealogy** (ก้อน 2): หน้า `/trace` ไล่ย้อนสายโซ่ RM lot → งาน → FG lot (read view เป็นหลัก สายข้อมูลมีอยู่แล้ว)
- **B1 eBR** (ก้อน 3): หน้า view รวมแฟ้มการผลิตของ lot + ปุ่มพิมพ์ (print CSS)
> แผนเต็ม: `~/.claude/plans/1-b3-deviation-declarative-otter.md`

---

## 📅 บันทึกวันที่ 24 มิถุนายน 2569 — เฟส 11 / D11 (A6 ก้อน 2): in-process QC + QA sample — ปิด A6

### ✅ วันนี้ทำอะไรไปบ้าง — ตรวจคุณภาพระหว่างผลิต + จุดเก็บตัวอย่าง QA 🔬🧫
> ปิดส่วนสุดท้ายของ A6 · ฟอร์มอยู่ในหน้า job detail (board/[jobNo]) · ⚠️ in-process "ไม่ผ่าน" จะเชื่อม deviation (B3) ภายหลัง
1. **DB (`web/supabase/migrations/0024_quality_checks.sql`)** — ⏳ รอ paste:
   - `inprocess_checks` (QC ระหว่างผลิต: station[enum 4 กลุ่ม]/param/value/unit/`result`[pass-fail]/checked_by/checked_at)
   - `qa_samples` (จุด/รอบเก็บตัวอย่าง: sample_point/qty/unit/collected_by/collected_at)
   - enum `check_result` (pass/fail) · meta+audit+RLS+realtime ทั้ง 2 ตาราง
   - RPC `add_inprocess_check` (qc/manager · งานต้องอยู่ in_production/qc/qa) + `add_qa_sample` (qa/manager)
     · เขียนชื่อผู้ตรวจ/ผู้เก็บอัตโนมัติจาก session (ALCOA)
2. **แอป `board/[jobNo]`** — build ผ่าน:
   - ส่วน "ตรวจระหว่างผลิต (In-process QC)" — ตาราง + ฟอร์ม (เห็น/เพิ่มได้เฉพาะ qc/manager) แสดงผ่าน/ไม่ผ่าน
   - ส่วน "จุดเก็บตัวอย่าง (QA Sample)" — ตาราง + ฟอร์ม (เฉพาะ qa/manager)
   - `lib/data/quality-checks.ts` + `quality-actions.ts` + `quality-checks.tsx` · realtime 2 ตารางใหม่
3. push แล้ว → **= ปิด A6 + ปิดส่วน A ของ D11 ทั้งหมด** (A4·A6 เสร็จ) 🎉

### ✅ verified DB แล้ว (24 มิ.ย. 69)
- ผู้ใช้ paste `0024_quality_checks.sql` แล้ว · Claude เช็กผ่าน REST: ตาราง `inprocess_checks`+`qa_samples` มีจริง (200)
  · ฟังก์ชัน `add_inprocess_check`+`add_qa_sample` มีจริง + guard ทำงาน ✅
- เหลือผู้ใช้ทดสอบ UI: qc → งานกำลังผลิต/QC → "ตรวจระหว่างผลิต" บันทึกผล · qa → "จุดเก็บตัวอย่าง" บันทึกจุด

### ▶️ ขั้นถัดไป (ปิดท้าย D11)
- **B3 Deviation** — เปิดเหตุผิดปกติ (ประเภท/ความรุนแรง/ผู้รับผิดชอบ/กำหนดปิด) ผูก job/lot/เครื่อง · เชื่อม in-process fail
- **B4 Notification** — แจ้งเตือนงานใกล้เกินกำหนด/ค้างนาน/ถูกตีกลับ/deviation (เริ่ม in-app inbox)
> ก่อนเริ่ม B3/B4: fetch Notion เช็ก requirement ล่าสุด

---

## 📅 บันทึกวันที่ 24 มิถุนายน 2569 — เฟส 11 / D11 (A6 ก้อน 1): คลัง FG

### ✅ วันนี้ทำอะไรไปบ้าง — รับสินค้าสำเร็จรูปเข้าคลัง 📦🏬
> fetch Notion ก่อนเริ่ม (demo-feature-suggestions = 10 ฟีเจอร์เดิม ครบแล้ว · ไม่มีของใหม่) · ใช้ดีไซน์ A6 จาก Roadmap
> เริ่ม A6 · แบ่ง 2 ก้อน → ก้อน 1 = คลัง FG (เติมเมนู /warehouse placeholder) · ก้อน 2 = in-process QC + QA samples
1. **DB (`web/supabase/migrations/0023_fg_inventory.sql`)** — ⏳ รอ paste:
   - `fg_inventory` (สต็อก FG ต่อ 1 งาน · `unique(job_id)` · qty/unit/lot/location/received_date) · meta+audit+RLS+realtime
   - RPC `receive_fg(job, qty, ...)` (warehouse/manager · **upsert** ต่องาน · ดึง product/lot/unit จากงานอัตโนมัติ
     · validate งานต้องถึง `finished_goods` ก่อน · กันจำนวนติดลบ) · `can_manage_fg()` = warehouse/manager
2. **แอป:** หน้า `/warehouse` (page + warehouse-view + actions) — การ์ดต่อหนึ่งงาน FG + สรุป (งาน FG/รอรับเข้า/ยอดรวม)
   · ปุ่ม "รับเข้าคลัง"/"แก้คลัง" (จำนวน/หน่วย/ตำแหน่ง/ล็อต/หมายเหตุ) · `lib/data/fg.ts` (listFgJobs) · realtime jobs+fg_inventory
   · nav: เปิดเมนู "คลัง / FG" (ready=true) · build ผ่าน · push แล้ว

### ✅ verified DB แล้ว (24 มิ.ย. 69)
- ผู้ใช้ paste `0023_fg_inventory.sql` แล้ว · Claude เช็กผ่าน REST: ตาราง `fg_inventory` มีจริง (200)
  · ฟังก์ชัน `receive_fg` มีจริง + guard ทำงาน · มีงาน finished_goods 2 งาน (JOB-002, JOB-004) ให้ทดสอบรับเข้าได้เลย ✅
- เหลือผู้ใช้ทดสอบ UI: warehouse/manager → "คลัง / FG" → กด "รับเข้าคลัง" ใส่จำนวน+ตำแหน่ง → บันทึก → การ์ดขึ้น "รับเข้าคลังแล้ว"

### ▶️ ขั้นถัดไป (A6 ก้อน 2 + ปิด D11)
- **A6 ก้อน 2:** in-process QC (`inprocess_checks`: job/station/param/value/pass-fail/checked_by) + จุดเก็บ sample QA (`qa_samples`)
  — ฟอร์มในหน้า job detail · ⚠️ in-process fail ควรเชื่อม deviation (B3) ภายหลัง
- จากนั้น B3 deviation · B4 notification (in-app inbox) → ปิด D11

---

## 📅 บันทึกวันที่ 24 มิถุนายน 2569 — เฟส 11 / D11 (A4 ก้อน 3): สถานีย่อยจริง + route — ปิด A4

### ✅ วันนี้ทำอะไรไปบ้าง — กระบวนการผลิตจริงหลายสถานี + ลำดับต่อยา 🛠️
> fetch Notion ก่อนเริ่ม · ⚠️ Notion เตือนชัด "อย่าแตะ enum production_station เดิม (prod_records+dashboard พัง) → ทำตาราง config"
> ทำตามเป๊ะ: ไม่แตะ enum · ใช้ตาราง stations + station_group map เข้า 4 กลุ่มเดิม
1. **DB (`web/supabase/migrations/0022_stations.sql`)** — ⏳ รอ paste:
   - `stations` (สถานีย่อย config: code/name/`station_group`[enum เดิมเป็นกลุ่ม rollup]/seq/is_active)
     + **seed 10 สถานี**: ชั่ง/เตรียม(prep) · บด/ร่อน/ผสมแห้ง/ผสมเปียก(mixing) · ตอก/แคปซูล/ฟิล์ม/คัด-ขัด(tableting) · บรรจุ(packing)
   - `product_routes` (ยา→ลำดับสถานี `step_no` · `unique(product_id,station_id)`) · meta+audit+RLS+realtime
   - RPC `upsert_station` + `set_product_route(product, jsonb)` (manager · แทนที่ route ทั้งชุด atomic + กันสถานีซ้ำ/ไม่มีจริง)
2. **แอป `/recipes`** — build ผ่าน:
   - แผง "⚙️ ตั้งค่าสถานี (master)" ด้านบน (manager) — ตาราง+เพิ่ม/แก้สถานี (เลือกกลุ่มหลัก rollup)
   - ส่วน "🛠️ ขั้นตอนการผลิต (Route)" ในการ์ดยา — แสดงเป็น chain (1.→2.→3.) สีตามกลุ่ม · แก้ได้: เลือกสถานี + เลื่อนลำดับ ▲▼ + เพิ่ม/ลบ
   - `lib/data/stations.ts` (`listStations`) + ต่อ route เข้า `listProductsWithRecipes`
3. push แล้ว → **= ปิด A4 ครบ 3 ก้อน** (สูตร/BOM · บรรจุ · สถานีย่อย) 🎉

### ✅ verified DB แล้ว (24 มิ.ย. 69)
- ผู้ใช้ paste `0022_stations.sql` แล้ว · Claude เช็กผ่าน REST: `stations` seed ครบ 10 สถานี (map กลุ่มถูก)
  · `product_routes` มีจริง (200) · ฟังก์ชัน `upsert_station`+`set_product_route` มีจริง + guard ทำงาน ✅
- เหลือผู้ใช้ทดสอบ UI: manager → /recipes → แผง "ตั้งค่าสถานี" เห็น 10 สถานี → การ์ดยา "แก้ขั้นตอน" เรียงลำดับ ▲▼ → บันทึก

### ▶️ ขั้นถัดไป (D11 ต่อ)
- **A6** คลัง FG + in-process QC + จุดเก็บ sample (QA) — `fg_inventory` · `inprocess_checks` · `qa_samples`
- จากนั้น B3 deviation · B4 notification (in-app inbox)
> ก่อนเริ่ม A6: fetch Notion เช็ก requirement ล่าสุด

---

## 📅 บันทึกวันที่ 24 มิถุนายน 2569 — เฟส 11 / D11 (A4 ก้อน 2): รูปแบบบรรจุ

### ✅ วันนี้ทำอะไรไปบ้าง — ระบุรูปแบบบรรจุของยาแต่ละตัว 📦
> fetch Notion (หน้า Roadmap) ก่อนเริ่ม · A4 เหลือ 2 ส่วน → แยกเป็นก้อน 2 (บรรจุ · เล็ก/ปลอดภัย) + ก้อน 3 (สถานีย่อย/route · ใหญ่)
> ⚠️ Notion เตือน: "อย่าเปลี่ยน enum production_station — production_records เดิมจะพัง · ทำตาราง config (stations + product_routes) ปลอดภัยกว่า" → เก็บไว้ทำก้อน 3
1. **DB (`web/supabase/migrations/0021_packaging.sql`)** — ⏳ รอ paste:
   - เพิ่มคอลัมน์ `products.pack_type` (Blister/Strip/ซอง/ขวด/กระปุก/อื่นๆ) + `pack_pattern` (เช่น "แผง 50×10's")
   - RPC `update_product_packaging(product, type, pattern)` (manager/admin · audit ผ่าน trigger เดิมของ products)
2. **แอป:** หน้า `/recipes` แสดงรูปแบบบรรจุบนการ์ดยา + ปุ่ม "แก้บรรจุ" (เลือก type + กรอกรายละเอียดแผง)
   · `lib/data/packaging-constants.ts` (ตัวเลือก pack type) · build ผ่าน · push แล้ว

### ✅ verified DB แล้ว (24 มิ.ย. 69)
- ผู้ใช้ paste `0021_packaging.sql` แล้ว · Claude เช็กผ่าน REST: คอลัมน์ `pack_type`+`pack_pattern` มีจริง (200)
  · ฟังก์ชัน `update_product_packaging` มีจริง + guard ทำงาน · ผู้ใช้ทดสอบ UI ผ่าน ✅

### ▶️ ขั้นถัดไป (D11 ต่อ)
- **A4 ก้อน 3:** สถานีย่อยจริง — ตาราง `stations` (config: บด/ร่อน/ผสมแห้ง/ฉาบ/ฟิล์ม/คัด-ขัด/บรรจุ) + `product_routes` (ยา→ลำดับสถานี)
  ⚠️ ใช้ตาราง config ไม่แตะ enum เดิม (Notion เตือน) · map สถานีย่อย→กลุ่ม 4 enum เดิมเพื่อให้ dashboard ไม่พัง
- จากนั้น **A6** คลัง FG + in-process QC + sample · (+ B3 deviation · B4 notification)

---

## 📅 บันทึกวันที่ 24 มิถุนายน 2569 — เฟส 11 / D11 (A4 ก้อน 1): สูตรการผลิต / BOM

### ✅ วันนี้ทำอะไรไปบ้าง — สูตรยา + รายการวัตถุดิบที่ใช้ (Bill of Materials) 🧪📋
> เริ่ม D11 · ผู้ใช้เลือกทำ A4 ก่อน · แบ่ง A4 เป็น 2 ก้อน → **ก้อน 1 = Recipe/BOM** (ฐาน ต่อกับ A2 วัตถุดิบ)
> หมายเหตุ: ยังไม่ได้ fetch Notion รอบนี้ (A4 อยู่ใน Roadmap D11 ที่โพสต์ไว้แล้ว) — รอบหน้าก่อนก้อนใหม่ค่อยเช็ก
1. **DB (`web/supabase/migrations/0020_recipes.sql`)** — ⏳ รอ paste:
   - `product_recipes` (หัวสูตร: ผูก product · ชื่อ/เวอร์ชัน · `batch_size`+`batch_unit` · is_active · note)
   - `recipe_items` (BOM: ผูก `materials` ของ A2 · qty/แบตช์ · unit · note · `unique(recipe_id, material_id)` กันซ้ำ)
   - meta+audit trigger + RLS (อ่านทุกคน authenticated · เขียนผ่าน RPC) + realtime ทั้ง 2 ตาราง
   - RPC security definer (เฉพาะ manager/admin ผ่าน `can_manage_recipes()`):
     · `upsert_recipe(...)` เพิ่ม/แก้หัวสูตร · `set_recipe_items(recipe, jsonb)` **แทนที่ BOM ทั้งชุด atomic**
       (validate ใน DB: วัตถุดิบมีจริง · qty ไม่ว่าง/ไม่ติดลบ · กันวัตถุดิบซ้ำในสูตร)
2. **แอป (`web/`)** — build ผ่าน:
   - `lib/data/recipes.ts` — `listProductsWithRecipes()` (join product→recipes→items→material) + `getMaterialOptions()`
   - หน้า `app/(app)/recipes/` — `page.tsx` (guard อ่านทุก role, จัดการเฉพาะ manager) + `recipes-view.tsx` + `actions.ts`
     · การ์ดต่อยา → เพิ่ม/แก้หัวสูตร · **BomEditor** เลือกวัตถุดิบจาก dropdown + qty/หน่วย/หมายเหตุ (เพิ่ม/ลบแถว) บันทึกทีเดียว
   - `lib/nav.ts` — เมนูใหม่ "สูตรการผลิต / BOM" (ready=true)
   - realtime: `product_recipes` + `recipe_items`
3. commit + push แล้ว → Vercel auto-deploy

### ✅ verified DB แล้ว (24 มิ.ย. 69)
- ผู้ใช้ paste `0020_recipes.sql` แล้ว · Claude เช็กผ่าน REST: ตาราง `product_recipes`+`recipe_items` มีจริง (200)
  · ฟังก์ชัน `upsert_recipe`+`set_recipe_items` มีจริง + guard ทำงาน (ตอบ "ยังไม่ได้เข้าสู่ระบบ" เมื่อไม่มี session) ✅

### ⚠️ เหลือผู้ใช้ทดสอบ UI จริง (RPC ต้องมี session — เทสผ่าน REST ไม่ได้)
- ทดสอบ: login เป็น **manager** → /recipes → เลือกยา → "＋ เพิ่มสูตร" (ตั้งขนาดแบตช์) →
  "แก้รายการวัตถุดิบ" เลือกวัตถุดิบจากคลัง A2 + จำนวน → บันทึก → เห็น BOM ในตาราง · login role อื่น = เห็นแต่ดูอย่างเดียว

### ▶️ ขั้นถัดไป (D11 ต่อ)
- **A4 ก้อน 2:** สถานีย่อยจริง (บด/ร่อน/ผสมแห้ง/ฉาบ/ฟิล์ม/คัด-ขัด) + รูปแบบบรรจุ (Blister/Strip/ซอง/ขวด)
- จากนั้น **A6** คลัง FG + in-process QC + จุดเก็บ sample (QA) · (+ B3 deviation · B4 notification ตามลำดับ)
> ก่อนเริ่มก้อนใหม่: fetch Notion เช็ก requirement ล่าสุด

---

## 📅 บันทึกวันที่ 23 มิถุนายน 2569 — เฟส 10 / D10 เสร็จครบ (A0·A1·A2·A3·A5)

### ✅ D10 = เติม brief CEO รอบแรก — เสร็จครบ 5 ก้อน 🎉 (migration 0012–0019 paste+verify หมดแล้ว)
> ทุกก้อน: เขียนผ่าน RPC security definer + RLS + audit + realtime ตามแพตเทิร์นเดิม · build ผ่าน · paste + verify REST + ผู้ใช้ทดสอบ UI ผ่าน · push ขึ้น Vercel แล้ว
- **A0 จัดการผู้ใช้ + role admin** (0012–0013) — หน้า `/admin/users` · admin = has_role ผ่านทุก role · `lib/auth/roles.ts`
- **A1 เครื่องจักร** (3 ก้อน · 0014–0015) — หน้า `/machines` (ทะเบียน+สถานะ+เตือนซ่อม/สอบเทียบ) · เลือกเครื่องตอนบันทึกผลผลิต (กันเครื่องซ่อม) · รายงานการใช้เครื่อง (`machine-usage.ts`)
- **A2 วัตถุดิบ/คลัง + เบิก** (3 ก้อน · 0016–0017) — หน้า `/materials` (`materials`+`material_lots`) · ใบเบิก `material_requisitions` ในหน้างาน (request→issue ตัดสต็อก atomic, กันไม่ผ่าน/หมดอายุ/ไม่พอ) · แถบเตือนงานยังไม่เบิก
- **A3 Line Clearance** (0018) — ตาราง `line_clearances` + สองลายเซ็น (perform≠check) · **GATE ใน `advance_job_status`: มีแผน→กำลังผลิต ต้องผ่าน clearance ก่อน**
- **A5 จำนวนคน + auto เลขงาน** (0019) — `headcount` ใน production_records (ค่าแรง=คน-ชม.×อัตรา ในแดชบอร์ด) · sequence `job_no_seq` ออก `JOB-YYYY-NNNN` ถ้าเว้นว่าง
- เมนูใหม่: จัดการผู้ใช้ · เครื่องจักร · วัตถุดิบ/คลัง · commit ล่าสุด `df1fdb1`

### ▶️ ขั้นถัดไป — D11 (รอบ 2 ตาม Roadmap Notion)
- **A4** สูตรการผลิต (recipe/BOM) + สถานีย่อยจริง + รูปแบบบรรจุ (Blister/Strip/ซอง/ขวด)
- **A6** คลัง FG + in-process QC + จุดเก็บ sample (QA)
- (+ เริ่ม B3 deviation · B4 notification ตามลำดับ)
> หมายเหตุ go-live: A3 ทำให้งานสถานะ "มีแผนแล้ว" เดิม ต้องทำ Line Clearance ก่อนเริ่มผลิต — แจ้งทีมกันงง

---

## 📅 บันทึกวันที่ 23 มิถุนายน 2569 — เฟส 10 / D10 (ก้อน 1): หน้า Admin จัดการผู้ใช้ + role admin

### ✅ วันนี้ทำอะไรไปบ้าง — ปลดคอขวด go-live: จัดการบัญชี/สิทธิ์ในแอปเอง 👤🔑
> เริ่ม D10 ตาม Roadmap หลัง D9 (Notion) ข้อ A0 · ผู้ใช้เลือกทำ A0 ก่อน + ขอเพิ่ม role `admin` ทำได้ทุกอย่าง
1. **หน้า `/admin/users`** (เห็นเฉพาะ manager/admin) — `page.tsx` (guard) + `users-admin.tsx` (UI) + `actions.ts`:
   - สร้างบัญชี (อีเมล+รหัส+ชื่อ+แผนก+role) · แก้ role · แก้ชื่อ/แผนก · **รีเซ็ตรหัสผ่าน** · **ระงับ/เปิดบัญชี**
   - **Auth ops** (createUser/updateUserById/ban) → admin client (secret key, server-only)
   - **เขียน profiles/roles** → RPC security definer (auth.uid ทำงาน → audit เก็บ "ใครทำ")
   - สร้างบัญชี `email_confirm:true` (ล็อกอินได้ทันที) · ระงับ = ban auth ~100 ปี (บล็อกล็อกอินจริง)
   - บัญชี seed เดิม (มีอีเมล ยังไม่ผูก auth) → สร้างด้วยอีเมลเดิม trigger `handle_new_user` ผูกให้อัตโนมัติ
2. **เพิ่ม role `admin` = ทำได้ทุกอย่าง** — แก้ครั้งเดียวครอบทุกที่:
   - DB `has_role()` เพิ่มเงื่อนไข "หรือเป็น admin" → guard RPC ทุกตัว + RLS ทุก policy ผ่านอัตโนมัติ
   - แอป `lib/auth/roles.ts` (ใหม่) `hasRole`/`hasAnyRole` (admin ผ่านเสมอ) แทน `.includes("manager")` ทั่วแอป
     (nav · ปุ่มสร้างงาน · DL cost · เปลี่ยนสถานะงาน · บันทึกผลผลิต · หน้า admin)
   - กัน lockout: บัญชีตัวเองต้องคงสิทธิ์ manager หรือ admin ไว้
3. **DB (paste แล้ว):** `0012_admin_users.sql` (RPC: admin_set_roles/admin_update_profile/admin_set_active)
   + `0013_admin_role.sql` (เพิ่ม enum `admin` + has_role ใหม่ + lockout)
4. **ทดสอบ:** `npm run build` ผ่าน (TypeScript เคลียร์)

### ✅ verified แล้ว (23 มิ.ย. 69)
- ผู้ใช้ paste 0012 + 0013 บน Supabase แล้ว · Claude เช็กผ่าน REST:
  `has_role('admin')`=false (enum admin มีจริง + ฟังก์ชันคอมไพล์ผ่าน) · admin_set_roles/update_profile/set_active
  มีอยู่ + guard ทำงาน (ปฏิเสธ session ที่ไม่ถูกต้อง) ✅ · commit+push แล้ว (`f62a75b`) → Vercel auto-deploy

### ▶️ ขั้นถัดไป (เหลือทดสอบ UI จริง + D10 ก้อนต่อไป)
- **เหลือ:** ผู้ใช้ทดสอบบน UI จริง (login manager → ตั้ง admin → admin เห็น/ทำได้ทุกอย่าง · รีเซ็ตรหัส/ระงับบัญชี · กัน lockout)
- D10 ก้อนถัดไปตาม roadmap: A1 เครื่องจักร · A2 เบิกวัตถุดิบ · A3 line clearance · A5 จำนวนคน/auto job no

---

## 📅 บันทึกวันที่ 23 มิถุนายน 2569 — เฟส 9 / D9 (ก้อน 2): Checklist UAT

### ✅ วันนี้ทำอะไรไปบ้าง — เครื่องมือให้ทีมไล่ทดสอบก่อนเปิดใช้จริง 📋
1. **`docs/uat-checklist.md` (ใหม่)** — checklist ภาษาไทย ติ๊กได้ สำหรับทีม:
   - Flow ครบวงจรส่งต่อกันทีละแผนก (ผู้บริหารสร้างงาน→ยืนยันแผน · ผลิตเริ่ม/บันทึก/ส่ง QC · QC ลงนาม→QA · QA→FG)
     — ระบุ role + สถานะคาดหวังทุกขั้น (อิง `TRANSITIONS` จริงใน job-constants)
   - ทดสอบกันสิทธิ์/กันข้ามขั้น · ตีกลับ QC/QA · ฟีเจอร์เด่น (realtime/offline/มือถือ/รายงาน/แดชบอร์ด/audit)
   - ความพร้อม go-live (ตั้งบัญชีจริง ~30 คน · กัน seed ปนงานจริง) + ตารางจดปัญหา + ช่องสรุปผล
2. ไม่ต้อง paste DB · ไม่แตะโค้ดแอป (เป็นเอกสาร) → ทีมเปิดไฟล์ใช้ได้เลย

### ▶️ ขั้นถัดไป (D9)
- ทีมไล่ทดสอบตาม `docs/uat-checklist.md` กับงานจริง → เก็บปัญหา/feedback กลับมาแก้
- เก็บงาน go-live: ตั้งบัญชีผู้ใช้จริง · จัดการ seed · (ออปชัน) หน้า admin จัดการผู้ใช้ (backlog เดิม)

---

## 📅 บันทึกวันที่ 23 มิถุนายน 2569 — เฟส 9 / D9 (ก้อน 1): หน้าสร้างงานผลิตใหม่

### ✅ วันนี้ทำอะไรไปบ้าง — ปิด gap ก่อน UAT: สร้าง order/job ในแอปได้ 🆕
> เริ่ม D9 · เช็ก Notion: ฟีเจอร์หลัก 10 ข้อ (1–10) ครบแล้ว · ข้อ 11–23 เป็น backlog อนาคต (กลุ่ม A/B/C) ไม่บล็อก v1
> **เจอ gap:** ไม่มี UI สร้าง order/job (งานมาจาก seed เท่านั้น) → ตรง requirement ข้อ 1 ที่ยังไม่ได้ทำ · ผู้ใช้เลือกให้ทำก่อน UAT
1. **DB (`web/supabase/migrations/0011_create_job.sql`)** — ⏳ รอ paste:
   - `create_product(code,name,dosage_form,std_hours)` — เพิ่มยาใหม่ (manager เท่านั้น) · กันรหัสซ้ำ
   - `create_job_with_order(customer,product,qty,unit,due,job_no,planned_start/end,lot)` — สร้าง order+job(+ล็อต) ธุรกรรมเดียว
     · manager เท่านั้น · งานเริ่มสถานะ `pending_announce` · `order_no='ORD-'||job_no` (unique อัตโนมัติ) · audit GUC
   - ทั้งคู่ security definer (แพตเทิร์นเดียวกับ add_production_record/advance_job_status)
2. **แอป (`web/`)** — build ผ่าน:
   - `lib/data/products.ts` (`getProducts()` สำหรับ dropdown)
   - `app/(app)/board/new/` — `page.tsx` (server, guard manager) + `new-job-form.tsx` (ฟอร์ม + **เพิ่มยาใหม่ inline**) + `actions.ts`
   - `board/page.tsx` + `board-view.tsx` — ปุ่ม **"＋ สร้างงานใหม่"** บนบอร์ด (เห็นเฉพาะ manager)
   - งานใหม่ขึ้นบอร์ดสดทันที (realtime `jobs` ที่มีอยู่แล้ว)

### ✅ verified แล้ว (23 มิ.ย. 69)
- ผู้ใช้ paste 0011 + ทดสอบ UI จริง: **manager สร้างงานใหม่ + เพิ่มยา inline ได้ · เด้งเข้าหน้างาน · ขึ้นบอร์ด** ✓

### ▶️ ขั้นถัดไป (D9 — หลังสร้างงานได้แล้ว)
- ทำ checklist UAT (flow ครบวงจร: สร้างงาน→ผลิต→QC→QA→FG ต่อ role) + ตรวจความพร้อม go-live (ตั้งบัญชีผู้ใช้จริง ~30 คน)

---

## 📅 บันทึกวันที่ 23 มิถุนายน 2569 — เฟส 8 / D8 ส่วน 2 (ก้อน 3/4): ขัดเกลามือถือ

### ✅ วันนี้ทำอะไรไปบ้าง — touch target + กันบั๊ก iOS zoom 📱
> สำรวจทุกหน้าก่อน: โครง responsive ดีอยู่แล้ว (ตารางมี overflow-x-auto + min-w · grid ยุบ 2 คอลัมน์ · drawer มือถือ)
> → จุดที่ขัดได้คุ้มสุด = ขนาดนิ้วแตะ + บั๊ก iOS · ทำผ่าน base CSS ครั้งเดียวครอบทั้งแอป (ไม่ไล่แก้ทุกไฟล์ให้เสี่ยง)
1. **`app/globals.css`** — เพิ่ม 2 กฎ:
   - จอ ≤640px: `input/select/textarea { font-size:16px }` → **กันบั๊ก iOS Safari ซูมเองตอนแตะช่องกรอก** (ฟอนต์ <16px)
   - `@media (pointer: coarse)` (อุปกรณ์สัมผัส): ปุ่ม/ช่องกรอก `min-height:44px` + ตัด tap-highlight + `touch-action:manipulation`
     → นิ้วแตะสะดวก **เฉพาะมือถือ/แท็บเล็ต ไม่กระทบเดสก์ท็อปที่ใช้เมาส์**
2. **`components/app-shell.tsx`** — เมนู nav `py-2→py-2.5` · ปุ่มออกจากระบบ `py-1.5→py-2` (กดง่ายขึ้น)
3. **ทดสอบ:** `npm run build` ผ่าน · ไม่ต้อง paste DB (CSS/markup ล้วน)

### ⚠️ ขั้นที่ผู้ใช้ทดสอบ (ออปชัน — deploy แล้วเทสได้เลย)
- เปิดบนมือถือจริง (หรือ DevTools โหมดมือถือ) → แตะช่องกรอกผลผลิต/วันที่ **ไม่ควรซูมเอง** · ปุ่มกดง่ายขึ้น
- เดสก์ท็อปต้องเหมือนเดิม (กฎ touch ใช้เฉพาะ pointer: coarse)

### ▶️ ขั้นถัดไป — ปิด D8 แล้วไป D9
- **ตัดสินใจ (ผู้ใช้เห็นด้วย):** เลื่อนก้อน perf ออกไป — ตอนนี้ข้อมูลยังน้อย วัดผลไม่ได้ + index หลักมีครบแล้ว
  → ทำ perf ตอนใช้งานจริงไปสักพัก มีข้อมูลเยอะ แล้ว EXPLAIN ของจริง (ย้ายเข้า Backlog)
- **= ปิด D8 (hardening สำคัญครบ: realtime · RLS tests · offline · mobile)** → เริ่ม **D9: UAT + deploy production**
  (อ่าน `docs/recommendations.md` + fetch Notion เช็ก requirement ใหม่ก่อนเริ่ม D9)

---

## 📅 บันทึกวันที่ 23 มิถุนายน 2569 — เฟส 8 / D8 ส่วน 2 (ก้อน 2/4): Offline-resilient save (C1)

### ✅ วันนี้ทำอะไรไปบ้าง — บันทึกผลผลิตแบบทนเน็ตกระตุก 📶💾
> เลือกทำก้อน offline ก่อน (Claude เลือกให้) เพราะคนหน้าไลน์ใช้มือถือ + เน็ตโรงงานกระตุก → ข้อมูลห้ามหาย (ALCOA)
1. **DB (`web/supabase/migrations/0010_record_idempotency.sql`)** — ⏳ รอ paste:
   - เพิ่มคอลัมน์ `client_id uuid unique` ใน `production_records` (idempotency key จากฝั่ง client)
   - **ยกเครื่อง `add_production_record()` เพิ่มพารามิเตอร์ `p_client_id`** (drop ตัว 8-arg เดิม → สร้าง 9-arg)
     · ถ้า client_id นี้เคยบันทึกแล้ว → คืน id เดิม (ไม่ทำซ้ำ) · insert ใช้ `on conflict (client_id) do nothing` กัน race
     · = **retry ปลอดภัย ไม่เกิด record ผลผลิตซ้ำ** (เคสเขียน DB สำเร็จแต่ response หลุด)
2. **`web/lib/offline-queue.ts` (ใหม่)** — คิว localStorage (SSR-safe): `newClientId/upsertPending/removePending/pendingForJob`
   - แต่ละรายการมี `clientId` (UUID) → เก็บใน `pd_pending_records_v1`
3. **`record-actions.ts`** — `addRecord()` รับ `clientId` เพิ่ม → ส่ง `p_client_id` เข้า rpc
4. **`record-form.tsx` (ยกเครื่อง)** — บันทึกแบบทนเน็ต + สถานะชัด:
   - **แยก error ถาวร (validation/สิทธิ์/สถานะ) ออกจากเน็ตหลุด (throw)** — ถาวร=ไม่ retry · เน็ต=retry
   - auto-retry 3 รอบ backoff (1.5/3/6s) · ครบแล้วยังไม่ได้ → **ค้างไว้ในคิว** (ไม่หาย)
   - เก็บลง localStorage ทันทีที่กดบันทึก → **ปิดจอ/รีโหลดก็ไม่หาย** · เปิดหน้าใหม่เจอ banner รายการค้าง
   - **auto-retry เมื่อเน็ตกลับมา** (event `online`) + ปุ่ม "ลองบันทึกอีกครั้ง" + ปุ่ม "ทิ้ง" (confirm)
   - สถานะ: กำลังบันทึก / เน็ตมีปัญหา–ลองใหม่ (n/4) / ค้างไว้ / บันทึกแล้ว ✓
5. **ทดสอบ:** `npm run build` ผ่าน (TypeScript เคลียร์)

### ✅ verified แล้ว (23 มิ.ย. 69)
- ผู้ใช้ paste 0010 + ทดสอบบน UI จริง: **บันทึกปกติได้ ("บันทึกแล้ว ✓")** ·
  **Offline → "ลองใหม่/ค้างไว้" → เปิดเน็ตกลับ บันทึกเองอัตโนมัติ ไม่มีแถวซ้ำ** ✓ (idempotency ทำงานจริง)

### ▶️ ขั้นถัดไป (D8 ส่วน 2 — เหลือ 2 ก้อน)
- ขัดเกลามือถือ (touch target) · perf (index/query)

---

## 📅 บันทึกวันที่ 23 มิถุนายน 2569 — เฟส 8 / D8 ส่วน 2 (ก้อน 1/4): เทส RLS (B4)

### ✅ วันนี้ทำอะไรไปบ้าง — สคริปต์เทส RLS ด้วย impersonation 🔐
> D8 ส่วน 2 มี 4 ก้อน: **(1) เทส RLS** ← เสร็จ (รอ paste) · (2) groundwork offline · (3) ขัดเกลามือถือ · (4) perf
1. **อ่าน policy จริงทั้งหมดก่อนเขียนเทส** — 0003 (baseline read) · 0004 (write_* แยก role + audit_log อ่านเฉพาะ manager/qa)
   · 0008 (approvals) · 0002 (audit append-only: revoke update/delete + trigger) · ยืนยัน `log_audit()` เป็น security definer
   (= การเขียน audit จาก trigger ข้าม RLS ได้ → เทส "เขียนสำเร็จ" ไม่พังเพราะ audit)
2. **ไฟล์ใหม่ `web/supabase/tests/rls_impersonation_test.sql`** — ⏳ รอ paste:
   - **ฟังก์ชัน `pg_temp.rls_test()` return ตารางผลลัพธ์** (ไม่ใช้ temp table) · insert/update ทดสอบถูก
     `raise '__UNDO__'` ในบล็อกย่อยเพื่อย้อนกลับ = **ไม่แตะข้อมูลจริง รันซ้ำได้** · อิง uid auth จริง (ไม่ hardcode)
   - 🔧 **แก้บั๊กรอบแรก:** เวอร์ชันแรกใช้ temp table + `begin/rollback` → Supabase SQL Editor commit ทีละ
     statement ทำให้ `on commit drop` ลบ temp ทันที (error `relation "_tu" does not exist`) → เปลี่ยนเป็นฟังก์ชัน
   - สวมรอยด้วย `set role authenticated` (plain SET, ทนกับ autocommit) + ตั้ง `request.jwt.claims.sub` · anon ใช้ `set role anon`
   - **เทสครอบคลุม:** (1) role ผูก auth ครบไหม · (2) ตรรกะ write policy ผ่าน `has_role()` (แม่น ไม่ขึ้นกับ grant)
     · (3) อ่าน jobs: ทุก role เห็น / anon ไม่เห็น · (4) **audit_log เห็นเฉพาะ manager/qa** (เคส "ว่างเงียบ" คลาสสิก)
     · (5) insert ตาม write_* · (6) update วัด row_count (แก้ profile คนอื่น = 0 แถวเงียบ) · (7) audit_log แก้ไม่ได้
   - **แยกแยะสาเหตุปฏิเสธ: RLS บล็อก vs ไม่มี grant ตาราง** (แอปเขียนผ่าน service key อยู่แล้ว) → ผลเทสไม่หลอกตา
   - ออกผลเป็นตาราง: คอลัมน์ `result` = ✅/❌ + RAISE NOTICE สรุป ผ่าน X/Y
3. อัปเดต `web/supabase/README.md` — เพิ่มหัวข้อวิธีรันเทส RLS · build แอปไม่กระทบ (เป็น SQL ล้วน)

### ✅ verified แล้ว (23 มิ.ย. 69)
- ผู้ใช้ paste + รัน `tests/rls_impersonation_test.sql` บน Supabase จริง → **ผ่านครบ 27/27 เคส** ✅
  (anon อ่านไม่ได้ · audit_log เห็นเฉพาะ manager/qa · เขียน/แก้ตาม policy · แก้ profile คนอื่น=เงียบ · audit_log แก้ไม่ได้)
- 🔧 เจอ 2 บั๊กระหว่างทาง แก้แล้ว: (1) temp table `on commit drop` โดน editor ลบทันที → เลิกใช้ temp table
  (2) `array || string-literal` ตีความเป็น array||array (22P02) → เปลี่ยนเป็น `array_append()`

### ▶️ ขั้นถัดไป (D8 ส่วน 2 — ก้อนที่เหลือ)
- groundwork offline (C1: optimistic save + retry) · ขัดเกลามือถือ (touch target) · perf (index/query)

---

## 📅 บันทึกวันที่ 22 มิถุนายน 2569 — เฟส 8 / D8 ส่วน 1: Realtime

### ✅ วันนี้ทำอะไรไปบ้าง — อัปเดตหน้าจอแบบสด (Realtime) 🔴🟢
> ผู้ใช้เลือกทำ D8 แบ่งเป็นก้อน (เหมือน D6): **(1) Realtime** ← เสร็จ · (2) เทส RLS + perf + มือถือ + offline ← ถัดไป
1. **เช็ก roadmap + Notion ก่อนเริ่ม** — `recommendations.md` D8 = hardening (realtime · RLS tests B4 · perf · mobile · offline) ·
   fetch Notion **Lean PRD**: ยืนยัน pain point หลัก = "ไม่อัปเดตแบบ Real-time" → ทำ realtime ก่อนตรงโจทย์สุด · ไม่มี requirement ใหม่
2. **`components/realtime-refresh.tsx` (ใหม่)** — client component ฟัง Supabase Realtime (`postgres_changes`)
   ของตารางที่ระบุ → เมื่อมีคนเพิ่ม/แก้/ลบ สั่ง `router.refresh()` (debounce 300ms) ดึงข้อมูลใหม่จาก server
   - **คงสถาปัตยกรรม server-first**: client ไม่ถือ state ข้อมูลเอง แค่สั่ง revalidate
   - มีป้าย "🟢 อัปเดตสด" มุมจอล่างขวา (โผล่เฉพาะตอนเชื่อมต่อสำเร็จ)
   - RLS ยังบังคับ — รับเฉพาะ event ของแถวที่ตัวเองมีสิทธิ์อ่าน
3. **ฝังลง 4 หน้า:** บอร์ดงาน (`jobs`) · แดชบอร์ด (`jobs`+`production_records`) ·
   รายงานประจำวัน (`production_records`) · รายละเอียดงาน (`jobs`+`production_records`+`approvals`)
4. **DB (ไฟล์ `web/supabase/migrations/0009_realtime.sql`)** — ⏳ รอ paste:
   - `alter publication supabase_realtime add table ...` 3 ตาราง (jobs/production_records/approvals)
   - ใช้ DO block กัน duplicate (รันซ้ำได้)
5. **ทดสอบ:** `npm run build` ผ่าน (TypeScript เคลียร์)

### ⚠️ ขั้นที่ผู้ใช้ต้องทำเอง
- **paste `0009_realtime.sql`** ลง Supabase SQL Editor (ต่อจาก 0001–0008) → realtime ถึงจะ broadcast
  (ถ้ายังไม่ paste: หน้าจอยังใช้ได้ปกติ แต่ต้องรีเฟรชเองเพื่อเห็นของใหม่ · ป้าย "อัปเดตสด" อาจขึ้นแต่ไม่มี event มา)
- ทดสอบ: เปิดบอร์ดงาน 2 หน้าจอ (หรือมือถือ+คอม) → จอ A เปลี่ยนสถานะงาน → จอ B ขยับเองภายในไม่กี่วินาที

### ▶️ ขั้นถัดไป (D8 ส่วน 2)
- **เทส RLS ด้วย impersonation** (recommendations B4 — RLS ผิดคืนค่าว่างเงียบ ต้องเทสที่ผลลัพธ์) ·
  perf (index/query) · ขัดเกลามือถือ · groundwork offline (optimistic save + retry ตาม C1)

---

## 📅 บันทึกวันที่ 22 มิถุนายน 2569 — เฟส 7 / D7: Dashboard / KPI + ต้นทุนค่าแรง

### ✅ วันนี้ทำอะไรไปบ้าง — แดชบอร์ดผู้บริหาร 📊💰
1. **เช็ก roadmap + Notion ก่อนเริ่ม** — `recommendations.md` D7 = "Dashboard / KPI + DL cost" ·
   fetch Notion demo-feature-suggestions: ยัง 10 ฟีเจอร์เดิม ไม่มี requirement ใหม่ (ข้อ 1 "ผู้บริหารดูภาพรวม")
2. **`lib/data/dashboard.ts` (ใหม่)** — `getDashboardData(from, to)`:
   - นับงานตามสถานะ + งานติดปัญหา (ภาพรวมงานปัจจุบัน — ไม่อิงช่วงวันที่)
   - รวม input/output/loss/hours ของ `production_records` ในช่วง [from, to] · **Yield%** = output/input
   - แยกตามสถานี (prep/mixing/tableting/packing) ครบทุกสถานีแม้ไม่มีข้อมูล
   - ค่าคงที่ `DEFAULT_LABOR_RATE = 60` (บาท/ชม.)
3. **`app/(app)/page.tsx` — ยกเครื่องหน้า `/` (เมนู "แดชบอร์ด")** เป็นหน้าสรุปผู้บริหารจริง:
   - ฟอร์มเลือกช่วงวันที่ (from–to, method=get · default = ต้นเดือน→วันนี้)
   - การ์ดงานตามสถานะ (เดิม) + KPI ผลผลิต: ผลิตได้รวม / ของเสียรวม / Yield% / ชม.แรงงานรวม
   - **ส่วนต้นทุนค่าแรง (DL cost) — เห็นเฉพาะ manager** (ต้นทุน = ข้อมูลอ่อนไหว):
     ปรับ rate ได้ (ช่อง ฿/ชม.) · ยอดรวม = ชม.รวม × rate · ตารางแยกสถานี (ชม./ผลิตได้/ของเสีย/ค่าแรง) + แถวรวม
4. **ไม่ต้อง paste DB เพิ่ม** — อ่านข้อมูล D5 ที่มีอยู่ · `npm run build` ผ่าน (TypeScript เคลียร์)

### 📌 หมายเหตุการออกแบบ (กันลืม)
- **อัตราค่าแรงยังไม่เก็บใน DB** — ใช้ค่า default 60 ฿/ชม. + ปรับชั่วคราวผ่าน URL param (`?rate=`)
  ค่าจะรีเซ็ตเมื่อรีโหลด · ถ้าผู้บริหารอยากให้จำค่าถาวร → ต่อยอดเป็นตาราง `app_settings` (ต้อง paste SQL + RLS) ภายหลัง
- DL cost = "ต้นทุนค่าแรงทางตรง" คิดจากชั่วโมงที่บันทึกเท่านั้น (ประเมินเบื้องต้น ไม่รวม OT/สวัสดิการ)

### ⚠️ ขั้นที่ผู้ใช้ควรทดสอบ (ออปชัน — โค้ดพร้อม/ไม่ต้อง paste)
- login เป็น **manager** เปิดหน้าแรก → เห็นช่อง "ค่าแรง" + ส่วน DL cost · ลองเปลี่ยนช่วงวันที่/อัตรา
- login เป็น role อื่น (เช่น production) → **ไม่เห็น** ส่วนต้นทุนค่าแรง (เห็นแค่ KPI ผลผลิต)

### ▶️ ขั้นถัดไป (D8)
- **D8 hardening**: realtime · RLS tests · perf · ขัดเกลา mobile · groundwork offline
  (อ่าน `docs/recommendations.md` roadmap D8 + fetch Notion เช็ก requirement ใหม่ก่อน)

---

## 📅 บันทึกวันที่ 22 มิถุนายน 2569 — เฟส 6 / D6 ส่วน 2: Daily Report

### ✅ วันนี้ทำอะไรไปบ้าง — หน้ารายงานประจำวัน 📊
1. **Daily Report** — หน้า `/daily` สรุปบันทึกผลผลิตของทุกงานในวันที่เลือก (ไม่ต้อง paste DB เพิ่ม — อ่านข้อมูล D5)
   - `lib/data/daily.ts` (`getDailyReport(date)`) — join `production_records → jobs(job_no) → orders → products(name)`
     + ชื่อ operator · sort ตามเลขงานใน JS (order ตามคอลัมน์ embed ไม่จัดลำดับแถวหลัก)
   - `app/(app)/daily/page.tsx` — server component อ่าน `searchParams.date` (default วันนี้) ·
     ฟอร์มเลือกวันที่ (method=get) · การ์ดสรุป (จำนวน/ผลิตได้รวม/ของเสียรวม/ชม.รวม) · ตารางคลิกเข้างานได้
   - `nav.ts` — เพิ่มเมนู "รายงานประจำวัน" (ready=true, ทุก role) · ลบ placeholder `/production` ที่ไม่ได้ใช้
     (บันทึกผลผลิตอยู่ในหน้า job แล้ว) · `npm run build` ผ่าน
2. **= D6 เสร็จครบทั้ง 2 ส่วน** (E-signature + Daily Report)

### ▶️ ขั้นถัดไป (D7)
- **D7**: Dashboard / KPI + ต้นทุนค่าแรง (DL cost) — สรุปภาพรวมผู้บริหาร
  (อ่าน `docs/recommendations.md` roadmap D7 + fetch Notion เช็ก requirement ใหม่ก่อน)

---

## 📅 บันทึกวันที่ 22 มิถุนายน 2569 — เฟส 6 / D6 ส่วน 1: E-signature

### ✅ วันนี้ทำอะไรไปบ้าง — ลายเซ็นอิเล็กทรอนิกส์ (lite) ของ QC/QA 🖊️
> ผู้ใช้เลือกทำ D6 เป็น 2 ก้อน: **(1) E-signature** ← verified ✅ · (2) Daily Report ← เสร็จแล้ว (บล็อกบน)
1. **เช็ก Notion ก่อนเริ่ม** — fetch Lean PRD + demo-feature-suggestions: ไม่มี requirement ใหม่
   (หมายเหตุ: ปุ่ม QC/QA อนุมัติ/ตีกลับ ทำไปแล้วใน D4 · D6 = เพิ่มชั้น "ลงนามยืนยันรหัส" + เก็บ approvals ตาม A3)
2. **DB (ไฟล์ `web/supabase/migrations/0008_approvals.sql`)** — ⏳ รอ paste:
   - ตาราง `approvals` (job/ผู้ลงนาม/stage qc-qa/decision approve-reject/reason/signed_at + ALCOA cols)
     + meta trigger + audit trigger + RLS (อ่านได้ authenticated, เขียนผ่านฟังก์ชันเท่านั้น)
   - ฟังก์ชัน `sign_job_decision(job, stage, decision, reason)` (security definer):
     ตรวจ role ตาม stage · reject ต้องมีเหตุผล · เช็กงานอยู่ขั้นนั้นจริง · บันทึกลายเซ็น
     **แล้วเรียก `advance_job_status()` ขยับสถานะในธุรกรรมเดียว (atomic)** — ใช้ด่านเดิมเป็น gatekeeper
3. **แอป (`web/`)** — build ผ่าน:
   - `board/actions.ts` เพิ่ม `signDecision()` — **ยืนยันรหัสผ่านซ้ำ** ผ่าน verifier client แยก
     (`@supabase/supabase-js` publishable key, persistSession:false → ไม่แตะ session ที่ล็อกอิน) ก่อนเรียก rpc
   - `job-constants.ts` — mark `esign:true` + `stage` บน transition qc/qa ทั้ง 4
   - `job-actions.tsx` — ปุ่ม QC/QA (🖊️) เปิดแผงลงนาม: ช่องรหัสผ่าน (+เหตุผลถ้าตีกลับ) · ปุ่มอื่นทำงานเหมือนเดิม
   - `lib/data/approvals.ts` (`getApprovalsForJob`) + แสดงรายการลายเซ็นในหน้า `board/[jobNo]`
     (เขียว=อนุมัติ/แดง=ตีกลับ + เหตุผล + เวลา)
4. **ทดสอบ:** `npm run build` ผ่าน (TypeScript เคลียร์)

### ⚠️ ขั้นที่ผู้ใช้ต้องทำเอง
- **paste `0008_approvals.sql`** ลง Supabase SQL Editor (ต่อจาก 0001–0007)
- ทดสอบ: login เป็น **qc** เปิดงานสถานะ QC (เช่น JOB-002) → กด "🖊️ QC ผ่าน → ส่ง QA"
  → ใส่รหัสผิดดูว่าปฏิเสธ · ใส่รหัสถูกดูว่าสถานะขยับเป็น QA + ลายเซ็นโผล่ในหน้า

### ▶️ ขั้นถัดไป (D6 ส่วน 2)
- **Daily Report** — หน้าสรุปบันทึกผลผลิตรายวัน (รวมทุกงาน กรองตามวันที่/สถานี) + เพิ่มเมนู
  (ข้อมูลมีแล้วจาก D5 · เหลือทำหน้า report view)

---

## 📅 บันทึกวันที่ 22 มิถุนายน 2569 — เฟส 5 / D5

### ✅ วันนี้ทำอะไรไปบ้าง — ฟอร์มบันทึกผลผลิตรายวัน + validation ฝั่ง server + audit 🎉
1. **เช็ก Notion ก่อนเริ่ม** — fetch หน้า demo-feature-suggestions: เป็น 10 ฟีเจอร์เดิม ไม่มี requirement ใหม่
   (D5 ตรงกับข้อ 9 "Decimal Input & Validation" + ข้อ 5 "History/Activity Log")
2. **DB (ไฟล์ `web/supabase/migrations/0007_production_records.sql`)** — ⏳ รอ paste:
   - ฟังก์ชัน `add_production_record(...)` แบบ `security definer` = **ด่านตัดสินความถูกต้องที่ server**
     (แพตเทิร์นเดียวกับ `advance_job_status` ของ D4)
   - ตรวจ: ล็อกอิน · สิทธิ์เฉพาะ production/manager · **guard สถานะ** (บันทึกได้เฉพาะ in_production/qc/qa —
     ก่อนหน้ายังไม่ผลิต, FG ล็อกแล้วตาม A5)
   - validate: input/output จำเป็น · ห้ามติดลบ · รองรับทศนิยม · `output ≤ input` · `output + loss ≤ input` ·
     ชม. 0–24 · วันที่ล้ำอนาคตไม่ได้
   - ตั้ง audit GUC (`app.current_profile_id` + เหตุผล) → trigger บันทึก **ใครกรอก** อัตโนมัติ (ALCOA Attributable/Accurate)
3. **แอป (`web/`)** — สร้างครบ + build ผ่าน:
   - `lib/data/station-constants.ts` — ค่าคงที่สถานี (prep/mixing/tableting/packing) + `validateRecord()`
     **ใช้ร่วม client/server** (ตรรกะตรงกับ DB) + `RECORDABLE_STATUSES`
   - `lib/data/production.ts` — `getRecordsForJob()` (join ชื่อผู้บันทึกผ่าน FK `operator_id`)
   - `board/[jobNo]/record-actions.ts` (server action `addRecord` → rpc) + `record-form.tsx`
     (ฟอร์ม decimal input `step="any"`, validate ทันทีฝั่ง client, error แยกช่อง)
   - หน้า `board/[jobNo]/page.tsx` — เพิ่มตารางบันทึกผลผลิต + ปุ่ม/ฟอร์ม
     (แสดงฟอร์มเฉพาะคนมีสิทธิ์ + สถานะที่บันทึกได้ · ไม่งั้นขึ้นข้อความอธิบาย)
4. **ทดสอบ:** `npm run build` ผ่าน (TypeScript เคลียร์ · ทุก route generate ครบ)

### ⚠️ ขั้นที่ผู้ใช้ต้องทำเอง
- **paste `0007_production_records.sql`** ลง Supabase SQL Editor (ต่อจาก 0001–0006) → ปุ่มบันทึกผลผลิตถึงจะทำงาน
  (ถ้ายังไม่ paste: ตารางบันทึกเดิมดูได้ แต่กดบันทึกจะ error "ไม่พบฟังก์ชัน")
- ทดสอบ: login เป็น **production** เปิด JOB-001 (in_production) → กด "+ บันทึกผลผลิต"
  ลองกรอก output > input หรือค่าติดลบ ดูว่า DB/ฟอร์มปฏิเสธ · กรอกถูกต้องดูว่าบันทึก + ขึ้นในตาราง
- 📌 rpc นี้ต้องมี session ผู้ใช้จริง (อิง `auth.uid()`) → ทดสอบผ่าน REST+secret key ไม่ได้ ต้อง login ทดสอบ (เหมือน D4)

### ▶️ ขั้นถัดไป (D6)
- **D6**: Daily Report + **QC/QA approve-reject + e-signature (lite)** — ยืนยันรหัสผ่านซ้ำตอนอนุมัติ/ตีกลับ
  + ตาราง `approvals` (เก็บชื่อ/ผลตัดสิน/เหตุผล/เวลา) ตาม recommendations.md A3
  (อ่าน `docs/recommendations.md` A3 + fetch Notion เช็ก requirement ใหม่ก่อน)

---

## 📅 บันทึกวันที่ 22 มิถุนายน 2569 — เฟส 4 / D4

### ✅ วันนี้ทำอะไรไปบ้าง — บอร์ดงาน + รายละเอียด job + กันข้ามสถานะ 🎉
1. **เช็ก Notion ก่อนเริ่ม** — ไม่มี requirement ใหม่ · ใช้ดีไซน์ board จาก prototype ที่ทีม/CEO อนุมัติ
   (kanban ตามสถานะ + filter bar + KPI cards + การ์ดงาน) · ผู้ใช้เลือก **kanban บนคอม / ลิสต์ซ้อนบนมือถือ**
2. **DB (ไฟล์ `web/supabase/migrations/0006_job_transitions.sql`)** — ⏳ รอ paste:
   - ฟังก์ชัน `advance_job_status(job_id, to, reason)` **บังคับลำดับสถานะที่ server** (กันข้ามขั้น)
   - ตรวจสิทธิ์ตาม role · reject ต้องมีเหตุผล · บันทึก audit (set GUC ใครทำ/เหตุผล)
   - flow: รอแจ้งผลิต→มีแผนแล้ว→กำลังผลิต→QC→QA→FG · reject: QC/QA→กำลังผลิต
3. **แอป (`web/`)** — สร้างครบ + build ผ่าน:
   - `lib/data/job-constants.ts` (สี/label/transitions — ใช้ได้ทั้ง server/client) + `lib/data/jobs.ts` (query join)
   - `app/(app)/board/page.tsx` + `board-view.tsx` — KPI + search/filter (job/lot/ลูกค้า/ยา/สถานะ/เฉพาะปัญหา)
     + kanban responsive · การ์ดสีตามสถานะ + ป้ายปัญหา
   - `app/(app)/board/[jobNo]/page.tsx` — รายละเอียด + stepper สถานะ + ปุ่มเปลี่ยนสถานะตาม role
     (`job-actions.tsx` + `actions.ts` เรียก rpc) · ตีกลับมีช่องเหตุผล
   - เปิดเมนู "บอร์ดงาน" ใน nav (ready=true)
4. **ทดสอบ:** build ผ่าน · embed join query คืนข้อมูลถูก (ลูกค้า/ยา/lot) · proxy กันหน้าได้

### ⚠️ ขั้นที่ผู้ใช้ต้องทำเอง
- **paste `0006_job_transitions.sql`** ลง Supabase SQL Editor (ต่อจาก 0001-0005) → ปุ่มเปลี่ยนสถานะถึงจะทำงาน
  (ถ้ายังไม่ paste: หน้าบอร์ด/รายละเอียดดูได้ แต่กดเปลี่ยนสถานะจะ error "ไม่พบฟังก์ชัน")
- ทดสอบ: login เป็น role ต่างกัน (เช่น production กด "ส่งตรวจ QC", qc กด "ผ่าน/ตีกลับ") ดูว่ากันข้ามขั้น/สิทธิ์ได้

### ▶️ ขั้นถัดไป (D5)
- **D5**: ฟอร์มบันทึกผลิตรายวัน · decimal/validation ฝั่ง server · ผูก batch/lot · เขียนลง audit
  (อ่าน `docs/recommendations.md` C2 + fetch Notion เช็ก requirement ใหม่ก่อน)

---

## 📅 บันทึกวันที่ 22 มิถุนายน 2569 — เฟส 3 / D3

### ✅ วันนี้ทำอะไรไปบ้าง — ระบบล็อกอิน + สิทธิ์ตาม role + PWA 🎉
1. **เช็ก Notion ก่อนเริ่ม** — อ่านหน้า `demo-feature-suggestions` + `full-demo-decision` (ทีมสรุปทิศ
   "Production-first, quality-controlled, ERP-ready") · ไม่มี requirement ใหม่ที่ขัดแผน · D3 ตรงกับ "สิ่งที่ยังขาด"
2. **อ่าน docs Next.js 16 ก่อนเขียน** (ตาม `web/AGENTS.md`) — พบ breaking change: `middleware` → **`proxy`**
3. **DB (ไฟล์ `web/supabase/migrations/0004_auth_roles.sql`)** — ⏳ รอ paste ลง Supabase:
   - เพิ่ม `email` ใน profiles + trigger `handle_new_user` auto-link `auth.users` → profile ตามอีเมล
   - helper `current_profile_id()` + `has_role(role)` (security definer, wrap `auth.uid()` ตาม B1)
   - RLS policy แยกตาม role ทับ baseline 0003 (ผลิต/manager เขียน production_records, manager เขียน orders/products,
     audit_log อ่านได้เฉพาะ manager/qa ฯลฯ)
   - ใส่อีเมลให้ profiles ตัวอย่าง 5 ตัว → ใช้เป็นบัญชี demo ได้ทันที
4. **แอป (`web/`)** — สร้างครบ + build ผ่าน + ทดสอบ proxy:
   - `proxy.ts` + `lib/supabase/proxy-session.ts` — รีเฟรช session + กันหน้า protected (ยังไม่ login → เด้ง /login)
   - `lib/auth/dal.ts` (getUser/getProfile + role) · `lib/supabase/admin.ts` (secret key, server-only)
   - `app/login/` (ฟอร์ม useActionState) · `app/actions/auth.ts` (login/logout server actions)
   - `app/(app)/layout.tsx` + `app/(app)/page.tsx` (dashboard นับงานตาม status สดจาก DB)
   - `components/app-shell.tsx` — sidebar + เมนูตาม role + **เมนูมือถือปิดเองหลังเลือก** (requirement ข้อ 10)
   - `app/manifest.ts` + `public/icon.svg` — **PWA ติดตั้งหน้าจอได้** (C1)
5. **ทดสอบ:** `npm run build` ผ่าน · `GET /` (ยังไม่ login) → 307 ไป `/login` · `/login` 200 · manifest 200 ✅

### ⚠️ ขั้นที่ผู้ใช้ต้องทำเอง (โค้ดพร้อม + push แล้ว — แต่ยังใช้ login ไม่ได้จนกว่าจะทำ 2 ขั้นนี้)
1. **paste `0004_auth_roles.sql`** ลง Supabase SQL Editor (ต่อจาก 0001-0003+seed)
2. **สร้างบัญชี Auth** ใน Supabase → Authentication → Users → Add user (ติ๊ก **Auto Confirm User**)
   ใช้อีเมล demo 5 ตัว (เช่น `manop.mgr@pdmonitor.app` = ผู้บริหาร) — รายละเอียดใน `web/supabase/README.md`
3. เปิด https://pd-monitor.vercel.app → ล็อกอิน → เห็นเมนูตามสิทธิ์

### ▶️ ขั้นถัดไป (D4)
- เริ่ม **D4**: หน้า board + job · สถานะปัญหา/สี/search/filter · **state-machine guard** (กันข้ามสถานะ)
  (อ่าน `docs/recommendations.md` C3 + fetch Notion เช็ก requirement ใหม่ก่อน)

### 💡 Backlog / ค้างไว้คุยต่อ (เกิดจากคำถามผู้ใช้ 22 มิ.ย.)
- **หน้า admin จัดการผู้ใช้** (เสริม นอก roadmap D1–D9) — สร้างบัญชี + กำหนด role + **รีเซ็ตรหัสผ่าน**
  ในแอปเอง ไม่ต้องเข้า Supabase dashboard · ใช้ `lib/supabase/admin.ts` (secret key) +
  `auth.admin.updateUserById()` · เหมาะกับโรงงาน (คนงานหน้าไลน์มักไม่มีอีเมลจริง → admin รีเซ็ตให้)
  → ยังไม่ตัดสินใจว่าจะแทรกเฟสไหน (ถามผู้ใช้ก่อน)
- หมายเหตุความปลอดภัยที่อธิบายผู้ใช้แล้ว: รหัสผ่านเก็บแบบ hash ดูย้อนหลังไม่ได้ — admin "ตั้งใหม่" ได้ ไม่ใช่ "อ่านของเดิม"

---

## 📅 บันทึกวันที่ 22 มิถุนายน 2569 — เฟส 2 / D2

### ✅ วันนี้ทำอะไรไปบ้าง — ออกแบบฐานข้อมูล (schema) 🎉
1. **เช็ก Notion ก่อนเริ่ม** — ไม่มี requirement ใหม่จากทีม (Dev Log ทีมยืนยัน D2 = ออกแบบ DB + role + audit log)
2. **เขียนไฟล์ SQL migration ครบชุด** ใน `web/supabase/` (ตามที่ตกลง: เขียนไฟล์ → ผู้ใช้ paste เองใน Supabase)
   - `migrations/0001_schema.sql` — 8 ตาราง + 4 enum + คอลัมน์ ALCOA+ (`created_by/updated_by/created_at/updated_at/version`)
     + trigger `set_row_meta` (เวลา server + เพิ่ม version + กันแก้ created_at/by)
     ตาราง: `profiles, user_roles, products, orders, batches, jobs, production_records`
   - `migrations/0002_audit_log.sql` — `audit_log` **append-only** + trigger `log_audit` ผูกทุกตารางหลัก (A1)
     · ดึง "ใครทำ/เหตุผล" จาก GUC `app.current_profile_id`/`app.audit_reason` (server ตั้งใน D3+)
     · กันแก้/ลบ audit_log ด้วย trigger + revoke
   - `migrations/0003_rls.sql` — **เปิด RLS ทุกตาราง** (B1, default-deny) · baseline: authenticated อ่านได้
     · เขียนผ่าน server (secret key bypass RLS) · policy แยก role ค่อยทับใน D3
   - `seed.sql` — ข้อมูลตัวอย่าง (5 profiles ครบ role, 3 ยา, 3 orders, 2 batches, 3 jobs [มี 1 งานติดปัญหา], 3 daily records)
   - `README.md` — คู่มือ paste ทีละขั้นภาษาไทย
3. **Design choices สำคัญ:**
   - state machine `job_status`: รอแจ้งผลิต→มีแผนแล้ว→กำลังผลิต→QC→QA→FG (requirement ข้อ 2)
   - `problem_flag` แยกจาก flow หลัก (ติดปัญหา/รอแก้/ล่าช้า — ข้อ 3) · station: prep/mixing/tableting/packing
   - `profiles` standalone (ยังไม่ผูก `auth.users`) → seed ได้เลย ผูก auth ใน D3 ผ่าน `auth_user_id`
   - CHECK ระดับ DB: `output_qty <= input_qty`, ห้ามติดลบ, `expiry > manufacture` (C2/A4)

### ✅ ลง Supabase + verify แล้ว (22 มิ.ย. 69)
ผู้ใช้ paste 0001→0002→0003→seed สำเร็จ · Claude ตรวจผ่าน REST API:
8 ตารางครบ · seed เข้าครบ · audit_log=24 (trigger ครบ) · RLS กัน anon ได้ ·
CHECK output≤input ปฏิเสธ (HTTP 400) · audit_log append-only กัน UPDATE ได้จริง

### ▶️ ขั้นถัดไป (D3)
- หลัง paste schema สำเร็จ → เริ่ม **D3**: auth + role + permission helper (`has_role` security definer)
  · PWA manifest · responsive layout · ผูก `profiles.auth_user_id` กับ Supabase Auth
  · ใส่ RLS policy แยกตาม role ทับ baseline ใน 0003
  (อ่าน `docs/recommendations.md` หมวด B/C + fetch Notion เช็ก requirement ใหม่ก่อน)

---

## 📅 บันทึกวันที่ 19 มิถุนายน 2569 — เฟส 1 / D1

### ✅ วันนี้ทำอะไรไปบ้าง — เริ่มสร้างแอปจริง 🎉
1. **Scaffold แอปจริง Next.js ในโฟลเดอร์ `web/`** (ของเดิม prototype/ ยังอยู่ครบ ไม่กระทบ)
   - Next.js 16 + TypeScript + Tailwind v4 + App Router · ฟอนต์ไทย Noto Sans Thai · `lang="th"`
   - shadcn/ui ติดตั้งแล้ว (`components.json`, ปุ่มตัวอย่าง, `lib/utils.ts`)
   - หน้าแรกเป็น placeholder ภาษาไทย "PD Monitor — เฟส 1 พร้อมแล้ว"
2. **วาง Supabase client แบบ server-first** (`web/lib/supabase/server.ts` + `client.ts` ใช้ `@supabase/ssr`)
   - ตั้งชื่อ key ถูกหลัก: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`
     (key รุ่นใหม่ `sb_publishable_`/`sb_secret_` — anon เลิกใช้สิ้นปี 2026 ตาม recommendations.md B2)
   - ค่าจริงอยู่ใน `web/.env.local` (ไม่ขึ้น git) · อัปเดต `.env.example` ให้ naming ตรงกัน
3. **ทดสอบผ่าน:** `npm run build` ผ่าน + `npm run dev` เปิด `localhost:3000` ขึ้นหน้าไทย HTTP 200 ✅
4. **Commit + push ขึ้น GitHub แล้ว** (`f0573da` บน master) — repo เป็น public แล้ว เลยลอง auto-deploy

### ⚠️ ขั้นที่ต้องทำต่อในแดชบอร์ด Vercel (ต้องทำในบัญชีเอง — โค้ดพร้อมแล้ว)
แอปจริงอยู่ใน `web/` แต่ Vercel project ยังชี้ Root Directory = `prototype` อยู่ ต้องตั้งค่าให้ build `web/`:
1. Vercel → project ที่ผูกกับ repo `pd-monitor` → **Settings → Build → Root Directory** เปลี่ยนเป็น `web`
2. **Settings → Environment Variables** (Production) เพิ่ม 3 ตัว (คัดค่าจาก `web/.env.local`):
   `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`
3. Redeploy (หรือ push อีกครั้งให้ auto-deploy) → เช็กว่าหน้าใหม่ขึ้น (ไม่ใช่ prototype เดิม)
- ถ้ายังขึ้น prototype = ยังไม่เปลี่ยน Root Directory · ถ้า build error เรื่อง env = ยังไม่ใส่ env vars
- 📌 หมายเหตุ: เห็น manual `vercel --prod` รันไปที่ org **claude-work** แยกต่างหาก (ไม่เกี่ยวกับ auto-deploy นี้)

### ▶️ ขั้นถัดไป (D2)
- ตั้งค่า Vercel ตามด้านบนให้ auto-deploy ขึ้น `web/` สำเร็จก่อน (ปิดงาน D1)
- แล้วเริ่ม **D2**: ออกแบบ schema + seed · เปิด RLS ทุกตาราง · `audit_log` trigger · คอลัมน์ ALCOA · batch mfg/expiry
  (อ่าน `docs/recommendations.md` หมวด A/B/D + fetch Notion เช็ก requirement ใหม่ก่อน)

---

## 📅 บันทึกวันที่ 19 มิถุนายน 2569

### ✅ วันนี้ทำอะไรไปบ้าง
1. **จัดไฟล์ตั้งต้นโปรเจค** — เพิ่ม `CLAUDE.md` (โหลดอัตโนมัติทุก session), `README.md`, `.env.example`,
   skill `handoff` (จบงาน → อัปเดต note + memory + push)
2. **เชื่อม Notion ของทีม** — ดึง requirement (Lean PRD + 10 ฟีเจอร์ที่น้องๆ เสนอ) มาเก็บที่
   `docs/requirements-from-notion.md` · page หลัก `38092ef2-c18f-803d-bb95-f49546a4f466`
3. **ค้นข้อมูลภายนอก → เขียนคำแนะนำคุณภาพ** `docs/recommendations.md` (GMP/ALCOA+, RLS, state machine,
   PWA, batch traceability) + ปรับ roadmap ดึง security/audit มา D2–D3
4. **ปรับ prototype (demo) ตามคำแนะนำ + 10 ฟีเจอร์ทีม** ⭐ งานหลักวันนี้:
   - board: ค้นหา/กรอง (job/lot/ลูกค้า/ยา/สถานะ) + ป้ายงาน "ติดปัญหา/ล่าช้า" เด่น
   - record: validation (output ≤ input, ห้ามติดลบ, ทศนิยม) + เตือนเมื่อทำผิดลำดับ station
   - job: Activity Log (จำลอง audit) + ปุ่ม QC/QA อนุมัติ/ตีกลับ (ตีกลับต้องมีเหตุผล) + วัน Mfg/Exp
   - mobile menu ปิดเองหลังเลือกหน้า
   - ทดสอบ syntax + logic ผ่านหมด (validateRecord, flag, data shape)

### 🌐 ลิงก์ demo ใหม่ (ใช้ตัวนี้!)
**https://prototype-omega-eight.vercel.app** ← demo เวอร์ชันล่าสุดครบฟีเจอร์ (โชว์ CEO ตัวนี้)

### ⚠️ ปัญหา Vercel เดิม (สำคัญ — กันงง)
- `pd-monitor.vercel.app` (โปรเจคเดิมใต้ทีม **claude-work**, ผูก GitHub) **ค้างที่เวอร์ชัน 4 ชม.ก่อน**
  เพราะ Vercel **บล็อก auto-deploy ทุก push** ตั้งแต่ migrate มาบัญชี claude-med:
  เหตุ = Hobby plan ห้าม deploy private repo ที่ commit author ไม่ใช่เจ้าของโปรเจค (author=pongphetpim@gmail.com)
- **ทางแก้ที่ใช้:** สร้างโปรเจคใหม่ชื่อ `prototype` (deploy ไฟล์ตรง ไม่ผูก git → ไม่โดนบล็อก) → URL ด้านบน
- **อัปเดต demo ครั้งหน้า:** แก้ไฟล์ใน `prototype/` แล้วรันมือ → `cd prototype && vercel deploy --prod`
  (โปรเจคใหม่นี้ไม่ auto-deploy จาก git)
- **ค้างไว้แก้ทีหลัง:** ถ้าอยากได้ auto-deploy + URL สวย ต้องจัดการบัญชี (ย้าย pd-monitor มา claude-med
  หรือทำให้ commit author ตรงเจ้าของโปรเจค) — ดู "ทาง 3" ที่คุยไว้

### ▶️ ขั้นถัดไป
- เปิดลิงก์ใหม่ดู demo (ลองค้นหา, กรอกฟอร์มผิดดู error, เข้า job QC กดอนุมัติ/ตีกลับ) → โชว์ CEO เก็บ feedback
- พร้อมเริ่ม **เฟส 1 (D1)**: scaffold Next.js ใน `web/` — แผนที่ `~/.claude/plans/claude-md-shiny-perlis.md`
  + `docs/recommendations.md`

---

## 📅 บันทึกวันที่ 15 มิถุนายน 2569 (วันแรก)

### ✅ วันนี้ทำอะไรไปบ้าง

1. **วางแผนภาพรวมทั้งโปรเจค** — วิเคราะห์จากเอกสาร 2 ไฟล์ (`asset/voice.md` + PDF ผังระบบ)
   สรุปว่าจะทำ **เว็บแอปติดตามการผลิตยา** แทนการจดมือ → แผนเก็บไว้ที่
   `C:\Users\mkt01\.claude\plans\ceo-app-proud-scroll.md`

2. **สร้าง HTML Prototype (Demo)** — ต้นแบบกดเล่นได้จริง ครบ 4 โมดูล responsive ภาษาไทย
   อยู่ในโฟลเดอร์ **`prototype/`** (ยังไม่มี backend จริง ใช้ข้อมูลตัวอย่าง)

3. **สร้าง PDF บทสรุปผู้บริหาร** (6 หน้า) — สำหรับนำเสนอ CEO → `docs/executive-summary.pdf`

4. **สร้าง PDF คู่มือเทคนิคสำหรับทีม** (7 หน้า) — สำหรับน้องๆ ในทีม → `docs/technical-guide.pdf`

5. **แก้บั๊ก prototype** — หน้า board/job/record/daily/dashboard เคยขึ้นว่างเปล่า
   สาเหตุ: บรรทัดเสียใน `assets/mock-data.js` (`const product = SEED ? null : null;`) ทำให้ JS พัง
   → ลบบรรทัดทิ้งแล้ว ทดสอบครบทุกหน้าผ่านหมดแล้ว ✅

6. **ติดตั้ง Vercel CLI** เรียบร้อย (v54.14.0) เตรียม deploy demo ขึ้นออนไลน์

### 📂 ไฟล์ทั้งหมดที่มีตอนนี้

```
web app/
├── note.md                      ← ไฟล์นี้ (บันทึกงาน)
├── asset/                       ← ข้อมูลตั้งต้น (voice.md, PDF, ไฟล์เสียง)
├── prototype/                   ← ⭐ Demo กดเล่นได้ (เปิด index.html)
│   ├── index.html               ← หน้าภาพรวม + ลิงก์เข้าทุกหน้า
│   ├── board.html               ← Pending Order Board
│   ├── job.html                 ← รายละเอียด Job + timeline
│   ├── record.html              ← ฟอร์มบันทึกการผลิต
│   ├── daily.html               ← Daily Report
│   ├── dashboard.html           ← Dashboard / KPI
│   └── assets/                  ← style.css, app.js, mock-data.js
└── docs/                        ← เอกสารนำเสนอ (PDF + HTML ต้นฉบับ)
    ├── executive-summary.pdf    ← บทสรุปผู้บริหาร (ส่ง CEO)
    └── technical-guide.pdf      ← คู่มือเทคนิค (ส่งทีม)
```

### 🧠 สิ่งที่ตัดสินใจไว้แล้ว (เพื่อไม่ต้องคิดซ้ำ)

- **Tech stack:** Next.js (TypeScript) + Tailwind + shadcn/ui · Supabase (PostgreSQL + Auth + Realtime) · Deploy บน Vercel
- **กลยุทธ์:** โชว์ Demo ให้ CEO ก่อน → เก็บ feedback → ค่อยสร้างของจริง
- **วิธีทำงาน:** ใช้ Claude Pro → แบ่งทำทีละเฟส (D1–D9) เพื่อประหยัด token
- **ผู้ใช้จริง:** ใช้ทั้งคอมและมือถือ → ต้องออกแบบ responsive
- **โมดูลใน Demo:** Pending Order Board, บันทึกการผลิต, Daily Report, Dashboard/KPI

---

## ▶️ พรุ่งนี้เริ่มตรงนี้ (Next Steps)

### 🚀 งานค้างอันดับแรก: deploy demo ขึ้น Vercel (ทำต่อจากเมื่อวาน)
Vercel CLI ติดตั้งแล้ว เหลือแค่รัน 3 คำสั่งนี้เอง (ต้อง login บัญชี Vercel เอง):
```bash
cd "C:\Users\mkt01\Desktop\web app\prototype"
vercel login        # ครั้งแรกครั้งเดียว (เลือก GitHub/Google/Email)
vercel --prod       # ตอบ Y / N / ชื่อ project / Enter ตามค่าแนะนำ
```
- ตอนถาม: Set up & deploy → **Y** | Link existing → **N** | project name → `pd-monitor-demo` | directory → **Enter** | modify settings → **N**
- เสร็จแล้วได้ลิงก์ `https://xxx.vercel.app` → ส่งให้ CEO กดจากมือถือได้
- แก้ไฟล์แล้วอยากอัปเดต → รัน `vercel --prod` ซ้ำ
- (ถ้าอยากให้เว็บอัปเดตอัตโนมัติทุกครั้งที่แก้โค้ด ต้องเชื่อมผ่าน GitHub — ค่อยทำทีหลังได้)

### ก่อนเริ่ม ให้ทำ 2 อย่างนี้
1. เปิด `prototype/index.html` ดู Demo อีกรอบให้นึกภาพออก
2. (ถ้ายังไม่ได้ทำ) เอา Demo ไปโชว์ CEO เก็บ feedback ว่าชอบ/อยากปรับอะไร

### แล้วเลือกทางใดทางหนึ่ง

**ทาง A — ถ้า CEO ยังไม่ดู / อยากปรับ Demo ก่อน:**
- ปรับแก้หน้าใน `prototype/` ตาม feedback (สั่ง Claude Code ได้เลย)

**ทาง B — ถ้าพร้อมสร้างของจริงแล้ว → เริ่ม "เฟส 1" (D1):**
- เปิด session ใหม่กับ Claude Code แล้วบอกว่า:
  > "อ่านไฟล์ `note.md` และ `.claude/plans/ceo-app-proud-scroll.md` แล้วเริ่มเฟส 1: ตั้งโปรเจค Next.js + Tailwind + เชื่อม Supabase + deploy เปล่าขึ้น Vercel"
- เป้าหมายเฟส 1 = เปิดเว็บเปล่าบน Vercel ได้สำเร็จ
- สิ่งที่ต้องเตรียม: สมัครบัญชี **GitHub, Supabase, Vercel** (ฟรีทั้งหมด) + ติดตั้ง Node.js, VS Code, Git
  (รายละเอียดอยู่ในหน้า 4 ของ `docs/technical-guide.pdf`)

---

## 🔧 บันทึกเทคนิค (กันลืม)

- **วิธีแปลง HTML → PDF** (ใช้ Chrome headless):
  ```bash
  "/c/Program Files/Google/Chrome/Application/chrome.exe" --headless --disable-gpu \
    --no-pdf-header-footer "--print-to-pdf=<output ไฟล์ Windows path>" "<input.html Windows path>"
  ```
  ⚠️ ต้องใช้ Windows path เต็ม (เช่น `C:\Users\...`) ไม่งั้น Access denied
- ถ้าแก้เนื้อหา PDF → แก้ไฟล์ `.html` ใน `docs/` แล้ว gen ใหม่ด้วยคำสั่งข้างบน
- Roadmap ทั้งหมด (D1–D9) อยู่ในไฟล์แผน `.claude/plans/ceo-app-proud-scroll.md`

---

## 📅 บันทึกวันที่ 2 กรกฎาคม 2569 — ปรับปรุงจาก Dev Log รอบ 13 (ก้อน 1/5): แก้ regression + quick wins

### ที่มา
ผู้ใช้ให้เช็ก Notion "📋 บันทึกความคืบหน้า (Dev Log)" รอบ 13 — เช็กลิสต์ทดสอบ UI มี **1 ข้อยังไม่ผ่าน** (แจ้งเตือนไม่เด้งตอนส่งต่อ/ตีกลับงาน) + ทีมเสนอ **ระบบเพิ่ม 8 ข้อ**
→ ตรวจโค้ดจริง วางแผนแบ่ง 5 ก้อน · แผนเต็ม: `~/.claude/plans/notion-jazzy-raccoon.md`
> **Decisions ผู้ใช้:** ข้อ1 = เรื่องเบิกวัตถุดิบ (จำกัด 3 สถานะ) · ข้อ4 = ผ่อน gate เป็น "อย่างน้อย 1 สถานี" · ลำดับ = ให้ Claude จัดกลุ่ม

### 🔎 เจอ root cause = regression 2 จุด (migration รุ่นหลัง recreate ทับแล้วลืมยกโค้ดเดิมมา)
- **แจ้งเตือนหาย:** `0032` สร้าง `advance_job_status` ใหม่ (เพิ่ม gate QC) แต่ตัดบล็อก `create_notification` ของ `0029` ทิ้ง
- **Job No อัตโนมัติหาย (ข้อ3):** `0031` สร้าง `create_job_with_order` ใหม่ (ผูกสูตร/route) แต่ตัด logic ออกเลขอัตโนมัติของ `0019` ทิ้ง

### ✅ ก้อน 1 — เสร็จ + verified DB + deployed (migration `0034`)
- **`0034_regression_fixes_quickwins.sql`** รวม 5 เรื่อง:
  1. `advance_job_status` — ยกบล็อกแจ้งเตือนของ 0029 กลับมา (คง gate line clearance+inprocess+deviation ครบ)
  2. `inprocess_route_complete` — **[ข้อ4]** ผ่อนเป็น "มี ≥1 สถานีใน route ที่ตรวจผ่าน" (งานไม่มี route = ไม่บล็อก)
  3. `create_job_with_order` — **[ข้อ3]** ยกออกเลขอัตโนมัติของ 0019 กลับมา (คงผูกสูตร active + copy job_routes)
  4. `request_material` — **[ข้อ1]** ขอเบิกได้เฉพาะ pending_announce/planned/in_production (ก่อนส่ง QC)
  5. `request_edit` — **[ข้อ6]** ใบเบิกที่ 'issued' (จ่ายแล้ว) ขอแก้ไขไม่ได้
- **แอป:** `board/[jobNo]/requisitions.tsx` — ซ่อนปุ่ม "ขอแก้ไข" เมื่อใบเบิก issued · (new-job-form รองรับเว้นเลขงานอยู่แล้ว ไม่ต้องแก้)
- verified REST ✅: 5 RPC guard ทำงาน + `inprocess_route_complete` ตอบ 200 · build ผ่าน · push `26aea3b` · Vercel deploy แล้ว

### ▶️ ครั้งหน้าเริ่มตรงนี้
- **เหลือทีมทดสอบ UI จริง ก้อน 1:** (a) QC/QA ส่งต่อ/ตีกลับ → แผนกปลายทางเห็นกระดิ่ง (b) สร้างงานเว้นเลขงาน → ได้ JOB-YYYY-NNNN (c) ตรวจ in-process 1 สถานี → ส่ง QC ได้ (d) เบิกวัตถุดิบตอน qc/qa → บล็อก (e) ใบเบิกจ่ายแล้ว → ไม่มีปุ่มขอแก้ไข
- **ก้อนถัดไป (ตามแผน `notion-jazzy-raccoon.md`):** ก้อน2 = ลบงาน [ข้อ2] (migration 0035 + ยืนยันรหัสผ่าน) · ก้อน3 = สิทธิ์แก้สถานี/เครื่อง [ข้อ8] · ก้อน4 = UI/มือถือ 3 ฟอร์ม [ข้อ7] · ก้อน5 = ผูกสูตรกับบันทึกผลิต+เบิกตาม BOM [ข้อ5]
- migration ล่าสุด = **0034**

---

## 📅 บันทึกวันที่ 2 กรกฎาคม 2569 — ปรับปรุงจาก Dev Log รอบ 13 (ก้อน 2/5): ลบงานในบอร์ด [ข้อ 2]

### ✅ ก้อน 2 — เสร็จ + verified DB + deployed (migration `0035`)
- **`0035_delete_job.sql`** — RPC `delete_job(p_job_id)`:
  - เฉพาะ **manager/admin** · ลบงาน → ตารางลูกทั้งหมดผูก `on delete cascade` ลบตาม (ผลผลิต/ใบเบิก/QC/QA/เคลียร์ไลน์/deviation/FG/route/notification/edit_requests) + ลบ order/batch กำพร้า + audit ("ลบงาน JOB-xxx")
- **แอป:** `board/actions.ts` action `deleteJob(jobId, jobNo, password)` ยืนยันรหัสผ่านซ้ำ (แพตเทิร์นเดียวกับ `signDecision`) · `board/[jobNo]/delete-job-button.tsx` (ปุ่ม 🗑️ + dialog ถามยืนยัน + รหัสผ่าน) · `page.tsx` แสดงเฉพาะ manager/admin (danger zone ใต้การ์ดดำเนินการ)
- verified REST ✅: `delete_job` guard ทำงาน · build ผ่าน · push `009c320` · Vercel deploy
- ⚠️ ลบงานที่จ่ายวัตถุดิบแล้ว = ไม่คืนสต็อกอัตโนมัติ (ปกติงานที่ลบยังไม่ได้จ่าย)

### ▶️ ครั้งหน้าเริ่มตรงนี้
- **เหลือทีมทดสอบ UI ก้อน 2:** login manager/admin → หน้างาน → ปุ่ม "🗑️ ลบงานนี้" → กรอกรหัสผ่าน → งานหายจากบอร์ด · login ฝ่ายอื่น = ไม่เห็นปุ่ม
- **ก้อนถัดไป (ตามแผน `notion-jazzy-raccoon.md`):** ก้อน3 = สิทธิ์แก้สถานี/เครื่อง [ข้อ8] · ก้อน4 = UI/มือถือ 3 ฟอร์ม [ข้อ7] · ก้อน5 = ผูกสูตรกับบันทึกผลิต+เบิกตาม BOM [ข้อ5]
- migration ล่าสุด = **0035**

---

## 📅 บันทึกวันที่ 2 กรกฎาคม 2569 — ปรับปรุงจาก Dev Log รอบ 13 (ก้อน 3/5): สิทธิ์แก้ สถานี/เครื่องจักร [ข้อ 8]

### ✅ ก้อน 3 — เสร็จ + verified DB + deployed (migration `0036`)
- **`0036_edit_station_machine.sql`** — ให้ manager/admin ขอแก้ "สถานี/เครื่องจักร" ผ่านระบบ Amendment เดิม:
  - `request_edit` — เพิ่ม `station_id` ใน whitelist ของ in-process QC (คง guard ใบเบิก issued จาก 0034)
  - `review_edit_request` — branch in-process: set `station_id` + **อัปเดต enum `station`(กลุ่ม) ควบอัตโนมัติ** จาก `stations.station_group` (กัน dashboard เพี้ยน) · production record (station/machine) apply ได้อยู่แล้วจาก 0033
  - หมายเหตุ: `EditRequestButton` รองรับ field `select`+options + review apply station/machine ของ production record มีมาก่อนแล้ว — งานจริงจึงเหลือ in-process + เปิด field ใน UI
- **แอป:** `production.ts`+`station-constants.ts` เพิ่ม `machine_id` ในแถวบันทึกผลผลิต (prefill) · `page.tsx` เพิ่ม field สถานี+เครื่องจักรในปุ่มขอแก้บันทึกผลผลิต (เห็นเฉพาะ manager/admin) + โหลด machines ให้ editor · `quality-checks.tsx` เพิ่ม field สถานีในปุ่มขอแก้ in-process (manager/admin, เลือกจาก route)
- verified REST ✅: request_edit/review_edit_request guard ทำงาน · build ผ่าน · push `32e1710` · deploy

### ▶️ ครั้งหน้าเริ่มตรงนี้
- **เหลือทีมทดสอบ UI ก้อน 3:** login manager → หน้างาน → บันทึกผลผลิต/ผลตรวจ QC กด "✏️ ขอแก้ไข" → มี field สถานี(+เครื่องจักร) ให้เลือก → อนุมัติที่ `/edit-requests` → ค่าจริงเปลี่ยน + dashboard ยังรวมกลุ่มถูก · login ฝ่ายอื่น = ไม่เห็น field สถานี/เครื่อง
- **ก้อนถัดไป (ตามแผน `notion-jazzy-raccoon.md`):** ก้อน4 = UI/มือถือ 3 ฟอร์ม [ข้อ7] · ก้อน5 = ผูกสูตรกับบันทึกผลิต+เบิกตาม BOM [ข้อ5]
- migration ล่าสุด = **0036**

---

## 📅 บันทึกวันที่ 2 กรกฎาคม 2569 — ปรับปรุงจาก Dev Log รอบ 13 (ก้อน 4/5): ปรับ UI/มือถือ [ข้อ 7]

### ✅ ก้อน 4 — เสร็จ + deployed (frontend ล้วน ไม่มี migration)
- **ปัญหา:** ตารางในหน้างานบังคับ `min-w-[...]` → มือถือเลื่อนแนวนอน เห็นข้อมูลไม่ครบ
- **แก้:** ทุกตาราง = **มือถือแสดงเป็นการ์ด (เห็นครบทุกช่องในใบเดียว) · จอกว้าง (md:) แสดงเป็นตารางเหมือนเดิม**
  - `page.tsx` บันทึกผลผลิตรายวัน · `requisitions.tsx` เบิกวัตถุดิบ · `quality-checks.tsx` in-process QC + QA sample
  - แยก helper กันเขียนซ้ำระหว่างการ์ด/ตาราง: `productionEditFields` (page) · `rowActions` (requisitions) · `checkEditButton`/`resultBadge`/`showStation` (quality-checks)
- build ผ่าน · push `32650d6` · deploy

### ▶️ ครั้งหน้าเริ่มตรงนี้
- **เหลือทีมทดสอบ UI ก้อน 4:** เปิดหน้างานบนมือถือ (จอแคบ) → บันทึกผลผลิต/เบิกวัตถุดิบ/ตรวจ QC แสดงเป็นการ์ดเห็นครบ ไม่ต้องเลื่อนซ้ายขวา · จอคอม = ตารางเหมือนเดิม
- **ก้อนสุดท้าย (ก้อน 5, ตามแผน `notion-jazzy-raccoon.md`):** ผูกสูตรกับบันทึกผลิต + เบิกตาม BOM [ข้อ5] — ก้อนใหญ่สุด (migration 0037: production_records.station_id + add_production_record รับ station_id map enum · ฟอร์มเลือกสถานีตาม route · เบิกกรองเฉพาะวัตถุดิบใน BOM ของสูตร)
- migration ล่าสุด = **0036** (ก้อน 4 ไม่มี migration)

---

## 📅 บันทึกวันที่ 2 กรกฎาคม 2569 — ปรับปรุงจาก Dev Log รอบ 13 (ก้อน 5/5): ผูกสูตรกับบันทึกผลิต + เบิกตาม BOM [ข้อ 5] — จบครบ 🎉

### ✅ ก้อน 5 — เสร็จ + verified DB + deployed (migration `0037`)
- **`0037_recipe_station_bom.sql`:**
  - `production_records.station_id` (FK stations) + `add_production_record` รับ `p_station_id` → lookup `station_group` → set ทั้ง station_id + enum station ควบ (**คง dashboard/รายงาน rollup, ไม่แตะ enum**) · drop signature เดิม (รับ production_station)
  - `request_edit` + `review_edit_request` — แก้ `station_id` ของบันทึกผลผลิตได้ (set enum group ควบ) · คง inprocess station_id (0036) + guard ใบเบิก issued (0034)
- **แอป:**
  - (ก) `record-form.tsx` เลือก**สถานีตาม route ของสูตร** (ไม่มี route → ทุกสถานี active) · กรองเครื่องจักรตามกลุ่มของสถานี · `RecordFormValues.station`→`station_id` · บันทึกผลผลิตแสดง**ชื่อสถานีย่อยจริง** (`production.ts` join + `station_name`, fallback ชื่อกลุ่ม)
  - (ข) `requisitions` เบิกเฉพาะวัตถุดิบใน **BOM ของสูตร** (`getRecipeMaterialIds` → `getSelectableLots(ids)`) + ป้าย "แสดงเฉพาะวัตถุดิบตามสูตร"
  - ปุ่มขอแก้บันทึกผลผลิต (manager/admin) แก้สถานี = **สถานีย่อย (station_id)** แล้ว
- verified REST ✅: add_production_record สัญญาใหม่ + column station_id + request_edit guard · build ผ่าน · push `7821263` · deploy

### 🎉 ปรับปรุงจาก Dev Log รอบ 13 — เสร็จครบ 5 ก้อน (บั๊กแจ้งเตือน + ข้อเสนอ 8 ข้อ)
- ก้อน1 regression แจ้งเตือน+Job No + ข้อ1,4,6 (0034) · ก้อน2 ลบงาน ข้อ2 (0035) · ก้อน3 สิทธิ์แก้สถานี/เครื่อง ข้อ8 (0036) · ก้อน4 UI มือถือ ข้อ7 · ก้อน5 ผูกสูตร+BOM ข้อ5 (0037)

### ▶️ ครั้งหน้าเริ่มตรงนี้
- **เหลือทีมทดสอบ UI ทั้ง 5 ก้อนบนเว็บจริง** (https://pd-monitor.vercel.app) — ดูเช็กลิสต์ในแต่ละบล็อกด้านบน
- ก้อน 5: สร้าง/เปิดงานที่มี route → ฟอร์มบันทึกผลผลิตโชว์สถานีย่อยตามสูตร + dashboard/DL cost ยังรวมกลุ่มถูก · ฟอร์มเบิกโชว์เฉพาะวัตถุดิบในสูตร
- (ออปชัน) โพสต์สรุปกลับ Notion Dev Log · หรือทำ MES ที่เหลือ (B5 Barcode/QR · B6 OEE · B7 capacity · B8 integration)
- migration ล่าสุด = **0037**

---

## 📌 สถานะปัจจุบัน
**🆕 (28 ส.ค. 69) Part D เสร็จครบ 4 ก้อน ✅ — migration ล่าสุด `0073` (paste + verify ครบแล้ว)** —
  **ลักษณะยา** ช่องใหม่ในทะเบียนผลิตภัณฑ์ + แสดงในหน้ารายละเอียดงาน (อ่านอย่างเดียว) ·
  **บริษัท UMEDA / POUND** เลือกตอนสร้างงาน · POUND มีช่อง "หมายเหตุ" · **เลข Job No. เดินแยกชุดต่อบริษัท** ·
  🔑 เก็บใน DB มีอักษรนำ (`690001` / `P690001`) แต่**หน้าจอแสดงเลขเปล่า + ป้ายบริษัท** → งานเก่า 10 ใบไม่ต้องแตะ ·
  **หน้าใหม่ "บริษัท / เลขงาน"** (ผู้บริหาร) ตั้งเลขถัดไป + เลขตั้งต้นปีใหม่ · ตั้งย้อนหลังถูกปฏิเสธ ·
  🐞 **Incident Case: ถอดแผนกแล้วปิดเคสไม่ได้** — ต้นเหตุคือบันทึกการมอบหมายทีไรก็ดันสถานะกลับเป็น "ส่งแผนกแก้ไข"
  (ไม่ต้องถอดแผนกด้วยซ้ำ) → แก้ให้นับแผนกที่ค้างใหม่แล้วเลื่อนขั้นเอง ·
  **ปุ่มเดียวต่อบทบาท** — QA เห็น "ตรวจสอบผลแก้ไข" · แผนกเห็น "รายงานผลแก้ไข" (คลิกเดียวส่ง QA) · คนอื่นไม่มีปุ่ม ·
  **ป้ายฝ่ายนับ role สูงสุด** (`current_role_badge()` แยกจาก `current_role_group()` ที่ใช้จับคู่แผนก — ห้ามรวมกัน) ·
  🐞 **หัวหน้า QC อนุมัติคำขอแก้ผลตรวจ in-process ได้แล้ว** (เดิมยื่นได้แต่ไม่มีใครในสายงานอนุมัติได้) ·
  **ลบคอลัมน์ `sample_point`** — ประวัติไม่หาย `audit_log` เก็บค่าเดิมไว้ 19 แถว ·
  🎉 **role ครบทุกฝ่ายแล้ว** (qa 4 · qc_lead 5 · production_lead 5) — คำเตือนที่ค้างจาก Part C.3/C.4 ปิดได้ ·
  📌 **เหลือทำเอง 2 อย่าง:** ซ่อมเคส `022` (กด "แก้ไขการมอบหมาย" ซ้ำ) · ตั้งเลขเริ่มต้น 2 บริษัท ·
  🔜 **ถัดไป: Part D อีก 2 หมวดที่ยกไว้** — ปริ้นใบแจ้งผลิต F.PLN.01 · ปริ้นตารางบอร์ดงาน 12 คอลัมน์
**(27 ส.ค. 69 รอบบ่าย) Part C.4 เสร็จครบ 6 ก้อน ✅ — migration ล่าสุด `0069` (paste + verify ครบแล้ว)** —
  🐞 **ซ่อม `/trace` ที่พังเฉพาะงานที่มีรายการเบิก** (เรียกฟังก์ชันจากไฟล์ `"use client"` — ไม่เกี่ยวกับสิทธิ์) ·
  **เพิ่ม error boundary ให้ทั้งแอป** (เดิมไม่มีเลยสักไฟล์ พังทีไรได้จอเปล่า) ·
  🐞 **หัวหน้า QC กดอนุมัติผลตรวจได้แล้ว** (บั๊ก cast enum ตั้งแต่ 0064 — บันทึกไม่ได้เลยทั้งรอบ) ·
  **แก้ Valid date ได้** · **ปุ่มขอแก้ไขผลตรวจเห็นเฉพาะ QC** (ฝ่ายผลิตต้องไม่มี — กันที่ DB ด้วย) ·
  **จุดเก็บตัวอย่าง → "ตรวจ Finished product"**: กรอกวันที่+เวลาเอง · มีผล ผ่าน/ไม่ผ่าน · QA แก้/ลบได้ (soft delete + บังคับเหตุผล) ·
  **Deviation → Incident Case** flow ใหม่ 5 ขั้น: เปิด → QA ตรวจสอบ → ส่งแผนกแก้ → ส่งกลับ QA → QA อนุมัติ ·
  QA คัดแยก **DEV/OOS/NC + เลขที่ (พิมพ์เอง)** + ติ๊ก **แผนกรับผิดชอบได้หลายแผนก** (WHS/QC/PRD/ENG) แล้วแจ้งเตือนทุกแผนก ·
  **ต้องส่งกลับครบทุกแผนก** ถึงเข้า "รอ QA อนุมัติ" (QA ถอดแผนกที่ไม่ตอบ หรือปิดข้ามขั้นพร้อมเหตุผลได้) ·
  **เปิดเคสอัตโนมัติ** เมื่อ in-process ไม่อนุมัติ / ตัวอย่าง Finished product ไม่ผ่าน (กันซ้ำ 2 ชั้น) ·
  🐞 แถม: **`current_role_group()` เคย tag หมายเหตุของ admin เป็น "ฝ่ายผลิต"** + 5 ฝ่ายคอมเมนต์ไม่ได้เลย → แก้แล้ว ·
  🚨 **ต้องมีคนถือ role `qa` จริง** ไม่งั้นเคสค้าง "รอ QA ตรวจสอบ" ตลอดไป **และปล่อย FG ไม่ได้** ·
  ⚠️ **ด่าน GMP เข้มขึ้น** — ผลตรวจไม่อนุมัติ/ตัวอย่างไม่ผ่าน บล็อก `qa → FG` ทันที ·
  🔜 **ถัดไป: ทีมทดสอบ UI ตามเช็กลิสต์ 6 หมวดในหน้า Notion "Part C.4 claude"**
**(27 ส.ค. 69) Part C.3 เสร็จครบ 6 ก้อน ✅ — migration `0064`** —
  **เลิกใช้ "กลุ่มหลัก" (enum `production_station`) ทั้งระบบ** ใช้ชื่อสถานีจริง 9 ตัวอย่างเดียว ·
  **เครื่องจักรผูกสถานีจริง** + ล้างของเก่า seed ใหม่ 18 เครื่อง (สถานีละ 2) ·
  **หน้ารายละเอียดงานแบ่งเป็นแท็บตามขั้นตอน (route)** — LC / บันทึกผลผลิต / ตรวจ QC แยกกันทุกขั้นตอน ·
  แต่ละขั้นตอน**ผูกเครื่องจักรได้หลายเครื่อง** (การ์ดรายละเอียดอ่านอย่างเดียว) ·
  **Line Clearance = 1 ใบต่อ ขั้นตอน × เครื่อง** · ผู้ยืนยัน = **หัวหน้าฝ่ายผลิต** · ติ๊กอย่างน้อย 1 ข้อพอ ·
  **ย้ายด่าน LC** จากตอนกด "เริ่มผลิต" ไปกั้นที่การบันทึกผลผลิตรายสถานี/เครื่อง ·
  **บันทึกผลผลิต:** กะ · OT · **นาที** (แทนชั่วโมง) · "ยอดที่ต้องการ" · หน่วยแยกต่อช่อง · คอลัมน์ **สถานะ QC + ปุ่ม 🔬 QC** ·
  **In-process QC:** Valid date + **ขั้นตอนรออนุมัติจากหัวหน้า QC** (สองลายเซ็น) · ด่านส่ง QC นับเฉพาะผลที่อนุมัติแล้ว ·
  **role ใหม่ 2 ตัว:** `production_lead` (หัวหน้าฝ่ายผลิต) · `qc_lead` (หัวหน้า QC) ·
  🚨 **ต้องตั้ง 2 role นี้ให้คนจริงที่หน้า Admin ก่อนใช้งาน** ไม่งั้นยืนยัน LC / อนุมัติ QC ไม่ได้ → งานเดินต่อไม่ได้ ·
  ⚠️ ด่าน LC ไม่กั้นถ้าขั้นตอนนั้นยังไม่ผูกเครื่องจักรเลย (กันล็อกตาย) ·
  🔜 **ก้อนถัดไป = Part C.4** (trace บั๊ก · จุดเก็บตัวอย่าง · Deviation flow ใหม่) — มีใน Notion แล้ว
**(21 ส.ค. 69) Part C.2 เบิกวัตถุดิบ/บรรจุภัณฑ์ เสร็จครบ 4 ก้อน ✅** —
  **เลิกผูกการเบิกกับล็อตในคลัง** เปลี่ยนเป็นรายการจดมือ (ชื่อ · RM/PM · จำนวน+หน่วย · หมายเหตุ · พร้อม/ไม่พร้อม) ·
  **ฝ่ายผลิตเพิ่ม/แก้/ลบ · ฝ่ายคลังคลิกป้ายสถานะเปลี่ยนได้ทันทีบนการ์ด** (กั้นสิทธิ์ที่ DB ด้วยการแยก RPC ไม่ใช่ซ่อนปุ่ม) ·
  แก้ข้อมูลหลังคลังกด "พร้อม" → **เด้งกลับเป็น "ไม่พร้อม" เอง** (แก้แค่หมายเหตุไม่เด้ง) ·
  **เมนูใหม่ "เบิกวัตถุดิบ / บรรจุภัณฑ์"** รวมของค้างทุกงานให้ฝ่ายคลังกดรวดเดียว ·
  สถานะ **ไม่กั้น** การเดินสถานะงาน แค่เตือน ·
  **migration ล่าสุด = 0057** (paste + verify ครบทั้ง 2 ไฟล์) ·
  ⚠️ **ไม่มีระบบใดตัดสต็อกคลังอัตโนมัติแล้ว** · ⚠️ **`/trace` ค้นจากเลขล็อตวัตถุดิบไม่ได้อีก** ·
  🔜 **ก้อนถัดไป = Line Clearance** (อยู่ในหน้า Notion Part C.2 เดียวกัน ผู้ใช้สั่งให้ยกไปรอบหน้า)
**(20 ส.ค. 69) Part C หน้ารายละเอียดงาน เสร็จครบ 3 ก้อน ✅** —
  **แก้ข้อมูลงานได้แล้วตามสิทธิ์แต่ละฝ่าย** (ปุ่ม "✏️ แก้ไขข้อมูล" เดียว บันทึกทีเดียว · ช่องที่แก้ไม่ได้ขึ้น 🔒 พร้อมบอกเหตุผล) ·
  **Status = ทะเบียน 51 ค่า** เลือกจาก dropdown + ช่องค้นหา + ปุ่ม ⚙ จัดการ (ทุกฝ่ายเพิ่ม/แก้/ลบเองได้ · แก้ชื่อแล้วงานเก่าเปลี่ยนตาม) ·
  **"มีแผน" ผูกเดือน/ปีได้** → โชว์เป็น `มีแผน08/26` (เก็บแยกคอลัมน์ ทำรายงานตามเดือนได้จริง) ·
  ชื่อช่องตรงใบงานจริง: **Batch Size · Job No. (เลขล้วน) · Reg No. · กำหนดส่ง · Packing Size (1)(2)(3)** ·
  ผู้บริหารข้ามด่านล็อกได้แต่ต้องกรอกเหตุผลลง Audit · **LOT No./วันผลิต/วันหมดอายุ ยังใช้ด่าน GMP เดิม ไม่มีใครข้ามได้** ·
  **migration ล่าสุด = 0054** (paste + verify REST ครบ · push แล้ว) ·
  ⚠️ **เหลือทดสอบ UI 15 ข้อ** (หมวดสิทธิ์ + ด่านล็อก) — เช็กลิสต์กดติ๊กได้ใน Notion "🍉 part C claude เเก้" ·
  📋 **Part C.2 รออยู่แล้วใน Notion** (เรื่องเบิกผลิตภัณฑ์)
**(19 ส.ค. 69) Part 3 ก้อน 3–5 + Part 3.1 ✅ (ไม่มีบันทึกรายวัน — สรุปจาก git log)** —
  ฝ่ายผลิตกรอก LOT No. + ด่านห้ามเริ่มผลิตถ้าไม่มีเลขล็อต (`0049`) · ลบผลิตภัณฑ์ถาวร (`0050`) ·
  ฝ่ายวางแผนปลดสถานะล็อตเอง (`0051`) · หน้าสร้างงานตัดช่อง Status + ล็อกหน่วยตามทะเบียนยา · หน้าเครื่องจักรกดเปลี่ยนสถานะบนการ์ด (`0052`)
**(18 ส.ค. 69) Part 3 ก้อน 1–2 เสร็จ (จาก 5 ก้อน) ✅** —
  **ทะเบียนลูกค้า** (เลือกจาก dropdown แทนพิมพ์อิสระ · แก้ชื่อแล้ว sync งานเก่าให้ · ลบต้องย้ายงานก่อน) ·
  **JOB NO. เลข 6 หลักแบบโรงงาน** `690001` (ปี พ.ศ. + running reset รายปี · ระบบออกให้เอง กรอกไม่ได้) ·
  **สร้างงานทีเดียว 1–50 ใบ** จากข้อมูลชุดเดียว · ช่องใหม่ **ใบคำขอ / C.P.O DATE / Status** ·
  ตัดช่องเลขล็อต+แผนเริ่ม-เสร็จออกจากหน้าสร้างงาน · ตัด "กระปุก" ·
  **🧹 ล้างข้อมูลทดสอบหมดแล้ว** (งาน · ใบสั่ง · ล็อตการผลิต · **ล็อตคลัง · สต็อก FG** = 0) — ทะเบียนยา/สถานี/ลูกค้า/ผู้ใช้ยังอยู่ครบ ·
  **migration ล่าสุด = 0048** (paste + verify REST ครบ) · ⚠️ **ยังไม่มีใครทดสอบ UI จริง** ·
  ⚠️ **ฝ่ายคลังต้องคีย์ล็อตของจริงเข้าใหม่ก่อนใช้งาน** ·
  🔜 ก้อน 3 = ฝ่ายผลิตกรอก LOT No. (ตอนนี้สร้างงานแล้วยังไม่มีที่กรอกเลขล็อต) ·
  บันทึกให้ทีมอ่าน: Notion "🍉 Part B.2 claude เเก้"
**(17 ส.ค. 69) Part 2.1 แก้ตามผลทดสอบ UI จริง เสร็จครบ 3 ก้อน ✅** —
  **ปุ่ม "ลบ" ลบจริงแล้ว** (ติดว่ามีคนใช้ → เปลี่ยนเป็นปิดใช้งานให้เอง พร้อมบอกเหตุผล) · **ฝ่ายคลังจัดการผลิตภัณฑ์ได้** (route ยังเป็นผู้บริหาร) ·
  **เลิกใช้ช่อง "ประเภท" ทั้งระบบ** (ผลข้างเคียง: dropdown สร้างงานเห็นผลิตภัณฑ์ทุกตัว) · ลำดับสถานีเพิ่มทีละ 1 ·
  **อุดช่องโหว่ GMP:** ยาไม่มีขั้นตอนการผลิต = สร้างงานไม่ได้ + ปุ่มซ่อมงานเก่า + ตัดการโชว์สถานีทั้งหมด ·
  **สถานะล็อตเหลือ 2 ค่า** (พร้อมใช้/ไม่พร้อมใช้) เฉพาะฝ่ายวางแผนตั้งได้ · หน้าเบิก dropdown หุบได้ + ไม่ล้างคำค้น ·
  **migration ล่าสุด = 0046** (paste + verify REST ครบ) · Vercel deploy Ready · ⚠️ **ยังไม่มีใครทดสอบ UI จริง** ·
  บันทึกให้ทีมอ่าน: Notion "🖐🏼 claude part B เเก้ไขเพิ่มเติม"
**(14 ส.ค. 69) Part 2 เสร็จครบ 4 ก้อน ✅** — **เลิกใช้ BOM ทั้งระบบ** · ทะเบียนเดียวรวม ยา/วัตถุดิบ/บรรจุภัณฑ์ (แยกด้วยช่อง "ประเภท" — **ตัดออกแล้วใน Part 2.1**) ·
  รูปแบบบรรจุย้ายมาอยู่ที่งานผลิต (สูงสุด 3 ขนาด) · สถานีการผลิต 9 ตัวจริง · ปุ่ม "ลบ" = ปิดใช้งาน (ประวัติ GMP ไม่หาย) · ช่องค้นหาล็อตในระบบเบิก ·
  **migration ล่าสุด = 0043** (paste + verify REST ครบทุกไฟล์) · ⚠️ **ยังไม่มีใครทดสอบ UI จริง** · เอกสาร feedback ต้นฉบับอยู่ `docs/feedback-raw-2026-08.md`
**(11 ส.ค. 69) Part A ตั้งค่าระบบ (feedback ทีม) ✅** — role ใหม่ 3 ตัว **PLN (วางแผน) · COST (บัญชีต้นทุน) · ENG (วิศวกรรม)** ·
  หน้าจัดการผู้ใช้มีแถบนับบัญชีต่อฝ่าย + ตารางอธิบายสิทธิ์ · หน้าเครื่องจักรมีช่อง "ห้อง" + ฝ่ายผลิตเพิ่มเครื่องเองได้ (ช่องซ่อม/สอบเทียบสงวนให้วิศวกรรม) ·
  **migration ล่าสุด = 0039** (paste + verify REST แล้ว) · เหลือทีมทดสอบ UI จริง
**(3 ก.ค. 69) แก้ 3 ปัญหา UI จากการใช้จริง ✅** — ฟอร์ม "ขอแก้ไข" เป็น modal (ไม่ถูกบีบบน PC) ·
  sidebar+ปุ่ม logout ค้างในจอ (sticky) · Audit 200→30/หน้า + แบ่งหน้า · frontend ล้วน ไม่มี migration (ล่าสุดยัง 0037) · build ผ่าน
**เฟส 0 (Demo + เอกสาร) = เสร็จแล้ว ✅** | prototype แก้บั๊กครบแล้ว ทดสอบผ่านทุกหน้า
**ขึ้น GitHub แล้ว ✅ (19 มิ.ย. 69 — บัญชีใหม่)** → 🔗 https://github.com/Claude-med/pd-monitor (Private)
  - บัญชี GitHub: `Claude-med` · branch `master`
**Deploy ขึ้น Vercel แล้ว ✅ (19 มิ.ย. 69 — บัญชีใหม่)** → 🔗 https://pd-monitor.vercel.app
  - บัญชี Vercel: `claude-med` · project: `pd-monitor` · Root Directory = `prototype`
**เชื่อม Vercel ↔ GitHub แล้ว ✅ = Auto-deploy**
  - อัปเดตเว็บครั้งหน้า: แก้ไฟล์ → `git add -A` → `git commit -m "..."` → `git push`
    แล้ว Vercel จะ build + อัปเดต https://pd-monitor.vercel.app ให้เองอัตโนมัติ (ลิงก์เดิมไม่เปลี่ยน)
  - ไม่ต้องรัน `vercel --prod` เองอีกแล้ว
**Supabase พร้อมแล้ว ✅ (19 มิ.ย. 69)** — project `pd-monitor` · region Singapore
  - URL + key เก็บไว้ใน `.env.local` (ที่รากโปรเจกต์ — ไม่ขึ้น GitHub)
  - key ใช้จริงตอนเริ่มเฟส 1 (Next.js)
**ไฟล์ตั้งต้น + คำแนะนำคุณภาพ พร้อมแล้ว ✅ (19 มิ.ย. 69)** — `CLAUDE.md`, `docs/recommendations.md`,
  `docs/requirements-from-notion.md` (เชื่อม Notion ทีม), skill `handoff`
**Prototype อัปเกรดตามคำแนะนำ + 10 ฟีเจอร์ทีมแล้ว ✅ (19 มิ.ย. 69)** — search/filter, สถานะปัญหา,
  validation, flow guard, activity log, QC/QA approve-reject (demo-level)
**Demo เวอร์ชันใหม่ LIVE แล้ว ✅** → 🔗 **https://prototype-omega-eight.vercel.app** (โปรเจค Vercel `prototype`, deploy ตรงไม่ผูก git)
**เฟส 1 / D1 = scaffold เสร็จแล้ว ✅ (19 มิ.ย. 69)** — แอปจริง Next.js 16 + Tailwind + shadcn/ui + Supabase client
  อยู่ในโฟลเดอร์ `web/` · build + dev ผ่าน · push ขึ้น GitHub แล้ว (`f0573da`, repo เป็น public แล้ว)
**🌐 แอปจริง LIVE แล้ว ✅ (19 มิ.ย. 69):** **https://pd-monitor.vercel.app** ขึ้นหน้า "เฟส 1 — โครงแอปพร้อมแล้ว"
  - auto-deploy กลับมาทำงาน! (push → Vercel build เอง) — แก้ได้ด้วยการปรับ repo เป็น public
  - Vercel: Root Directory = `web` · env 3 ตัวใส่ครบ · deploy by claude-med · Status Ready
  - หน้าตอนนี้ "เปล่าๆ" = ปกติ/ตั้งใจ (D1 แค่พิสูจน์ว่า deploy ครบวงจร — ฟีเจอร์เริ่ม D2)
**= เฟส 1 / D1 เสร็จสมบูรณ์ทุกข้อ ✅**
**เฟส 2 / D2 = เสร็จสมบูรณ์ + verify แล้ว ✅ (22 มิ.ย. 69)** — schema ลง Supabase จริงเรียบร้อย
  ตรวจผ่าน REST: 8 ตารางครบ · seed เข้าครบ · audit_log=24 (trigger บันทึกครบ) · RLS กัน anon ได้ (`[]`)
  · CHECK output≤input ปฏิเสธ (400) · audit_log append-only กัน UPDATE ได้จริง
**เฟส 3 / D3 = เสร็จสมบูรณ์ + verify แล้ว ✅ (22 มิ.ย. 69)** — login + role + PWA + app shell (responsive)
  · Next.js 16: `middleware`→`proxy` · build ผ่าน · `/`→307 /login
  · **บั๊กที่แก้:** trigger `set_row_meta` อ้าง `created_by` แต่ profiles ไม่มี → เพิ่ม `set_profile_meta()`
    (patch `0005_fix_auth_roles_meta.sql` — รันไฟล์เดียว ไม่ต้อง reset)
  · ผู้ใช้รัน 0005 + สร้างบัญชี Auth แล้ว · ตรวจผ่าน REST: email เข้าครบ · auto-link ทำงาน · has_role()/current_profile_id() OK
  · บัญชีทดสอบ `pongphetpim@gmail.com` = role manager (เห็นทุกเมนู)
**เฟส 4 / D4 = เสร็จสมบูรณ์ + verified ✅ (22 มิ.ย. 69)** — บอร์ดงาน + รายละเอียด job + กันข้ามสถานะ
  · kanban responsive + search/filter + KPI · stepper + ปุ่มเปลี่ยนสถานะตาม role (ตีกลับมีเหตุผล)
  · ผู้ใช้ paste 0006 แล้ว · ทดสอบ state machine ในฐานะ production: กันผิดสิทธิ์ ✓ · กันข้ามขั้น ✓
    · valid transition สำเร็จ + version bump + **audit บันทึกใคร/เหตุผล** ✓
  · 📌 ผู้ใช้เปลี่ยนอีเมล demo profiles → `test1-5@gmail.com` + สร้างบัญชี auth จริงครบ 5 แผนกแล้ว
  · 📌 มีโปรไฟล์ว่าง `somchai.prod@pdmonitor.app` หลงจาก test (ไม่มี role/ไม่ผูก = ไม่กระทบ) ลบได้ถ้าอยาก
**เฟส 5 / D5 = เสร็จสมบูรณ์ + verified ✅ (22 มิ.ย. 69)** — ฟอร์มบันทึกผลผลิตรายวัน + validation ฝั่ง server + audit
  · ฟังก์ชัน `add_production_record()` (security definer): สิทธิ์ production/manager · guard สถานะ in_production/qc/qa
    · output≤input · output+loss≤input · ห้ามติดลบ · ทศนิยม · ชม. 0–24 · วันที่ล้ำอนาคตไม่ได้ · ตั้ง audit GUC
  · `validateRecord()` ใช้ร่วม client/server · ฟอร์ม decimal + ตารางบันทึกในหน้า job detail
  · ผู้ใช้ paste 0007 แล้ว · ทดสอบจาก UI (login จริง): บันทึก JOB-001 input1000/output980/loss20 สำเร็จ
    · ทศนิยมเก็บถูก · operator_id = บัญชีที่ login จริง · **audit เก็บ changed_by + reason "บันทึกผลผลิต prep"** ✓
**เฟส 6 / D6 = เสร็จสมบูรณ์ ✅ (22 มิ.ย. 69)** — แบ่ง 2 ส่วน
  · **ส่วน 1 E-signature (lite)** — `0008_approvals.sql` (ตาราง approvals + `sign_job_decision()`) ·
    ยืนยันรหัสผ่านซ้ำก่อนลงนาม · บันทึกลายเซ็น + ขยับสถานะ atomic · **verified จาก UI** (login จริง):
    QC ตีกลับ (มีเหตุผล "test") + QC อนุมัติ บน JOB-001 · audit เก็บผู้ลงนาม "ลงนาม QC — อนุมัติ" ✓
  · **ส่วน 2 Daily Report** — หน้า `/daily` สรุปผลผลิตรายวันทุกงาน + กรองวันที่ + ยอดรวม + คลิกเข้างานได้
    (อ่านข้อมูล D5 ไม่ต้อง paste DB เพิ่ม · build ผ่าน · ⏳ ยังไม่ทดสอบ UI)
**เฟส 7 / D7 = เสร็จสมบูรณ์ ✅ (22 มิ.ย. 69)** — Dashboard / KPI + ต้นทุนค่าแรง (DL cost)
  · `lib/data/dashboard.ts` (`getDashboardData`) + ยกเครื่องหน้า `/` เป็นแดชบอร์ดผู้บริหาร
  · ช่วงวันที่ + KPI ผลผลิต/Yield%/ชม. · **ส่วน DL cost เฉพาะ manager** (ปรับ rate ได้ + แยกสถานี)
  · ไม่ต้อง paste DB เพิ่ม (อ่านข้อมูล D5) · build ผ่าน · ⏳ ยังไม่ทดสอบ UI
  · 📌 อัตราค่าแรงยังไม่เก็บถาวร (default 60 ฿/ชม. + URL param) — ต่อยอดเป็น `app_settings` ได้ภายหลัง
**เฟส 8 / D8 = กำลังทำ (แบ่งก้อน) — ส่วน 1 Realtime เสร็จ ✅ (22 มิ.ย. 69)**
  · `components/realtime-refresh.tsx` ฟัง Supabase Realtime → `router.refresh()` (server-first คงเดิม) + ป้าย "อัปเดตสด"
  · ฝัง 4 หน้า (บอร์ด/แดชบอร์ด/รายงานประจำวัน/รายละเอียดงาน) · build ผ่าน
  · ✅ **paste `0009_realtime.sql` แล้ว** (22 มิ.ย. 69 — เปิด publication 3 ตาราง) · ⏳ ยังไม่ verify UI 2 จอ
  · **ส่วน 2 = กำลังทำ (แบ่ง 4 ก้อน):**
    - **ก้อน 1 เทส RLS (B4) = verified ✅ (23 มิ.ย. 69)** — `web/supabase/tests/rls_impersonation_test.sql`
      (ฟังก์ชัน `pg_temp.rls_test()` return ตาราง · raise undo ไม่แตะข้อมูล · **ผ่าน 27/27 บน DB จริง**)
    - **ก้อน 2 Offline-resilient save (C1) = verified ✅ (23 มิ.ย. 69)** — `0010_record_idempotency.sql` (client_id idempotency)
      + `lib/offline-queue.ts` + ยกเครื่อง `record-form.tsx` (auto-retry/backoff · คิว localStorage · banner กู้ค้าง · online retry)
      · **ทดสอบ UI จริง: บันทึกปกติ + offline retry ไม่มีแถวซ้ำ ผ่าน**
    - **ก้อน 3 ขัดเกลามือถือ เสร็จ ✅ (23 มิ.ย. 69)** — `globals.css` (iOS zoom fix + touch 44px เฉพาะ pointer:coarse)
      + app-shell padding · build ผ่าน · ไม่ต้อง paste DB · ⏳ รอเทสบนมือถือ (ออปชัน)
    - **ก้อน 4 perf = เลื่อน (ตั้งใจ)** — ข้อมูลยังน้อย วัดไม่ได้ + index หลักมีครบ → ทำตอนข้อมูลเยอะ (Backlog)
  · **= D8 ปิดแล้ว (hardening สำคัญครบ)**
**เฟส 9 / D9 = กำลังทำ (23 มิ.ย. 69):**
  · **ก้อน 1 หน้าสร้างงานผลิตใหม่ = verified ✅** — `0011_create_job.sql` (`create_product`+`create_job_with_order`, manager เท่านั้น)
    + `/board/new` (ฟอร์ม + เพิ่มยา inline) + ปุ่มบนบอร์ด · **ทดสอบ UI จริงผ่าน** (ปิด gap requirement ข้อ 1)
  · **ก้อน 2 Checklist UAT เสร็จ ✅** — `docs/uat-checklist.md` (flow ครบวงจร + กันสิทธิ์ + ฟีเจอร์เด่น + go-live) · เอกสารล้วน
  · **= D9 เสร็จ** · ฟีเจอร์หลัก 10 ข้อครบ → ขึ้นรอบ MES-grade (D10–D12)
**เฟส 10 / D10 = เสร็จครบ ✅ (23 มิ.ย. 69)** — เติม brief CEO รอบ 1 (migration 0012–0019)
  · A0 จัดการผู้ใช้ + role admin · A1 เครื่องจักร · A2 วัตถุดิบ/เบิก · A3 Line Clearance (gate ก่อนผลิต) · A5 จำนวนคน + auto เลขงาน
**เฟส 11 / D11 = เสร็จครบ ✅ (24 มิ.ย. 69)** — เติม brief CEO รอบ 2 (migration 0020–0024)
  · A4 สูตร/BOM + รูปแบบบรรจุ + สถานีย่อย/route · A6 คลัง FG + in-process QC + จุดเก็บตัวอย่าง QA
**เฟส 12 / D12 = MES-grade เสร็จ 4 ก้อน ✅ (24 มิ.ย. 69)** — (migration 0025–0026)
  · **B3 Deviation** (`0025`) + GATE กัน QA→FG ถ้ามี deviation เปิดค้าง · verified DB
  · **B2 Lot Genealogy** หน้า `/trace` ไล่ย้อนล็อต (read view ไม่มี SQL) · deployed
  · **B1 eBR** หน้า `/board/[jobNo]/ebr` แฟ้มบันทึกการผลิต + พิมพ์ได้ (read view ไม่มี SQL) · deployed
  · **B4 Notification** (`0026`) กล่องแจ้งเตือน `/inbox` + กระดิ่ง · 4 triggers (reject/deviation/overdue/stuck) ส่งตาม role · verified DB
  · **ถัดไป (เลือกได้):** B5 Barcode/QR · B6 OEE/downtime · B7 Capacity · B8 Integration — หรือพัก UAT กับทีม
  · ⚠️ gotcha: push หลาย commit รวด Vercel อาจข้าม deploy → เช็ก `vercel ls pd-monitor --yes` · `SUPABASE_SECRET_KEY` ใน .env.local local ถูก rotate (verify ใช้ publishable key)
**ยกระดับคุณภาพจากรายงานผู้ทดสอบ = เสร็จครบ ✅ (1 ก.ค. 2569, migration 0027–0030)** — แก้บั๊กจริง 15/19 จาก Notion `production_qc_warehouse_bug_fix`
  · ก้อน 1 (frontend): login คงอีเมล · timezone Asia/Bangkok (`lib/format.ts`) · โชว์ note 3 จุด · seq สถานี +1
  · ก้อน 2 (`0027`–`0028`): ซ่อนงาน FG ที่รับเข้าคลังจากบอร์ด (`fg_received`) · QA sample เฉพาะ qa · บันทึกผลผลิตเฉพาะ in_production
  · ก้อน 3 (`0029`): แจ้ง role ปลายทางตอนเลื่อนขั้น (arrival) + auto-hide noti หมดหน้าที่ (`notifications.relevant_status`)
  · ก้อน 4 (`0030`): Deviation หมายเหตุแยกฝ่าย append-only (`deviation_comments`) + ปุ่มส่งกลับ QA (`submit_deviation_resolution`)
  · **เหลือทดสอบ UI จริงทุกก้อน** · **migration ล่าสุด = 0030**
**งานใหญ่ 2 ข้อที่เลื่อนไว้ = เสร็จครบ ✅ (1 ก.ค. 2569, migration 0031–0033)** — verified DB + deployed · เหลือทดสอบ UI จริง
  · **E1/E2** (`0031`–`0032`) ผูกสูตร/route กับงานตอนสร้าง (`jobs.recipe_id` + `job_routes` snapshot) + **GATE ส่ง QC ต้องตรวจ in-process ผ่านครบทุกสถานีในสูตร** (`inprocess_checks.station_id` + `inprocess_route_complete()`)
  · **F1** (`0033`) ระบบขออนุมัติแก้ไขย้อนหลัง (`edit_requests` + `request_edit`/`review_edit_request`/`cancel_edit_request`) ครอบ ผลผลิต/ใบเบิก/QC · อนุมัติ manager เสมอ · qa เฉพาะ QC · audit เก็บ old→new · หน้า `/edit-requests`
  · **migration ล่าสุด = 0033** · แผน: `~/.claude/plans/2-silly-volcano.md`

---

## 📅 บันทึกวันที่ 1 กรกฎาคม 2569 (session แก้บั๊ก UI มือถือ)
**แก้บั๊ก: ปุ่ม "ออกจากระบบ" หายบนมือถือจอแนวตั้ง = เสร็จ + deployed ✅**
- **อาการที่ผู้ใช้เจอ:** เปิดเว็บบนมือถือจอ**แนวตั้ง** ไม่เห็นปุ่มออกจากระบบ · พอหมุน**แนวนอน**ปุ่มกลับมา · iPad/คอมปกติ
- **สาเหตุจริง:** ในเมนู drawer มือถือ (`web/components/app-shell.tsx`) กล่อง panel เป็น `h-full` (block) แต่ข้างในมีแถวปุ่มปิด `X` วางซ้อนบน `sidebarInner` ที่กินความสูง 100% เต็ม → ความสูงรวมล้นเกินจอ ปุ่ม logout ล่างสุดถูกดันตกใต้ขอบจอ (panel ไม่มี scroll) · แนวนอนเห็นปุ่มเพราะจอกว้าง ≥768px ไปแสดง desktop sidebar แทน (ไม่เกี่ยวแนวตั้ง/นอนโดยตรง แต่เกี่ยวความกว้างข้าม breakpoint `md`)
- **สิ่งที่แก้ (2 จุด):**
  - panel ของ drawer: `h-full` → `flex h-dvh flex-col` (จัดคอลัมน์ + ใช้ dynamic viewport height หัก browser chrome มือถือ)
  - ครอบ `sidebarInner` ด้วย `<div className="min-h-0 flex-1">` → เมนูยาว scroll ในตัวเอง ปุ่มออกจากระบบตรึงล่างมองเห็นเสมอ
- **ไฟล์ที่เปลี่ยน:** `web/components/app-shell.tsx` (2 บรรทัด) · type-check ผ่าน · commit `6c271ec` push แล้ว → Vercel auto-deploy
- **▶️ พรุ่งนี้เริ่มตรงนี้ (next steps):**
  - ⏳ ยืนยัน UI จริงบนมือถือจอแนวตั้ง (เปิด https://pd-monitor.vercel.app หลัง deploy เสร็จ → กด hamburger → เห็นปุ่มออกจากระบบครบ)
  - ยังเหลือ **ทดสอบ UI จริง** งานค้างก่อนหน้า: migration 0027–0033 (E1/E2 gate in-process, F1 amendment, bug-fix 15 ข้อ)
  - งาน MES ที่เลือกทำต่อได้: B5 Barcode/QR · B6 OEE/downtime · B7 Capacity · B8 Integration — หรือพัก UAT กับทีม

---

## 📅 บันทึกวันที่ 1 กรกฎาคม 2569 (session เทสแอป + แก้บั๊ก QA อนุมัติแก้ไข)
**ผู้ใช้ขอ "เทสแอปว่ามีจุดไหนควรปรับ" → เทส + แก้ = เสร็จ + deployed ✅**

### วันนี้ทำอะไรไป
- **เทสแบบอ่านอย่างเดียว:** `npx tsc --noEmit` (สะอาด) + `npx eslint .` (เจอ 5 จุด) + ส่ง Explore 2 ตัวรีวิวโค้ดฟีเจอร์ล่าสุด (E1/E2/F1) และแกน auth/flow/timezone/realtime
- **ผลรวม: แอปแข็งแรงดี ไม่มีบั๊กร้ายแรง** — auth/redirect/flow(TRANSITIONS sync กับ RPC)/timezone(ใช้ util ครบ)/realtime(cleanup ครบ) ตรวจแล้วปลอดภัยทั้งหมด
- **เจอบั๊ก UX จริง 1 จุด (แก้แล้ว):** หน้า `/edit-requests` แสดงปุ่ม "อนุมัติ" ให้ QA บนคำขอทุกชนิด แต่ server (RPC `review_edit_request`) ให้ qa อนุมัติได้เฉพาะ `inprocess_check` → QA กดอนุมัติคำขอผลผลิต/ใบเบิกจะเจอ error เสมอ

### ไฟล์ที่เปลี่ยน (commit `46ffbd6`)
- **Fix 1 (UX สิทธิ์ QA — แก้เฉพาะฝั่ง UI ให้ตรง server ไม่แตะ SQL):**
  - `web/lib/data/edit-request-constants.ts` — เพิ่ม helper `canReviewEdit(roles, targetType)` สะท้อนกติกา RPC (manager/admin ทุกชนิด · qa เฉพาะ QC)
  - `web/app/(app)/edit-requests/page.tsx` + `edit-requests-view.tsx` — คำขอที่ผู้ใช้อนุมัติไม่ได้ → ซ่อนปุ่ม ขึ้น "🔒 คำขอนี้ต้องให้ผู้จัดการอนุมัติ"
  - `web/lib/data/edit-requests.ts` (`getPendingEditCount`) + `web/app/(app)/layout.tsx` — badge เมนูนับเฉพาะที่ผู้ดูอนุมัติได้จริง (qa = เฉพาะ inprocess_check)
- **Fix 2 (ESLint → 0 errors, cosmetic):** escape `"` ใน `deviations.tsx`+`trace/page.tsx` · eslint-disable + คอมเมนต์ที่ effect โหลดคิว offline ใน `record-form.tsx`
- **Notion:** โพสต์สรุปผลเทสเป็น sub-page ใต้หน้าโปรเจคหลัก → `39092ef2-c18f-81a1-8f02-d304dce54a94`

### ▶️ พรุ่งนี้เริ่มตรงนี้ (next steps)
- ⏳ **ยืนยัน UI จริง Fix 1:** login **qa** → `/edit-requests` → คำขอผลผลิต/ใบเบิก **ไม่มีปุ่มอนุมัติ** (ขึ้น "ต้องให้ผู้จัดการอนุมัติ") · คำขอ QC มีปุ่มปกติ · login **manager** เห็นปุ่มครบทุกใบ
- ยังเหลือ **ทดสอบ UI จริง** งานค้างเดิม: migration 0027–0033 (E1/E2 gate in-process, F1 amendment, bug-fix 15 ข้อ) + ปุ่ม logout มือถือแนวตั้ง
- งาน MES ที่เลือกทำต่อได้: B5 Barcode/QR · B6 OEE/downtime · B7 Capacity · B8 Integration — หรือพัก UAT กับทีม

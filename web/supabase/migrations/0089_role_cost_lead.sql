-- ============================================================
-- PD Monitor — Part F / 0089_role_cost_lead.sql
--   เพิ่มค่า enum app_role ตัวที่ 16: 'cost_lead' (หัวหน้าบัญชีต้นทุน)
--
-- 🚨 ไฟล์นี้ต้องมี "แค่บรรทัดเดียว" และต้องรันจบก่อนไฟล์ 0090
--    Postgres ใช้ค่า enum ที่เพิ่งเพิ่มภายใน transaction เดียวกันไม่ได้
--    (แพทเทิร์นเดียวกับ 0038 / 0060 / 0077 — ดู 0077:13-16)
--
-- 📌 ที่มา: 0077:18 เขียนไว้เองว่า "ไม่เพิ่ม cost_lead ตอนนี้ ถ้าวันหน้าต้องการ
--    ค่อยเพิ่มด้วย migration ใหม่ได้" — นี่คือรอบนั้น
--
-- 🔑 สิทธิ์ "เหมือนบัญชีต้นทุน" ได้มาเองโดยไม่ต้องแก้ guard ตัวไหนเลย
--    has_role() ให้ <ฝ่าย>_lead ผ่านสิทธิ์ของ <ฝ่าย> อยู่แล้ว (0078:50-68)
--
-- รันซ้ำได้ (if not exists)
-- ============================================================

alter type app_role add value if not exists 'cost_lead';

-- ============================================================
-- ✅ ตรวจหลัง paste
--   select count(*) from unnest(enum_range(null::app_role));   -- ต้องได้ 16
--   select 'cost_lead'::app_role;                              -- ต้องไม่ error
-- ============================================================
